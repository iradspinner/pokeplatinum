# Platinum Oxide: Tracker

Top-level task list. Tick items as they finish; add detail only where it changes what happens next. Companion to `docs/oxide/design-doc.md`.

Where things live, so each fact has one home: **status** is here; **durable facts** are in the design doc's findings log (section 8); the **encounter tool's** status, milestones and findings are in `docs/oxide/encounter-tool-build-plan.md`. Everywhere else points, it does not repeat.

**Where things stand (2026-09-21).** Phase 3, carrying the base ROM's edits into the source tree, is **finished**. All three hard stops were closed on 2026-09-20: the Repel prompt (fixed with a new script command), the Battleground init script (no init script at all now) and the trainer battle messages (no edit in the bank, only DSPRE zeroing unused slots). The encounter tool has M1 to M4 done and M5 next. **Phase 4, the engine port, has started.** Its engine-change list is settled (`docs/oxide/phase4-engine-change-answers.md`). **Elements 1 and 2 are done**: the Fairy type, apart from two pieces of fixed Pokedex and Battle Hall art listed under it, and the ability widening to u16 with a hidden-ability slot. Element 2 broke the save format, which is now tracked in `docs/oxide/save-layout.md`; a save made before it reads every ability as NONE, so start a new game. Also landed on 2026-09-20: the base ROM's last two gameplay rules, traded Pokemon always obeying and battling granting no EVs, the second of which makes vitamins the only EV source and raises them to 26 EVs each. **Element 3, the species-slot expansion, is most of the way done.** All 159 new species are in the tree with real data and real art, ids 494 to 652, so `MAX_SPECIES` is 654 and the three per-species archives hold 667 members. **Element 3's structure is finished as of 2026-09-21**, the caps audit being the last of it: it found and fixed one live defect, the alternate-form records for Deoxys, Wormadam, Giratina, Shaymin and Rotom being read from hardcoded archive members that the species insertion had moved. What is left under element 3 is all fill-in: TM and tutor and egg-move lists, Pokedex entry text, body shapes and dex scales, and footprints. The 30-box PC moved to element 8, where it already was. **Element 4, the move expansion, is next.** Nothing in Phase 4 is waiting on Ian. The tree is clean and `origin/oxide` is current. **Bug fixing is its own track from 2026-09-21**, run by a dedicated session: its status home is the "Open bug" entries under Phase 5 and nothing else in this file; it works from `~/roms/route202-hang.sav` and the pinned ROMs, uses `tools/oxide/live_watch.py` against Ian's Windows melonDS **with Ian driving the game** (recipe in `docs/oxide/setup-fork-and-wsl2.md` part 5b; the agent never runs its own melonDS, part 5's WSLg route is recorded there as a dead end and a 2026-09-22 session lost its budget to it), and records causes in the design doc's findings log. `git log -1` is the resume point; no commit hash is kept in this file because it goes stale within the hour.

**Second track: the encounter tool.** A separate tool for designing Oxide's wild encounter tables. It owns `tools/oxide/encounters/`, `res/field/encounters/`, `docs/oxide/encounters/` and the three `encounter-*.md` docs, and touches nothing else. **The tool is complete, all seven milestones done** (M1 round-trip I/O, M2 analysis, M3 linter, M4 browser editor, M5 dupe-out planner, M6 levels-only generator, M7 source-versus-ROM check; the generator's full species placement is deferred, possibly for good). **The authoring pass has started: writing every wild table from the species pick-list**, planned in `docs/oxide/encounter-authoring-plan.md` (all 171 land tables, early corridor first; the sidecar becomes the design source; its two-stage split is moot now that element 3 has put all 159 new species in the tree, so every pick-list line is placeable from Step 2 on and the reservation mechanism is not needed). Its Step 0, the tooling, is done (35/35), and Step 1 too (21/21): every area carries a progression `order`, the pick-list has its `tier` column, and R12 evaluates, failing 9 lines on the current tables. Step 2, the availability plan on paper, is next; Ian's review of the default `order` and `tier` is open and blocks nothing. How to resume cold, what each milestone found, and what it is waiting on are all in `docs/oxide/encounter-tool-build-plan.md`. This paragraph is the tool's whole footprint in this file; per-milestone entries go in the build plan, not here.

**Done in Phase 3, all verified against the base ROM:** species/move/evolution/learnset import; the trainer carry-over (all 928, ability/gender fields, 0 field mismatches, emulator-confirmed); all four synthetic-overlay routines (no items in trainer battles, Rare Candy chaining, uncapped battle frame rate, EV/IV viewer); encounters (125 tables); both in-game trades; map headers (58); the small constant edits; and the scripts, events and text: events 158 of 158, scripts 89 of 91 matching, with `scripts_common` and `scripts_init_battleground` intentionally diverged, text banks 72 of 78, with every mismatch explained under Phase 3 and none of them an outstanding edit. Three sets of base-ROM changes were deliberately *not* carried over, as DSPRE noise rather than edits, each with its evidence recorded: sprite heights, the six vitamin item records, and the encounter `unown_table`/`rate_form` fields.

**Next steps, in order:**

1. **Ian: the rest of the emulator pass** under "Waiting on Ian". The nurse hang is confirmed gone; the box deposit hang is confirmed still there and belongs to the bug track.
2. **Bug track:** the box deposit hang first, then the UNLOCK FPS ALWAYS crash entering Sandgem. Each bug entry under Phase 5 says what is known and which breakpoints to start from.
3. **Phase 4 element 3**, the species-slot expansion, down its remaining checklist (the fixed art, the code caps, the 30-box PC), then element 4, the move expansion.
4. **The encounter authoring pass**, Step 2 (the availability plan on paper) onward, on its own worktree branch; Ian's review of the default order and tiers is open and blocks nothing.
5. **Backlog items that are now cheap:** the six overworld sprites the carry-over missed (Phase 3 regression item), and the re-examination of the inventory's other "tool side effect" archives.

**To confirm the state after a restart**, from the repo root:

```
make rom
python3 tools/oxide/import_base_rom.py --base ~/roms/base.nds --vanilla ~/roms/vanilla.nds --dry-run   # every count 0
python3 tools/oxide/verify_narcs.py --built build/pokeplatinum.us.nds --ref ~/roms/base.nds
python3 tools/oxide/verify_narcs.py --built build/pokeplatinum.us.nds --encounters --source   # M7: built NARC vs res/ JSON
python3 tools/oxide/verify_narcs.py --built build/pokeplatinum.us.nds --ref ~/roms/base.nds --text
python3 tools/oxide/verify_narcs.py --built build/pokeplatinum.us.nds --ref ~/roms/base.nds --map-headers
python3 tools/oxide/bulk_scripts.py --dry-run   # would write 0; skips scripts_init_battleground and scripts_common
python3 tools/oxide/bulk_events.py --dry-run    # would write 0
python3 tools/oxide/bulk_text.py --dry-run      # would write 0; four generated banks skipped
python3 tools/oxide/scriptdis.py --rom ~/roms/vanilla.nds --verify
python3 tools/oxide/scriptdis.py --rom ~/roms/base.nds --verify --base-rom
```

The importer is idempotent, so a non-zero count means something moved. Both `scriptdis` runs should report 574 files walked, 0 failed, 550 init scripts read and 0 movement-target collisions.

**What "clean" looks like now that Phase 4 has changed these tables.** `verify_narcs.py` carries a `DIVERGED` list for Phase 4 changes, the same idea as the bulk tools' skip lists, and any Phase 4 element that changes a base-ROM table must add its members there or the gate fails. Since element 3 the three per-species archives no longer line up with the reference member for member, so the tool maps the reference's indices onto the built ones and reports each archive as a sentence rather than a diff. Expect exactly this:

- `pl_personal.narc`: 667 members against the reference's 508; **0 disagree**, 20 differ only at the intended bytes (the Fairy retypes), 159 are new species
- `wotbl.narc`: 667 against 508; 0 disagree, 159 new; 3 members differ only in trailing zero padding
- `evo.narc`: 667 against 508; 0 disagree, 159 new; 7 members differ on purpose (the natives that gain an evolution) and 501 differ only in trailing zero padding, which is the record going from 44 bytes to 56
- `pl_waza_tbl.narc`: identical apart from the intended bytes, the three Fairy moves
- the base ROM importer reports every count 0 and lists those same seven species' evolutions as not carried over

Anything else is a regression. The encounter tool's own checks are listed in its build plan. `tools/oxide/integrate.sh` runs all of this in one go. This whole list was last run clean on 2026-09-20 after Phase 4 element 3 landed.

**Files outside the repo that the tools need**, all of them copies, none of them rebuildable from here: `~/roms/base.nds` (the base ROM), `~/roms/vanilla.nds` (a byte-exact Rev 1 build, made once from `main`) and `~/roms/hardlove.nds` (the donor). If any goes missing, copy it again from the project folder on the G: drive; the paths are in the design doc and `docs/oxide/donor-tables.md`.

**Waiting on Ian:**

- Emulator checks not yet done: Rare Candy chaining; the frame-rate uncap (options menu reads UNLOCK FPS, with OFF / BATTLE / ALWAYS); Shinx's ability (always Rivalry, never Intimidate) and Bidoof/Starly hatch time (~255 steps, down from ~3825); and a walk through a few bulk-generated maps, the gift houses being the obvious ones. The "use another Repel?" prompt is now safe to answer yes; it is worth testing, since it is the one thing in this build that was broken and is now fixed rather than carried over.
- The Route 201 table in the working tree is temporarily all Glimmora, an uncommitted element 3 test by another session, and rides along in any local build until that session commits or reverts it.
- The ability/gender nibble direction (1=male, 2=female) is a guess; no Route 202 trainer exercises it. Swap if anything reads wrong in-game.
- **Start a new game first**: the old save reads every ability as NONE by design (element 2 moved the field) and is no longer a valid test bed.
- After the overworld-sprite carry-over lands (Phase 3, regression item): the NPC outside Sandgem's Pokemon Center draws correctly, and so do the Clown-type NPCs on the other 34 maps that share that sprite slot.
- Fairy in the emulator (Phase 5 has the exact checks): a Dragon move doing nothing to Clefairy or Ralts, the summary screen reading FAIRY, and the Pokedex info page showing the NORMAL plate rather than garbage.
- Two data questions from the carry-over, low priority because the default is to leave vanilla: whether the encounter `unown_table`/`rate_form` changes or the sprite-height changes were ever intended (evidence under Phase 3 says they were DSPRE re-saves).
- Encounter tool, none blocking: Ian's review of the default `order` and `tier` is done (2026-09-21) and recorded as decisions 5, 6, 9 and 11 in `encounter-authoring-plan.md`: one order correction (Old Chateau after Route 211 west), a `split` field per area to derive order from story gates, Unown stays off, surf tables designed but counted only where the water is reachable, and lines the game gives with certainty (Togepi, Riolu) barred from every table. Tiers and cost ceilings stay as committed. Still open: any off-list species the leak audit finds in scripts.
- Tabled, not blocking anything: the 21 native species where `New Pokedex.xlsx` and the base ROM disagree on stats (see Phase 0).

## Phase 0: Setup

- [x] Working folder created, both ROMs and both DSPRE extractions in place
- [x] Design doc v0.1 and this tracker written
- [x] Ian confirms Hardlove is an hg-engine build (2026-09-15)
- [x] Content scope set: species subset (Ian picks), all new moves with scripts/sprites/animations, all new abilities (2026-09-15)
- [x] Ian finalizes the "other engine changes" list in design doc section 3 (2026-09-20, `docs/oxide/phase4-engine-change-answers.md`)
- [x] Ian produces the species pick-list (2026-09-15, landed in the repo 2026-09-20 as `docs/oxide/species-pick-list.md` and `.csv`). 360 rows: 199 already in Platinum, 159 new slots, 2 cut (Toxel/Toxtricity, which need nature-conditional evolution Gen 4 has no method for), so 358 obtainable entries and 652 species in the finished ROM. Key decision: it is an **availability list, not a deletion list**; all 493 natives stay in the tree, because the 928 carried-over trainers use species the curated dex omits. National Dex IDs stay as internal species IDs, so the donor's index-equals-dex-plus-50 rule keeps working. Verified locally: all 21 rows of its sheet-vs-base-ROM conflict table match what is actually in `res/pokemon` today
- [ ] **Tabled (Ian, 2026-09-20): the 21 native species where `New Pokedex.xlsx` and the base ROM disagree on base stats.** Not a per-row decision to make now. It folds into a larger balance pass over the whole existing dex, which Ian wants to approach with analysis and automation tooling built first. The base ROM's values are what `res/` holds today and they stay until that pass happens. The conflict table is in `docs/oxide/species-pick-list.md`
- [x] **Evolution triggers for the two alt-evolution megas**, Gyarados M and Lopunny M. Answered in `docs/oxide/phase4-engine-change-answers.md` Q4: level-up holding a stone-like item, entered twice as methods 18 (day) and 19 (night), with Dragon Scale and Fist Plate as the proposed items and the choice open to change during the species port. This was listed as outstanding in three places for longer than it was actually open; Ian pointed that out on 2026-09-20

## Phase 1: Find out where Hardlove's expansions live

Done 2026-09-15. Write-up: `docs/oxide/phase1-hg-engine-survey.md`.

- [x] Read hg-engine's repo structure, `CONFIG.md` and wiki; list every engine expansion and how each is injected
- [x] Map each scoped element to hg-engine source files and the ROM files it changes
- [x] Confirm the map against the Hardlove extraction (overlays 129..151 and numbering match; specific config toggles not determinable from the extraction)
- [x] Locate Platinum's type chart, ability field, species/move tables (named in design doc section 8; exact addresses come from a pokeplatinum build in Phase 3)
- [x] Check plat-engine's state (dormant since 2023-08, ~10% of hook surface, same architecture as hg-engine)
- [x] Inspect the VSMaker2 Data folder (stale duplicate extraction; ignore)
- [x] Write findings into design doc section 8

## Phase 2: Approach decision

Done 2026-09-15. Write-up: `docs/oxide/phase2-approach-breakdown.md`.

- [x] Write the considerations breakdown for Ian (recommends C; verified by building a matching ROM from the decomp)
- [x] Ian picks the approach: C (2026-09-15)
- [x] Ian answers the logistics questions: keep overworld/trainer/stat/move edits; has GitHub, will fork; wants a WSL2 runthrough; Phase 4 order approved; battle AI added as an engine change, Hardlove AI excluded
- [x] Finalize the scope table in design doc section 3 (2026-09-20, with the engine-change answers)

## Phase 3: Tooling and setup (approach C)

- [x] Ian forks pret/pokeplatinum: https://github.com/iradspinner/pokeplatinum (2026-09-15)
- [x] GitHub access for Claude. Closed 2026-09-20: `git push origin oxide` from the WSL2 checkout works with Ian's own credentials and has done so reliably. The 2026-09-15 note about the app's proxy stripping credentials was about a different environment; it does not apply here. Commits no longer need to be treated as session-local
- [x] Ian sets up WSL2 and builds the fork (`docs/oxide/setup-fork-and-wsl2.md`). Done by 2026-09-15: `~/pokeplatinum` in WSL2 is the working checkout and every build since has run there
- [x] Claude clones the fork and confirms a matching build from it (SHA-1 0862ec35..., 2026-09-15)
- [x] Diff the base ROM against vanilla (`docs/oxide/phase3-base-rom-inventory.md`, with its corrections listed at the top of that file)
- [x] Ian answers the inventory's five Qs (2026-09-15): port all four synthetic-overlay routines; shiny odds raise ports as a one-constant change, palette hue shift deferred/droppable; Battle Arcade script commands port only if the carried-over field scripts call them; apply all eleven battle_edits fixes. Full detail: `docs/oxide/phase3-answers-and-trainer-format.md`
- [x] Correction (2026-09-15): the base ROM's trainer party data is vanilla format, not expanded; no trainer struct/loader extension needed. See `docs/oxide/phase3-answers-and-trainer-format.md` section 2. Replaced by the four items below.
- [x] Add optional `ability` (0|1|2) and `gender` (null|"male"|"female") per-mon fields to trainer JSON and to `tools/dataproc/src/trainerproc.c` (commit 5b8958368)
- [x] Make `TrainerData_BuildParty` (`src/trainer_data.c`) honor those fields, implemented correctly (pick a personality that satisfies the requested gender for that species' ratio) rather than copying the base ROM's buggy +/-2 nudge (commit 5b8958368); verified by inspecting the built `trdata`/`trpoke` NARCs directly, unset fields decode back to ability=0/gender=0 matching prior behavior
- [x] Extend `tools/oxide/import_base_rom.py` with a trainer importer (commit 3877a7641). Extended `jsonstyle.py` with array-index path support and `insert_key()` to reach into `party`'s list of objects. Nibble-direction question resolved: 1=male, 2=female (Ian's guess, 2026-09-15; swap later if anything reads wrong in-game)
- [x] Carried over all 928 trainers (header + party, including ability/gender). The 775 with unchanged party size were patched per-field; the other 153 (107 grow, 46 shrink; Ian resized their parties) had their whole `party` array rewritten in one shot instead of building separate insert/remove-element JSON tooling (commit 92527f42b)
- [x] Verified: `make rom` succeeds. Byte-identical NARC comparison no longer applies (the trainer-mon struct is 2 bytes wider now), so verification decodes both sides field-by-field instead: 0 mismatches across all 928 trainers, header and party
- [x] Emulator-confirmed by Ian (2026-09-15) against Route 202 (Tristan/Logan/Natalie, all bumped to Lv.7, Logan's Burmy using its new moveset): trainer data import is correct in-game, not just in the built NARCs
- [x] No items in trainer battles (commit 26e9b71d9): player side extends the existing `BATTLE_TYPE_FRONTIER_LINK` item-block in `BattleControllerPlayer_CommandSelectionInput` to also cover `BATTLE_TYPE_TRAINER`; AI side stops `BattleControllerPlayer_InitAI` from populating trainer items at all. hg-engine reference: `DISABLE_ITEMS_IN_TRAINER_BATTLE`. Emulator-confirmed by Ian (2026-09-15): Bag blocked with the "can't be used here" message in a Route 202 trainer battle
- [x] Rare Candy chaining (commit 5690f9203): `PartyMenuCB_LevelUp`'s post-level-up state now checks the bag for more of the used item and, if any remain, exits with a new `PARTY_MENU_EXIT_CODE_USE_ITEM_AGAIN` that `start_menu.c` handles by reopening the party menu straight into item-use (same item, mon preselected) instead of returning to the bag. hg-engine reference: `PartyMenu_ItemUseFunc_LevelUpLearnMovesLoop_Case6` in `src/party_menu.c`. **Not yet emulator-tested** (Ian had no Rare Candy on hand); still only verified by `make rom` succeeding
- [x] EV/IV viewer on the summary screen (commit 9af3fe16d). Reverse-engineered from the base ROM's own `EV+IV_Viewer` routine rather than from hg-engine's `IMPLEMENT_NEW_EV_IV_VIEWER`: the base splices `HandleInput_Main+0x34` so R cycles a mode byte 0/1/2, and `SetMonDataFromMon+0x1B2` so the seven stat reads become `MON_DATA_HP_IV..` in mode 1 and `MON_DATA_HP_EV..` in mode 2. In the decomp, `PokemonSummaryMonData` gains `ivs[STAT_MAX]`/`evs[STAT_MAX]` and `DrawSkillsPageWindows` picks which set to print; R is handled in `HandleInput_Main`. Scope held to numbers-only (2026-09-15): the HP/ATTACK/... labels are unchanged in all three views, so no text bank is touched. Three deliberate differences from the base ROM, all in the commit message: the real stats are not overwritten (the base ROM's overwrite makes `DrawHealthBar` divide by zero on a mon with 0 HP EVs), R is `JOY_NEW` not `JOY_REPEAT`, and the refresh is `PokemonSummaryScreen_DrawExtraWindows` rather than `ChangeSummaryMon(screen, 0)` (which replays the cry and can loop forever on an egg). **Not yet emulator-tested**; only verified by `make rom` and by reading the rebuilt arm9
- [x] Uncapped battle frame rate (commit 83770cd5b). Not ported from hg-engine's reference (raw unsymbolized ARM patch, no clean source) but reverse-engineered directly from the base ROM's own synthetic overlay (`weather_sys.narc` member 9, confirmed loaded at `0x023C8000` by diffing base vs `~/roms/vanilla.nds` arm9 and tracing the `NitroMain` boot hook byte-for-byte). `ShouldWaitForVBlank()` in `src/main.c` gates the main loop's `OS_WaitIrq` on `Options_ButtonMode`, repurposing `BUTTON_MODE_START_IS_X` (battle-only uncap) and `BUTTON_MODE_SWAP_XY` (always uncap) exactly as the base ROM does. The options-menu relabel (UNLOCK FPS, OFF / BATTLE / ALWAYS) came over with the text import on 2026-09-20, and exposed two defects in the selector that are fixed in commit 7f2b398e1. The message ids in `res/text/options_menu.json` still read `OptionsMenu_Text_ButtonMode*`; renaming them would mean touching `src/applications/options_menu.c` too, so they were left alone
- [x] Conversion scripts for species, moves, evolutions, learnsets: `tools/oxide/import_base_rom.py` + `verify_narcs.py`; 415 species and 108 move files updated; rebuilt NARCs verified byte-identical to the base ROM (commit c69ef83). The per-species diff of that import is the commit itself and the importer's regenerated `tools/oxide/import_report.md`; the frozen copy once kept under `docs/oxide/` was deleted 2026-09-20 as a duplicate
- [x] Conversion scripts for encounters and trades; heights and items deliberately not carried over (commits 7c2f36ead, 7f2b398e1)
  - [x] **Encounters**: 125 of 183 tables imported and verified field-by-field (`verify_narcs.py --encounters`). `unown_table` and `rate_form0..4` are excluded as DSPRE normalisation, not edits: 98 `unown_table` changes are 0 -> 1, which the game reads identically, and the 17 real ones set every Solaceon Ruins room to 8 (`UnownOnlyExcQue`), which would leave the whole ruins spawning only Unown ! and ? and make the F-R-I-E-N-D letters unobtainable. `rate_form0/1` picks the Shellos/Gastrodon form; 61 of the 63 changed tables contain neither species. Listed under "Waiting on Ian" in case either was intended
  - [x] **Trades**: both edited records imported and byte-verified. Kazza's Abra is now a Ditto with perfect IVs holding a Destiny Knot; Gaspar's Haunter is a Gengar. The json filenames still say abra and haunter
  - [x] **Heights: not carried over.** 298 of 1,976 `height.narc` members differ, but 164 of those write a byte where vanilla has an empty member (a gender the species does not have, which the decomp derives from the gender ratio and cannot express), and of the 116 species where vanilla had male and female offsets equal, every single one has only the male offset changed. A hand edit to a shared sprite would move both. Reads as the same DSPRE re-save that touched 2,788 of 2,964 `pl_pokegra` files. Listed under "Waiting on Ian"
  - [x] **Items: not carried over.** The only six differing records are the vitamins, and each grew from `ItemData`'s 34 bytes to 36 with the EV amount zeroed and everything after it shifted one byte right, so the friendship values no longer line up with the struct. That is DSPRE writing a malformed record, not an edit. The vitamin change Ian described is the EV cap (100 -> 252), which is a code edit and is done under the small constant edits
- [x] Carry over trainers. Superseded by the trainer items above: all 928 trainer records were carried over, not just the 488 the first diff counted as changed
- [x] Carry over map-header edits (commit 56b6609db). 58 of 593 headers: 57 weather and one battle background, all hand-made. The three type gyms get matching weather so the battle inherits it (Oreburgh sandstorm, Pastoria heavy rain, Snowpoint blizzard), Bertha's room sandstorm and Flint's ashfall, Mt Coronet B1F / Victory Road / every Turnback Cave room lose their fog, and Turnback Cave's Giratina room switches its battle background to the Distortion World. Verified byte-identical to the base ROM with `verify_narcs.py --map-headers`, which finds the table in both ROMs by content rather than by a build address
- [x] Carry over text. The 18 banks whose message count was unchanged were imported by index and verified message-for-message (`verify_narcs.py --text`): the options-menu relabel, Vs. Seeker renamed Pocket PC, Attack Order's description, 71 trainer names. The remaining 62 banks, the NPC dialogue for the field scripts, came over in the bulk pass below. Where a base-ROM bank filled a slot vanilla leaves unused (the Repel menu entries, a Teleport System entry, two Wayward Cave lines), the new message got a generated id; the code or script that calls it is a separate matter, see hard stop 3
- [x] Carry over scripts, events and their text. Six maps were rewritten by hand in the repo's idiom (`oreburgh_city_middle_house`, `sandgem_town_house`, `solaceon_town_northeast_house`, `floaroma_meadow_house`, `mt_coronet_1f_north_room_2`, `iron_island_b3f`) and 70 events-only maps patched; then **the rest was generated in bulk on 2026-09-20 at Ian's direction**. Against the base ROM: **events 158 of 158, scripts 89 of 91 matching, with `scripts_common` and `scripts_init_battleground` intentionally diverged, text banks 72 of 78**, no regressions. Method and tools: `docs/oxide/phase3-scripts-and-events-plan.md`
  - Tools: `tools/oxide/bulk_scripts.py`, `bulk_events.py`, `bulk_text.py` (each skips anything the current build already gets right, so the six hand-written maps keep their idiomatic source); `mapdiff.py` and `checkmap.py` for one map at a time; `scriptdis.py` underneath all of them
  - **The cost, and the thing to undo later.** The 86 generated scripts have machine labels (`JubilifeCity_0A4C`) and numeric operands where hand-written source would use named labels, `NPCMessage` and text-bank constants. New object events are named `LOCALID_OBJECT_<n>` and new messages `<Bank>_Text_<n>`. All correct, none readable, and it replaces hand-written upstream source, which is what the project's no-reformatting rule exists to protect. Every generated script says so at the top and the hand-written versions are in git history, so they can be re-humanised a map at a time (backlog)
  - [x] **Hard stop 1 closed (2026-09-20): `scripts_init_battleground`.** The base ROM's member starts with a zero, and that zero is the terminator: `FieldSystem_GetFixedInitScriptID` and `FieldSystem_GetFrameTableInitScriptID` both stop on it and return 0xffff. **The Battleground has no init script at all now**, where vanilla ran script 4 on transition and consulted a frame table on `VAR_BATTLEGROUND_STATE`. That fits the rest of the map being gutted. The 53 bytes after the terminator are the old table, left in place by DSPRE and never read; this builds the 4-byte equivalent instead, which the engine cannot distinguish. Listed in `bulk_scripts.py`'s `DIVERGED`. It was never a format problem: the earlier note said it needed its own emitter, when the whole content is one byte
  - [x] **Hard stop 2 closed (2026-09-20): `TEXT_BANK_NPC_TRAINER_MESSAGES` contains no edit.** The mapping never needed trainerproc's packing order: `poketool/trmsg/trtbl.narc` is that mapping, one (trainerID, messageType) pair per bank slot, already in the ROM and byte-identical between the ROMs. `tools/oxide/import_trainer_messages.py --check` validates all 2,497 slots against `res/trainers/data`. With the mapping in hand, **all 373 differences are unused slots whose filler byte DSPRE zeroed, every one on a DUMMY trainer**; no message text differs anywhere. Fourth instance of the same DSPRE re-save noise, after sprite heights, the vitamin item records and the encounter `unown_table`/`rate_form` fields, so vanilla's values are kept
  - [x] **Hard stop 3 closed (2026-09-20): the Repel prompt.** `SetRepelSteps <count>` added as a real script command, writing through `SpecialEncounter_GetRepelSteps`, registered at the end of the command table so no existing opcode moves; the three branches in `scripts_common` use it and drop their `ScrCmd_Unused_007` pokes. The base ROM's workaround (poke a fixed scratch address, then a hand-written routine in place of `Dummy088` copies it through the save pointer) is not reproduced. **`scripts_common` is now 15 bytes shorter than the base ROM's and is the first file that deliberately does not match it**, so `bulk_scripts.py` carries a `DIVERGED` list and refuses to regenerate it. Without that the restart check-list would silently undo the fix
  - Not defects, recorded so nobody re-opens them: `SPECIES_NAME` and `SPECIES_POKEDEX_ENTRY_EN` decode identically already; `NPC_TRAINER_NAMES` differs only in the `{TRNAME}` tag DSPRE re-applied across the bank; `SOLACEON_TOWN_NORTH_HOUSE` and `SURVIVAL_AREA_SOUTH_HOUSE` have identical text but DSPRE re-encrypted them with key 0xd43f where vanilla uses a per-bank key, and any key decodes the same; `ITEM_NAMES_WITH_ARTICLES` differs because renaming Vs. Seeker to Pocket PC propagates to the derived bank here where DSPRE left "a Vs. Seeker" behind
- [ ] **Regression found in play (Ian, 2026-09-20): the base ROM's custom overworld sprites were not carried over.** Ian saw a red glyph where an NPC stands outside Sandgem's Pokemon Center. That NPC is object 15 of `events_sandgem_town.json`, a base-ROM addition with `OBJ_EVENT_GFX_DUMMY_075`, and 43 object events on 35 maps use dummy graphics slots the same way (39 use `DUMMY_075`; `DUMMY_057`, `058`, `061`, `066` once each, on Mt Coronet outside north and south, Resort Area and Stark Mountain room 1). The base ROM fills those slots: `data/mmodel/mmodel.narc` members 55, 56, 59, 64, 65 and 73 (`dummy_0.nsbtx` to `dummy_5.nsbtx` in `res/graphics/field_sprites/field_sprites.order`) are 1,640-byte placeholders in vanilla and real sprites in the base ROM (6,760 bytes each, one 13,352, one 4,544). The Phase 3 inventory listed that archive's six changed members as "building models" under tool side effects; they are overworld sprites and they are Ian's edits. Overlay 5's sprite tables are unchanged in the base ROM (its four ov5 differences are all in code), so vanilla's dummy rows already describe these slots as generic 32x32 walking sprites in the follower palette group, and the fix is data only: extract the six members from `~/roms/base.nds` and build them in place of `dummy.png` for those six entries in `res/graphics/field_sprites/meson.build` (prebuilt NSBTX is the faithful route; converting back to PNG is optional). Verify by rebuilding and comparing `mmodel.narc` member for member against the base ROM, then Ian looks at the Sandgem NPC. **Follow-up in the same spirit:** re-examine the inventory's other "tool side effect" archives (`trfgra` 8 trainer sprites, `pl_batt_bg` 78, `pl_batt_obj` 50, `pl_otherpoke` 26, `titledemo` 2, `item_icon` 1, `waza_particle` 1) with the same test, a member growing from a placeholder size or changing beyond its header is an edit, not a re-save
- [x] Re-apply the base ROM's small constant edits as C changes (commit 9f901de9c): shiny threshold 8 to 255 (~1/8192 to ~1/257), vitamin EV cap 100 to 252 (`MAX_EV_VITAMIN`; the total is still capped by `MAX_EVS_ALL_STATS`), new-game defaults of fast text and SET battle style, HMs forgettable from the summary screen's move-select (the `HandleInput_SelectMove+0x8E` edit the inventory had misfiled under the EV/IV viewer), and **reusable TMs**. That last one is a correction: the inventory read `TeachMove+0x5B` as "probably HMs can be forgotten", but the call it skips is `Bag_TryRemoveItem`, so the base ROM simply never takes a TM out of the bag. Each edit was traced to the exact instruction the base ROM changed and confirmed in the rebuilt arm9
- [ ] Still not ported from the base ROM's arm9, all needing real understanding rather than a constant swap: the Vs. Seeker repurposed as a portable PC (`UseVsSeekerFromMenu`, two sites, pairs with the item text already imported); the palette hue-shift patch (six hooks into code written over the Battle Arcade region, deferred as low priority, droppable); and a single two-byte change to a lookup table at arm9 `0x0EC478` that has not been identified. The Battle Arcade custom commands are closed: only `Dummy088` is called (hard stop 3), the rest is dropped
- [x] Working `git pull && make rom` loop with Ian, verified end to end with one visible change (2026-09-15)

## Phase 4: Port, one element at a time

Started 2026-09-20 with the Fairy type. The order was approved by Ian on 2026-09-15 (from `docs/oxide/phase2-approach-breakdown.md` section 6), chosen so each step is testable on its own and the structural changes come first while the tree is still close to vanilla. hg-engine's source is the reference implementation for each feature; Hardlove's tables are the content source. Each element gets its own checklist here when it starts, with the emulator test that proves it.

Prerequisites, none of which touch a Phase 3 file:

- [x] **Read Hardlove's `a/0/2/8` members 9_07..9_13 directly (2026-09-20).** Written up in `docs/oxide/donor-tables.md`, with the evidence for each member, since none of it came from hg-engine's source. 7 hidden abilities, 8 base experience, 9 icon palettes, 11 form data, 12 form-to-species, 13 reversion, and 10 is not needed by the port and is recorded only so nobody re-derives it. The reader is a new `tools/oxide/donor.py`, which also decodes the species records, the learnsets and the evolutions; `tools/oxide/import_donor.py` is the writing half. The donor ROM is pinned at `~/roms/hardlove.nds`
- [x] Ian's "other engine changes" list. **Answered 2026-09-20: `docs/oxide/phase4-engine-change-answers.md`**, grounded in what the donor ROM actually shows rather than hg-engine's menu. Two findings the Phase 1 survey could not get from the extraction: Hardlove's item table is **2,687 records of 36 bytes against Platinum's 446 of 34**, and its evolution records hold **9 slots and use 19 methods Platinum does not have**. The menu is also shorter than it looks, because much of it arrived with the base ROM in Phase 3
- [x] **Species-ID scheme settled (Ian, 2026-09-20): numerically increasing after the end of the Platinum dex.** The National-Dex-as-internal-id idea from the pick-list is dropped; it would have grown every species-indexed table to 1010 slots plus forms to hold 159 species. New species append after `SPECIES_ARCEUS`, taking **494 to 652** in `dex_pos` order, and `SPECIES_EGG` and `SPECIES_BAD_EGG` move up to 653 and 654. Everything sized by the species count derives from the enum, so that one insertion carries `MAX_SPECIES`, `NATIONAL_DEX_COUNT`, `MOVESET_FORM_*`, the sprite-offset count and the three per-species archives (508 to 667 members) with it. The ten regional forms and the two alt-evolution megas take ids of their own rather than form records, because each has its own `dex_pos` and Gen 4 forms cannot have separate dex entries. Written up in `docs/oxide/species-id-scheme.md`, assignment in `docs/oxide/species-id-map.csv`, **frozen once element 3 lands**

Elements, in order:

- [x] **1. Fairy type (2026-09-20).** Done in two commits, the mechanism and then the Generation 6 retypings, so either can be reverted alone.
  - `TYPE_FAIRY` goes on the end of `generated/pokemon_types.txt`: type 18, `NUM_POKEMON_TYPES` 19. On the end rather than in a gap, because Hidden Power's "skip the Mystery type" arithmetic then needs no change and Fairy is not a Hidden Power type in Generation 6 either
  - Three matchup tables: `sTypeMatchupMultipliers` (battle engine and AI both read it), `sMoveTesterTypeChart` (the Poketch app's square chart, so every row gained a column), and they agree entry for entry. The twelve Fairy entries sit **before** the `0xFE` marker in the battle table, because everything after that marker is what Foresight ignores
  - `res/text/pokemon_type_names.json` gains FAIRY as message 18. `res/graphics/battle/type_icons/fairy.png` is a new 32x16 plate drawn against sub-palette 1 of `shared.pal`, the one with the pinks; it reuses colours already there rather than adding a fourth sub-palette, because the palette count is baked into every screen that shows a type icon. `sMovePaletteFairy` gives the move-select box its pink
  - **The trap, for anyone adding a type later:** the species processor packs the dex index archive as one bucket per type and *then* one per body shape, so an eighteenth type bucket silently shifts every body-shape bucket down one. `PDSI_FAIRY` is inserted into `pokedex_sort.c`'s positional enum to match, with `FT_FAIRY` beside it
  - Retyped to Generation 6: twenty species (the Clefairy, Jigglypuff, Togepi, Marill, Snubbull and Ralts lines plus Mawile and the Mr. Mime line) and three moves (Charm, Sweet Kiss, Moonlight). Each species was checked against vanilla first so no base ROM edit was overwritten; none of the twenty had one. **Which species end up Fairy is a Phase 5 balance question**; this is only the canonical set
  - Arceus plates are **not** done and cannot be: Multitype reads the held item's `HOLD_EFFECT_ARCEUS_*`, and there is no Pixie Plate item. It belongs with Phase 4 element 7, the item pass
- [ ] **Fairy, the two pieces left, both fixed art.** Neither affects battle:
  - The **Pokedex info page** draws type plates from its own packed atlas (`res/graphics/pokedex/type_icons.png` plus its cell, anim and palette data), which still holds eighteen. A Fairy Pokemon shows the NORMAL plate there. `PokedexGraphics_GetAnimIDfromType` gained a `default` at the same time, because vanilla left `animID` uninitialised for any type it did not list, which a new type walked straight into
  - The **Pokedex search grid** and the **Battle Hall grid** are fixed cell layouts with no Fairy cell, so neither offers the type. `FT_FAIRY` and the Battle Hall's type-indexed trainer class row exist so the data is ready when the art is
- Known and intended: the built ROM's dex index archive and battle sprite NARC each hold one member more than vanilla, and `pokemon_type_names` one message more. `verify_narcs.py` against the base ROM will report all three; they are the Fairy type, not a regression
- [x] **2. Ability field to u16, plus the hidden-ability slot (2026-09-20).** The id space is now 16 bits everywhere an ability is stored or passed; nothing caps it at 512 or at anything else, so the cap is whatever `generated/abilities.txt` grows to.
  - **Species record.** `u8 abilities[2]` became `u16 abilities[3]`, which takes `pl_personal.narc` from 44 bytes a record to 48 and shifts everything after the abilities by four. Slot 2 is the hidden ability and `ABILITY_NONE` there means the species has none, which is all 508 of them today. `SPECIES_DATA_ABILITY_HIDDEN` reads it. In the species JSON the third entry is optional, so five hundred files did not need an `ABILITY_NONE` written into them
  - **Stored ability.** Block A of a boxed Pokemon was full, so the ability moved to block B's spare u16 at 0x1A and block A's byte is dead. `MON_DATA_UNUSED_114`, the only accessor for that spare, lost its cases; its enum member stays so nothing else shifts. Written up in `docs/oxide/save-layout.md`, which is the file Phase 5 asked for
  - **Everything else that held an ability in a byte**: `BattleMon`, `Battler_Ability`'s return, `DamageCalcParams`, `RefreshPartyStatusMessage`, the summary screen, the wild-encounter field params, the daycare and AI locals. `trainer_data.h`'s `u8 ability` is a slot selector, not an id, and stayed. `FrontierPokemon` is a packed data record and also stayed a byte, so the one call site that fed it to the setter now widens it first rather than reading a byte of friendship along with it
  - **Hidden abilities are a mechanism, not yet a policy.** `Pokemon_TryGiveHiddenAbility` switches a Pokemon to slot 2 and returns FALSE when the species has none, and a new `GiveHiddenAbility partySlot, success` script command exposes it. Nothing calls it: which encounters and gifts hand out hidden abilities is the Phase 5 encounter-design question, and no species has a hidden ability until element 3 brings Hardlove's `a/0/2/8` table across
  - Verified by rebuilding: all 508 personal records agree with the base ROM field for field once the four-byte shift is applied; a temporary hidden ability on Clefairy read back correctly from the built NARC and was then reverted; the script disassembler still round-trips 574 of 574 with the new opcode in its table; the three bulk dry-runs still report nothing to do
- [x] **Emulator check for element 2, done (Ian, 2026-09-20).** Every existing Pokemon's ability had moved to a different block, so if the move were wrong every ability in the game would have been silently gone. Confirmed on a new save: Piplup shows an ability and the starter battle runs, which is the in-play proof that the moved field reads and writes correctly. An old save still reads every ability as NONE, so it stays a new-save-only build

- [ ] **3. Species-slot expansion.** Started 2026-09-20. Raise `MAX_SPECIES`, expand personal/evo/learnset/sprite/icon/dex/text tables and the save's dex flags, PC to 30 boxes. Evolution records go to nine slots. The overworld sprite import should cover every species, not only the ones Platinum shows, so a follower system stays possible later. Bring in the 159 pick-list species from Hardlove's 44-byte `SpeciesData` records, with the `a/0/2/8` tables from the prerequisite. Test: a new species is catchable, appears in the dex, and battles. Step by step:
  - [x] **The ability list (2026-09-20).** The 159 need 59 ability ids Platinum does not have, so this comes first. The whole donor table came across rather than those 59: ids 0..123 already agree, so importing 124..318 contiguously makes donor ids and Oxide ids the same number for good and spares every later import a lookup table. Names, uppercase names and descriptions are in `res/text`; the **effects are element 5**, so all 195 are inert. Where Hardlove reuses a display name (As One twice, Embody Aspect four times, Placeholder twice) the repeats take a numeric suffix rather than a guess at which form each one belongs to
  - [x] **The move list does not block this (measured 2026-09-20).** Learnsets reference moves Platinum does not have, and `SpeciesLearnsetEntry` packs `move:9 / level:7`, so it caps at 511 against the donor's 924. Measured against the 159: **2,046 of 2,571 learnset entries survive a filter to Platinum's own 467 moves, 80 per cent, no species is left with an empty learnset and the median keeps 13 of 15.** So the species records can land now with filtered learnsets and be refilled when element 4 widens the format and brings the moves. Twenty-eight entries are at level 0, hg-engine's evolution-move marker, which Platinum has no concept of and which become level 1
  - [x] **The sprite pipeline is proven (2026-09-20).** The donor's `a/0/0/4` is laid out exactly like Platinum's `pl_pokegra`, six members per species in the same order, uncompressed NCGR and NCLR. `nitrogfx <member> <out>.png -encodefronttoback -palette <normal.NCLR>` decodes a sprite and writes the `.key` the build needs to re-encode it, and an NCLR converts straight to the JASC `.pal` the build wants. Checked visually on Annihilape: both animation frames, correct palette
  - [x] **The species records and the graphics (2026-09-20).** All 159 written by `tools/oxide/import_donor.py species`, which is idempotent and re-runnable. **Three sources, each authoritative for different fields**, the split and the evidence in `docs/oxide/donor-tables.md`: base stats, types, both abilities and the hidden ability from `New Pokedex.xlsx`, which matches all 159 by `dex_pos` and name; everything else from the donor, including sprites, palettes, icons and cries. The sheet and the donor disagree on stats for only the two alt-evolutions, on types for four species and on abilities for about 124, because Hardlove redesigns abilities wholesale
  - [x] **Cries (2026-09-20, fixed 2026-09-21 after Ian found them broken).** The donor keeps one wave archive per species, `WAVE_ARC_PV<species>`, each a single IMA-ADPCM sample; 150 of the 159 have one and the nine that do not are regional forms that take their base species'. The decoder is thirty lines in `species_import.py`, checked by decoding Lucario's and correlating its envelope against the one already in the repo, 0.87. **Generating the files was only half of it and the first pass stopped there**, which is why a wild Glimmora was silent and its Pokedex cry screen crashed. Two things were missing. Every cry needs a bank and a wave archive in `res/sound/pl_sound_data.json` at its own species id, because `Sound_PlayPokemonCry` indexes that table by species directly; indices 495 to 699 were empty placeholders so the 159 fit at 494 to 652, with Shaymin Sky moving off 494 to 653. And **a cry has a size limit**: `PLAYER_PV`'s heap is 24,200 bytes and has to hold the bank and the whole wave at one byte a sample, so the ceiling is vanilla's longest cry, Jynx's 23,524 samples. HeartGold trims less and 41 of the 159 were over it; they are cut to that with a short fade
  - [x] **A latent `nitrosfx` bug came out of the same chase and is worth sending upstream.** `WriteSdat` writes the FAT, then pads the first file's data to a 0x20 boundary, but computes the FAT offsets without that padding, so every file in the archive reads from a few bytes early. It has never shown because the padding is zero for the unmodified game; adding 159 cries moved the FILE block and made it 8. Fixed in `tools/nitrosfx/sdat.c`, and the fix is a no-op when the padding is zero, so a matching build is unaffected
  - [x] **Two engine changes the data forced.** Base experience was a byte and these species run to 340, so it moved into the two bytes of padding at the end of the record, leaving it 48 bytes and nothing between catch rate and the abilities shifted. `MAX_LEARNSET_ENTRIES` was 20 and three species have more, so it is 34, the donor's own slot count; `move_reminder_data.c`'s hand-written 22 now derives from it
  - [x] **Nine evolution slots and the seven natives that gain an evolution (2026-09-20).** Eevee forced the widening, exactly as the engine-change answers predicted. `LoadSpeciesEvolutions` was also reading a whole 44-byte member into a 42-byte buffer, true in vanilla too, and now uses the member's size
  - [ ] **What is deliberately left short**, none of it blocking a new species from being caught, seen in the dex or battling:
    - **TM, tutor and egg-move lists are empty.** Hardlove keeps TM compatibility somewhere this project has not found, and how many TMs and which moves is a Phase 5 pass, so filling them now would be work thrown away. A type-based default could be generated in an hour if the species need to be playable before that pass
    - **Pokedex entry text is a placeholder** reading "No Pokedex data is available for this Pokemon yet". The donor has no entries for these species; its 1476-long banks are names, uppercase names, names with articles, categories, heights and weights, all of which did come across
    - **Body shape defaults to `SHAPE_BIPEDAL_TAILED` and the dex size-comparison scales and positions are defaults.** The shape only affects the Pokedex's search-by-shape and the scales only the size screen. The donor's dex tables stop at HeartGold's own count
    - **Footprints are blank, with `"has": false`**, which 109 natives already do. Not found in the donor; Dr. Footstep is the only thing that reads them
    - **Level-up learnsets are filtered to Platinum's own 467 moves**, which keeps 80 per cent of the entries and leaves no species empty. Element 4 refills them
    - Five species' gender ratio bytes (the Minccino and Gothita lines) read 190 where the enum has 191, a donor off-by-one, and are snapped to the nearest named value
  - [x] **The caps in code, audited (2026-09-21).** Every site that references `MAX_SPECIES` or `NATIONAL_DEX_COUNT` was read. All but one derive correctly and needed no change; the save's dex flags had already grown on their own, which is the row `save-layout.md` gained on 2026-09-21. **One live defect, now fixed**: `Pokemon_GetFormNarcIndex` hardcoded 496, 499, 501, 502 and 503 as the `pl_personal` and `wotbl` members holding the twelve alternate-form records, and those records moved to 655 onward when 159 species were inserted ahead of them, so the Deoxys, Wormadam, Giratina, Shaymin and Rotom forms were all reading a new species' record for their stats, types, abilities and learnset. The five literals now derive as `MAX_SPECIES + n` in `include/constants/forms.h`; `MAX_SPECIES + 1` is 496 in vanilla, so the expression reproduces the old numbers exactly. Verified against both the pinned vanilla build and the current one, and `make rom` is clean. **Two facts recorded rather than acted on**: `BattleHallWinRecords` is now 3,948 of the 4,096 bytes of its one save sector, which caps `MAX_SPECIES` at 679, and the save budget that 30 boxes has to fit in is worked out in `save-layout.md`. The PC to 30 boxes itself is element 8 and stays there
  - [ ] **Emulator check for the form fix**, cheap and worth folding into the next session at the controls: a Rotom that has taken a form, or a Giratina holding the Griseous Orb, should show its form's stats and types rather than the base form's. No new save needed; the fix is code only and moves nothing in the save
  - [x] **Emulator check for element 3: done (Ian, 2026-09-21).** A wild Glimmora on Route 201, the highest new id at 652, from a debug encounter table. Confirmed working: it appears and battles, sprite and moves right (Toxic Spikes and Rock Throw), Pokedex entry at number 652 and scrolling to it, the menu icon in party and box, the summary screen's types and ability, catching it, storing it in a box, and its cry. Two things failed on the first pass and were fixed the same day, both recorded under the cry entry above: no cry at all, and a hard crash on the Pokedex cry screen. **Two hangs were found during the same session and are logged as open bugs under Phase 5**; they are not known to be element 3's doing
  - [ ] **Re-run that list after each further Phase 4 element.** Start a new game. A new species has to be seen at all before anything else can be judged, and the cheapest way is the Pokedex: evolve or trade one in, or use a debug encounter if one is to hand. What to look at: the sprite and its palette, the menu icon, the cry, the summary screen's types and ability, and that the Pokedex info page does not garble. Charm on an Eevee should now offer Sylveon on level-up
- [ ] **4. Move expansion.** Move table, battle scripts, effect handlers, animations, text. **Widen the level-up learnset format to (u16 level, u16 move) as part of this**: Platinum packs both into one u16 and caps the move id at 511, and there are 924 moves. hg-engine's `data/battle_scripts` converts largely mechanically; animations and particles are the least examined part. Test: a new move animates and does the right damage
- [ ] **5. Ability effects.** Reimplement the new abilities' behaviour in the decomp's battle code from hg-engine's `ability.c` and `individual/*.c`
- [ ] **6. Battle AI.** Platinum's own AI (`src/battle/trainer_ai/`) is the baseline; Hardlove's AI is not ported.
  - [ ] **Understand it first (Ian, 2026-09-20).** Ian wants a rigorous understanding of how the trainer AI actually works, and is interested in fixing its bugs and making small deliberate changes. So: read `src/battle/trainer_ai/` and write down what each scoring function does, what the AI flags select, and how the thresholds interact, to the point where a change can be **predicted before it is made**. References Ian uses: lhearachel's Gen 4 AI gist (`ff61af1f58c84c96592b0b8184dba096`), `pokemow.com/Gen4/TrainerAI/` and its switching page. Note 350 carried-over trainers had their AI flags changed by the base ROM, so the flags in play are already non-vanilla and the write-up needs to say what each one does
  - [ ] Keep fixes and changes separate: a fix restores what the code plainly intended, a change makes the AI play differently and is Ian's call
  - [ ] Teach it the new moves and abilities, and the held items that change a decision (Eviolite, Assault Vest, Air Balloon, Rocky Helmet, Weakness Policy, the seeds)
  - [ ] Wild double battles are in, so the doubles-specific scoring gets exercised far more than in vanilla and needs the same scrutiny as singles
  - [ ] Apply the eleven ds-pokemon-hacking battle_edits fixes (Ian confirmed the full list, 2026-09-15): Fire Fang vs Wonder Guard, Rage Glitch, Water Immunity vs Dry Skin check, Sunny Day check, Foresight/Odor Sleuth Ghost check, Facade status check, Leaf Guard Sunny Day logic, Water Spout/Eruption HP check, charge-turn scoring fix, Thunder scoring fix, Discharge double-battle scoring fix. **These are the end of the AI work, not the start**: each should be explainable from the code before it is applied, not pattern-matched from the guide
- [ ] **7. Items, a curated subset.** Sits after the move expansion, because several held items reference new moves and abilities. New items go into Platinum's free slots rather than taking Hardlove's 2,687-record table; widen only if the list outgrows the free slots. In and out lists are in the answers file. Each held item needs its battle effect written into Platinum's engine with hg-engine's C as reference. **The TM count must not be hard-capped at 92** - how many TMs and which moves is a Phase 5 balance item, but the mechanism has to allow more
- [ ] **8. The remaining engine changes**, one at a time: hidden abilities (third slot, the script flag, Ability Patch); **nine-slot evolution records are done, landed with element 3 because Eevee needed the eighth**, but the 17 dead trade entries are still to strip; 30 PC boxes; always-set battle mode; 60 fps outside battle (drop it without asking if it misbehaves); wild double battles; single-use items restored after battle; always national dex; script-driven level caps (Phase 4 delivers the mechanism only); a lowered friendship evolution threshold
- [x] **A faster HP bar (2026-09-20).** Found in the base ROM as Ian expected: `UpdateGauge`'s `*temp -= fillOffset` patched to `*temp -= 2`, and since the HP call passes 1 that is exactly 2x. Implemented at the HP call site rather than in `UpdateGauge`, because the patched instruction is shared with the EXP gauge, whose step is computed and would crawl if forced to 2. HP behaviour is identical
- [x] **The two gameplay rules found in the base ROM (2026-09-20). Ian: keep both.** Chasing the HP bar closed all four ov16 edits the Phase 3 inventory could not explain, and two of them were rules rather than cosmetics. Both are now ported, with one deliberate difference from the base ROM on the EV side:
  - **Traded Pokemon always obey.** `BattleControllerPlayer_CheckObedience`'s badge-scaled thresholds (10/30/50/70) are all set to 255 in the base ROM, so the level test always passes and both disobedience rolls are unreachable. Ported as `OBEY_ALWAYS_MAX_LEVEL`, assigned once after the vanilla ladder so the rule it replaces stays readable in the source
  - **Battling grants no EVs, and vitamins grant 26 each.** The base ROM zeroes the literal 510 in ov16's copy of `MAX_EVS_ALL_STATS`, which makes the payout loop break before it writes anything. That cannot be copied literally here: the decomp has one `MAX_EVS_ALL_STATS`, and `src/item_use_pokemon.c` needs it at 510 to cap the vitamin total. So the battle side gets its own constant, `MAX_EVS_FROM_BATTLE 0` in `src/battle/battle_script.c`, and the loop guard reads that instead. Vitamins then went from 10 EVs to **26** (`itemUseParams` in the six `res/items/data/*.json`), so with `MAX_EV_VITAMIN` at 252 a stat maxes in 10 vitamins and the 510 total in 20. Ian asked for "26 or 52"; 26 was taken for the finer spreads, and 52 is a one-byte change in each of the six files if the vanilla 9,800 price makes 10-per-stat too expensive. Verified against the build: exactly six item records differ from vanilla, one byte each, 10 to 26
  - The fourth edit, `+0x10D58`, is the no-items-in-trainer-battles change and was already ported in Phase 3

## Phase 5: design passes raised while answering Phase 4

All four came out of Ian's 2026-09-20 answers. None is Phase 4 work; all of them
shape the finished game.

- [ ] **Level-cap split design.** Per split: the areas, trainers, items, and learnsets/evolutions available. Ian expects this to be a lot of work, and it **must precede the trainer balance pass** because it constrains it. Phase 4 only delivers the mechanism
- [ ] **TM pass.** How many TMs (likely more than 92) and which moves, done with the overall balance pass
- [ ] **Ability balance pass** across all species, including the base ROM's 228 duplicated second slots, which stay as they are until then
- [ ] **Encounter design decisions** that Phase 4 leaves open: which encounters set the hidden-ability flag (gifts, statics, a late area), and which areas have wild double battles
- [ ] Optional, not scheduled: a ROM-specific PKHeX/PKHaX build. Depends on the save layout being documented as it changes, so keep a `docs/oxide/save-layout.md` listing every block that moved and why

## Phase 5: Verify

- [x] Boots in emulator (Ian's 2026-09-15 run reached Route 202 trainers on this build; re-confirmed 2026-09-20 in melonDS with Fairy and the ability widening in, no crash on boot)
- [x] The save-format break behaves as documented (2026-09-20): Ian's pre-element-2 save loads and every Pokemon in it shows no ability, exactly what `docs/oxide/save-layout.md` predicts. That save is retired as a test bed; everything below is from a new game
- [x] New game to first battle without crash (Ian, 2026-09-20, melonDS, with elements 1 and 2 in: the starter battle ran and Piplup shows an ability on the new save, which is the in-play confirmation that the moved ability field reads and writes correctly). Re-check after each further Phase 4 element
- [ ] **Open bug (Ian, 2026-09-20): UNLOCK FPS set to ALWAYS hard-crashes on entering Sandgem Town**, taking melonDS down with the game. Not seen with the default OFF. The uncap is `ShouldWaitForVBlank` in `src/main.c`, which passes FALSE to `OS_WaitIrq` for the VBlank wait in the main loop when the option is ALWAYS (or BATTLE while the battle overlay is loaded); the selector and the disabled button remapping in `src/system.c` are the whole change. An emulator crashing rather than the game alone points at the ARM9 doing something the emulator cannot model when it never waits for VBlank, most likely a VRAM or DMA transfer racing the display at a map load (Sandgem is the first map with a town-sized transition after the option can be set). Same debugger setup as the hang below; reproduce with `~/roms/route202-hang.sav`, set ALWAYS, walk into Sandgem. Not attempted on 2026-09-20: the debugger session stopped before the hang below was reached (see that entry). Until fixed, the uncap should be treated as OFF-only; if it proves opaque, Ian's 2026-09-20 answer allows dropping the 60 fps feature without asking
- [x] **Pokemon Center nurse hang on "yes": fixed by the whiteout fix below, confirmed gone by Ian in melonDS (2026-09-21).** Its "yes" branch applies `Common_Movement_3012`, one of the seven `scripts_common` movement blocks the Repel edit pushed to an odd offset, and all seven are realigned in commit bb0c45993. Test: new game, first Pokemon Center, answer yes.
- [ ] **Open bug (Ian, 2026-09-21): backing out of the box deposit screen hangs. Still hangs on the realigned build (Ian, 2026-09-21), so it is not the `scripts_common` alignment bug and is the bug track's first open item.** On a new save. Not traced yet. The PC is a map object rather than a common script, and the base ROM added a PC entry, `Common_Entry58`, behind the shortened region, so the first read is: which script runs on leaving the deposit screen, and whether it applies a movement from a generated script other than `scripts_common` (those files keep the base ROM's padding and can be odd-aligned for the same reason; the `carry-over-map` skill's gotcha 6 says how to check). If no movement is involved, it is a third thing: trace with `live_watch.py` from the deposit screen, heartbeat first, then `ScriptContext_WaitForMovement`, `MapObject_HasAnimationEnded` and the PC overlay's exit
- [x] **Whiteout hang, found and fixed 2026-09-21, confirmed by Ian in melonDS the same night** (player and Mom turn to face each other, her lines play). The Repel fix shortened `scripts_common` by 15 bytes and pushed its seven movement blocks to odd offsets; the fix is `.balign 4, 0` before every `Common_Movement_*` label in `scripts_common.s`. The root cause, the stub method that found it and the rule for every generated script are in the design doc's findings log (2026-09-21) and the `carry-over-map` skill, gotcha 6. The three UNLOCK FPS and Sandgem items are unaffected
- [ ] A ported element visibly works in-game. Fairy is now testable: Clefairy and Ralts are both early and both Fairy, so a Dragon move against one should do nothing at all, a Poison or Steel move should do double, and the summary screen should read FAIRY. Charm, Sweet Kiss and Moonlight are Fairy now too, though all three are status moves, so the damage chart can only be checked from the defending side until a damaging Fairy move exists
- [ ] Check the Poketch move tester agrees with the battle engine on Fairy, and that the Pokedex info page on a Fairy Pokemon shows the NORMAL plate rather than garbage (the known gap, not a crash)

## Backlog / follow-ups

- **Live inspection of the running game (Ian, 2026-09-20).** A `tools/oxide/live.py` that speaks the GDB remote protocol to melonDS's stub directly (no GDB needed for reads): halt, continue, registers, memory by symbol from `build/main.nef.xMAP`, and decoders on top for the party and boxes (Gen 4 block shuffle and encryption, with the ability field where `save-layout.md` says), script flags and vars, current map and player position, the running script's state, and writes for a debug console (set a flag, warp, give an item or a Pokemon, set an ability). Every Phase 4 emulator test then becomes something an agent can run itself. Second piece: an offline reader for the melonDS `.sav` (raw flash image, layout from the decomp's save code) so the encounter tool can take its caught list from the real dex flags and Phase 4 save changes can be checked against a real save. The stub connection is done (`live.py`, `live_watch.py`, recipe in `docs/oxide/setup-fork-and-wsl2.md` part 5b); the decoders and the `.sav` reader are what is left
- **Re-humanise the 86 generated scripts**, a map at a time, as each map gets attention: named labels, `NPCMessage`, text-bank constants, real names for `LOCALID_OBJECT_<n>` and `<Bank>_Text_<n>`. `checkmap.py` is the check. Never during a faithful carry-over; the point of the bulk pass was to be done
- **Unify the clown-gift events** (Ian, 2026-09-20). Roughly a dozen maps carry the same Clown who hands out a Pokemon, written out longhand each time and not even in a consistent block order: some roll the die first, some give first, and `sandgem_town_house` still uses a list menu where the rest were converted to a random roll. Collapse them into one common script parameterised by the map, and have it **name the possible Pokemon** rather than rolling blind, so the player can see what is on offer. The full set of species per map is catalogued in `docs/oxide/pokemon-gifts.md`; the choice names are mostly still in each map's text bank, orphaned, from the pick versions. Do this after the faithful carry-over, not during it, so the two are separable in the history. Orphaned pick-event names still to remove: canalave_library_2f 7, pastoria_city_north_house 6, floaroma_town_middle_house 4, eterna_city_condominiums_1f 3, floaroma_meadow_house 3, jubilife_city_south_house_1f 3, solaceon_town_northeast_house 3
- Two raw regions the disassembler emits as `.byte` rather than decoding (`scripts_spear_pillar` 0x04b5 and `scripts_common` 0x1268 are the two to start from). They round-trip exactly; they are just not understood
- The two trade json files still named `abra` and `haunter` for what are now a Ditto and a Gengar
- Seventeen dead trade-evolution entries (methods 5 and 6) left beside the non-trade routes the base ROM added; harmless in single player, stripping them would free evolution slots on Clamperl
- Move `Platinum Oxide VSMaker2 Data\` out of the DSPRE contents folder (Ian's call; it is a stale duplicate)
- Update the chat-surface doc `claude/engine-tooling-comparison.md`: plat-engine is a patch kit not a decomp build; pokeplatinum is matching and active
- Palette hue-shift patch (IV/nature-driven shiny or per-form palette, base ROM's eight palette-loading hooks): low priority, investigate only after Phase 4 is done; drop if it proves opaque (Ian, 2026-09-15)

## Decisions made

Each decision lives with the fact it changed: the design doc's sections 3, 4 and 5 and the file each of them points at. The list that used to sit here duplicated those and was removed in the 2026-09-21 docs pass.
