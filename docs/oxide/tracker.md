# Platinum Oxide: Tracker

Top-level task list. Tick items as they finish; add detail only where it changes what happens next. Companion to `docs/oxide/design-doc.md`.

Where things live, so each fact has one home: **status** is here; **durable facts** are in the design doc's findings log (section 8); the **encounter tool's** status, milestones and findings are in `docs/oxide/encounter-tool-build-plan.md`. Everywhere else points, it does not repeat.

**Where things stand (2026-09-20, end of day).** Phase 3, carrying the base ROM's edits into the source tree, is **finished**. All three hard stops were closed on 2026-09-20: the Repel prompt (fixed with a new script command), the Battleground init script (no init script at all now) and the trainer battle messages (no edit in the bank, only DSPRE zeroing unused slots). The encounter tool has M1 to M4 done and M5 next. **Phase 4, the engine port, has started.** Its engine-change questions are written and waiting on Ian (`docs/oxide/phase4-engine-change-questions.md`); no element has been ported yet. The tree is clean and `origin/oxide` is current. `git log -1` is the resume point; no commit hash is kept in this file because it goes stale within the hour.

**Second track: the encounter tool.** A separate tool for designing Oxide's wild encounter tables. It owns `tools/oxide/encounters/`, `res/field/encounters/`, `docs/oxide/encounters/` and the three `encounter-*.md` docs, and touches nothing else. M1 (round-trip I/O) 13/13, M2 (analysis engine) 23/23, M3 (linter) 18/18, M4 (browser editor, after two usability rounds with Ian) 41/41. **Next is the authoring pass: writing every wild table from the species pick-list**, planned in `docs/oxide/encounter-authoring-plan.md` (two stages, since the 159 new species do not exist until Phase 4 step 3; all 171 land tables, early corridor first; the sidecar becomes the design source). How to resume cold, what each milestone found, and what it is waiting on are all in `docs/oxide/encounter-tool-build-plan.md`. This paragraph is the tool's whole footprint in this file; per-milestone entries go in the build plan, not here.

**Done in Phase 3, all verified against the base ROM:** species/move/evolution/learnset import; the trainer carry-over (all 928, ability/gender fields, 0 field mismatches, emulator-confirmed); all four synthetic-overlay routines (no items in trainer battles, Rare Candy chaining, uncapped battle frame rate, EV/IV viewer); encounters (125 tables); both in-game trades; map headers (58); the small constant edits; and the scripts, events and text: events 158 of 158, scripts 89 of 91 matching, with `scripts_common` and `scripts_init_battleground` intentionally diverged, text banks 72 of 78, with every mismatch explained under Phase 3 and none of them an outstanding edit. Three sets of base-ROM changes were deliberately *not* carried over, as DSPRE noise rather than edits, each with its evidence recorded: sprite heights, the six vitamin item records, and the encounter `unown_table`/`rate_form` fields.

**Next steps, in order:**

1. **Ian: emulator pass** on the items under "Waiting on Ian". The 86 bulk-generated scripts are verified byte-for-byte against the base ROM but have never been played from this build.
2. **Phase 4 prerequisites that touch no Phase 3 file:** read Hardlove's `a/0/2/8` tables, get Ian's "other engine changes" list, settle the species-ID scheme. All three are listed under Phase 4.
3. **Phase 4 proper**, in the order under Phase 4, starting with Fairy.

**To confirm the state after a restart**, from the repo root:

```
make rom
python3 tools/oxide/import_base_rom.py --base ~/roms/base.nds --vanilla ~/roms/vanilla.nds --dry-run   # every count 0
python3 tools/oxide/verify_narcs.py --built build/pokeplatinum.us.nds --ref ~/roms/base.nds
python3 tools/oxide/verify_narcs.py --built build/pokeplatinum.us.nds --ref ~/roms/base.nds --encounters
python3 tools/oxide/verify_narcs.py --built build/pokeplatinum.us.nds --ref ~/roms/base.nds --text
python3 tools/oxide/verify_narcs.py --built build/pokeplatinum.us.nds --ref ~/roms/base.nds --map-headers
python3 tools/oxide/bulk_scripts.py --dry-run   # would write 0; skips scripts_init_battleground and scripts_common
python3 tools/oxide/bulk_events.py --dry-run    # would write 0
python3 tools/oxide/bulk_text.py --dry-run      # would write 0; four generated banks skipped
python3 tools/oxide/scriptdis.py --rom ~/roms/vanilla.nds --verify
python3 tools/oxide/scriptdis.py --rom ~/roms/base.nds --verify --base-rom
```

The importer is idempotent, so a non-zero count means something moved. Both `scriptdis` runs should report 574 files walked, 0 failed, 550 init scripts read and 0 movement-target collisions. The encounter tool's own checks are listed in its build plan. This whole list was last run clean on 2026-09-20 after the Repel fix.

**Waiting on Ian:**

- Emulator checks not yet done: Rare Candy chaining; the frame-rate uncap (options menu reads UNLOCK FPS, with OFF / BATTLE / ALWAYS); the EV/IV viewer (R on the Skills page); Shinx's ability (always Rivalry, never Intimidate) and Bidoof/Starly hatch time (~255 steps, down from ~3825); and a walk through a few bulk-generated maps, the gift houses being the obvious ones. The "use another Repel?" prompt is now safe to answer yes; it is worth testing, since it is the one thing in this build that was broken and is now fixed rather than carried over.
- The ability/gender nibble direction (1=male, 2=female) is a guess; no Route 202 trainer exercises it. Swap if anything reads wrong in-game.
- Evolution triggers for Gyarados M and Lopunny M.
- The "other engine changes" list (design doc section 3). The full hg-engine menu is in `docs/oxide/phase1-hg-engine-survey.md` section 3. Gates Phase 4's last item only.
- Two data questions from the carry-over, low priority because the default is to leave vanilla: whether the encounter `unown_table`/`rate_form` changes or the sprite-height changes were ever intended (evidence under Phase 3 says they were DSPRE re-saves).
- Encounter tool: review of the progression `order` and the pick-list `tier` column once the authoring agent commits its defaults (the pass no longer waits on either; see `docs/oxide/encounter-authoring-plan.md`), whether Unown returns to the pick-list, and whether surf tables get designed in the first pass or only de-leaked.
- Tabled, not blocking anything: the 21 native species where `New Pokedex.xlsx` and the base ROM disagree on stats (see Phase 0).

## Phase 0: Setup

- [x] Working folder created, both ROMs and both DSPRE extractions in place
- [x] Design doc v0.1 and this tracker written
- [x] Ian confirms Hardlove is an hg-engine build (2026-09-15)
- [x] Content scope set: species subset (Ian picks), all new moves with scripts/sprites/animations, all new abilities (2026-09-15)
- [ ] Ian finalizes the "other engine changes" list in design doc section 3
- [x] Ian produces the species pick-list (2026-09-15, landed in the repo 2026-09-20 as `docs/oxide/species-pick-list.md` and `.csv`). 360 rows: 199 already in Platinum, 159 new slots, 2 cut (Toxel/Toxtricity, which need nature-conditional evolution Gen 4 has no method for), so 358 obtainable entries and 652 species in the finished ROM. Key decision: it is an **availability list, not a deletion list**; all 493 natives stay in the tree, because the 928 carried-over trainers use species the curated dex omits. National Dex IDs stay as internal species IDs, so the donor's index-equals-dex-plus-50 rule keeps working. Verified locally: all 21 rows of its sheet-vs-base-ROM conflict table match what is actually in `res/pokemon` today
- [ ] **Tabled (Ian, 2026-09-20): the 21 native species where `New Pokedex.xlsx` and the base ROM disagree on base stats.** Not a per-row decision to make now. It folds into a larger balance pass over the whole existing dex, which Ian wants to approach with analysis and automation tooling built first. The base ROM's values are what `res/` holds today and they stay until that pass happens. The conflict table is in `docs/oxide/species-pick-list.md`
- [ ] **Ian: evolution triggers for the two alt-evolution megas**, Gyarados M and Lopunny M. Donor slots and stat blocks are settled, the trigger is not. Blocks only those two entries in the Phase 4 species port

## Phase 1: Find out where Hardlove's expansions live

Done 2026-09-15. Write-up: `docs/oxide/phase1-hg-engine-survey.md`.

- [x] Read hg-engine's repo structure, `CONFIG.md` and wiki; list every engine expansion and how each is injected
- [x] Map each scoped element to hg-engine source files and the ROM files it changes
- [x] Confirm the map against the Hardlove extraction (overlays 129..151 and numbering match; specific config toggles not determinable from the extraction)
- [x] Locate Platinum's type chart, ability field, species/move tables (named in design doc section 8; exact addresses come from a pokeplatinum build in Phase 3)
- [x] Check plat-engine's state (dormant since 2023-08, ~10% of hook surface, same architecture as hg-engine)
- [x] Inspect the VSMaker2 Data folder (stale duplicate extraction; ignore)
- [x] Write findings into design doc section 8

## Phase 2: Approach decision

Done 2026-09-15. Write-up: `docs/oxide/phase2-approach-breakdown.md`.

- [x] Write the considerations breakdown for Ian (recommends C; verified by building a matching ROM from the decomp)
- [x] Ian picks the approach: C (2026-09-15)
- [x] Ian answers the logistics questions: keep overworld/trainer/stat/move edits; has GitHub, will fork; wants a WSL2 runthrough; Phase 4 order approved; battle AI added as an engine change, Hardlove AI excluded
- [ ] Finalize the scope table in design doc section 3 (waits on the "other engine changes" list)

## Phase 3: Tooling and setup (approach C)

- [x] Ian forks pret/pokeplatinum: https://github.com/iradspinner/pokeplatinum (2026-09-15)
- [x] GitHub access for Claude. Closed 2026-09-20: `git push origin oxide` from the WSL2 checkout works with Ian's own credentials and has done so reliably. The 2026-09-15 note about the app's proxy stripping credentials was about a different environment; it does not apply here. Commits no longer need to be treated as session-local
- [x] Ian sets up WSL2 and builds the fork (`docs/oxide/setup-fork-and-wsl2.md`). Done by 2026-09-15: `~/pokeplatinum` in WSL2 is the working checkout and every build since has run there
- [x] Claude clones the fork and confirms a matching build from it (SHA-1 0862ec35..., 2026-09-15)
- [x] Diff the base ROM against vanilla (`docs/oxide/phase3-base-rom-inventory.md`, with its corrections listed at the top of that file)
- [x] Ian answers the inventory's five Qs (2026-09-15): port all four synthetic-overlay routines; shiny odds raise ports as a one-constant change, palette hue shift deferred/droppable; Battle Arcade script commands port only if the carried-over field scripts call them; apply all eleven battle_edits fixes. Full detail: `docs/oxide/phase3-answers-and-trainer-format.md`
- [x] Correction (2026-09-15): the base ROM's trainer party data is vanilla format, not expanded; no trainer struct/loader extension needed. See `docs/oxide/phase3-answers-and-trainer-format.md` section 2. Replaced by the four items below.
- [x] Add optional `ability` (0|1|2) and `gender` (null|"male"|"female") per-mon fields to trainer JSON and to `tools/dataproc/src/trainerproc.c` (commit 5b8958368)
- [x] Make `TrainerData_BuildParty` (`src/trainer_data.c`) honor those fields, implemented correctly (pick a personality that satisfies the requested gender for that species' ratio) rather than copying the base ROM's buggy +/-2 nudge (commit 5b8958368); verified by inspecting the built `trdata`/`trpoke` NARCs directly, unset fields decode back to ability=0/gender=0 matching prior behavior
- [x] Extend `tools/oxide/import_base_rom.py` with a trainer importer (commit 3877a7641). Extended `jsonstyle.py` with array-index path support and `insert_key()` to reach into `party`'s list of objects. Nibble-direction question resolved: 1=male, 2=female (Ian's guess, 2026-09-15; swap later if anything reads wrong in-game)
- [x] Carried over all 928 trainers (header + party, including ability/gender). The 775 with unchanged party size were patched per-field; the other 153 (107 grow, 46 shrink; Ian resized their parties) had their whole `party` array rewritten in one shot instead of building separate insert/remove-element JSON tooling (commit 92527f42b)
- [x] Verified: `make rom` succeeds. Byte-identical NARC comparison no longer applies (the trainer-mon struct is 2 bytes wider now), so verification decodes both sides field-by-field instead: 0 mismatches across all 928 trainers, header and party
- [x] Emulator-confirmed by Ian (2026-09-15) against Route 202 (Tristan/Logan/Natalie, all bumped to Lv.7, Logan's Burmy using its new moveset): trainer data import is correct in-game, not just in the built NARCs
- [x] No items in trainer battles (commit 26e9b71d9): player side extends the existing `BATTLE_TYPE_FRONTIER_LINK` item-block in `BattleControllerPlayer_CommandSelectionInput` to also cover `BATTLE_TYPE_TRAINER`; AI side stops `BattleControllerPlayer_InitAI` from populating trainer items at all. hg-engine reference: `DISABLE_ITEMS_IN_TRAINER_BATTLE`. Emulator-confirmed by Ian (2026-09-15): Bag blocked with the "can't be used here" message in a Route 202 trainer battle
- [x] Rare Candy chaining (commit 5690f9203): `PartyMenuCB_LevelUp`'s post-level-up state now checks the bag for more of the used item and, if any remain, exits with a new `PARTY_MENU_EXIT_CODE_USE_ITEM_AGAIN` that `start_menu.c` handles by reopening the party menu straight into item-use (same item, mon preselected) instead of returning to the bag. hg-engine reference: `PartyMenu_ItemUseFunc_LevelUpLearnMovesLoop_Case6` in `src/party_menu.c`. **Not yet emulator-tested** (Ian had no Rare Candy on hand); still only verified by `make rom` succeeding
- [x] EV/IV viewer on the summary screen (commit 9af3fe16d). Reverse-engineered from the base ROM's own `EV+IV_Viewer` routine rather than from hg-engine's `IMPLEMENT_NEW_EV_IV_VIEWER`: the base splices `HandleInput_Main+0x34` so R cycles a mode byte 0/1/2, and `SetMonDataFromMon+0x1B2` so the seven stat reads become `MON_DATA_HP_IV..` in mode 1 and `MON_DATA_HP_EV..` in mode 2. In the decomp, `PokemonSummaryMonData` gains `ivs[STAT_MAX]`/`evs[STAT_MAX]` and `DrawSkillsPageWindows` picks which set to print; R is handled in `HandleInput_Main`. Scope held to numbers-only (2026-09-15): the HP/ATTACK/... labels are unchanged in all three views, so no text bank is touched. Three deliberate differences from the base ROM, all in the commit message: the real stats are not overwritten (the base ROM's overwrite makes `DrawHealthBar` divide by zero on a mon with 0 HP EVs), R is `JOY_NEW` not `JOY_REPEAT`, and the refresh is `PokemonSummaryScreen_DrawExtraWindows` rather than `ChangeSummaryMon(screen, 0)` (which replays the cry and can loop forever on an egg). **Not yet emulator-tested**; only verified by `make rom` and by reading the rebuilt arm9
- [x] Uncapped battle frame rate (commit 83770cd5b). Not ported from hg-engine's reference (raw unsymbolized ARM patch, no clean source) but reverse-engineered directly from the base ROM's own synthetic overlay (`weather_sys.narc` member 9, confirmed loaded at `0x023C8000` by diffing base vs `~/roms/vanilla.nds` arm9 and tracing the `NitroMain` boot hook byte-for-byte). `ShouldWaitForVBlank()` in `src/main.c` gates the main loop's `OS_WaitIrq` on `Options_ButtonMode`, repurposing `BUTTON_MODE_START_IS_X` (battle-only uncap) and `BUTTON_MODE_SWAP_XY` (always uncap) exactly as the base ROM does. The options-menu relabel (UNLOCK FPS, OFF / BATTLE / ALWAYS) came over with the text import on 2026-09-20, and exposed two defects in the selector that are fixed in commit 7f2b398e1. The message ids in `res/text/options_menu.json` still read `OptionsMenu_Text_ButtonMode*`; renaming them would mean touching `src/applications/options_menu.c` too, so they were left alone
- [x] Conversion scripts for species, moves, evolutions, learnsets: `tools/oxide/import_base_rom.py` + `verify_narcs.py`; 415 species and 108 move files updated; rebuilt NARCs verified byte-identical to the base ROM (commit c69ef83). The per-species diff of that import is the commit itself and the importer's regenerated `tools/oxide/import_report.md`; the frozen copy once kept under `docs/oxide/` was deleted 2026-09-20 as a duplicate
- [x] Conversion scripts for encounters and trades; heights and items deliberately not carried over (commits 7c2f36ead, 7f2b398e1)
  - [x] **Encounters**: 125 of 183 tables imported and verified field-by-field (`verify_narcs.py --encounters`). `unown_table` and `rate_form0..4` are excluded as DSPRE normalisation, not edits: 98 `unown_table` changes are 0 -> 1, which the game reads identically, and the 17 real ones set every Solaceon Ruins room to 8 (`UnownOnlyExcQue`), which would leave the whole ruins spawning only Unown ! and ? and make the F-R-I-E-N-D letters unobtainable. `rate_form0/1` picks the Shellos/Gastrodon form; 61 of the 63 changed tables contain neither species. Listed under "Waiting on Ian" in case either was intended
  - [x] **Trades**: both edited records imported and byte-verified. Kazza's Abra is now a Ditto with perfect IVs holding a Destiny Knot; Gaspar's Haunter is a Gengar. The json filenames still say abra and haunter
  - [x] **Heights: not carried over.** 298 of 1,976 `height.narc` members differ, but 164 of those write a byte where vanilla has an empty member (a gender the species does not have, which the decomp derives from the gender ratio and cannot express), and of the 116 species where vanilla had male and female offsets equal, every single one has only the male offset changed. A hand edit to a shared sprite would move both. Reads as the same DSPRE re-save that touched 2,788 of 2,964 `pl_pokegra` files. Listed under "Waiting on Ian"
  - [x] **Items: not carried over.** The only six differing records are the vitamins, and each grew from `ItemData`'s 34 bytes to 36 with the EV amount zeroed and everything after it shifted one byte right, so the friendship values no longer line up with the struct. That is DSPRE writing a malformed record, not an edit. The vitamin change Ian described is the EV cap (100 -> 252), which is a code edit and is done under the small constant edits
- [x] Carry over trainers. Superseded by the trainer items above: all 928 trainer records were carried over, not just the 488 the first diff counted as changed
- [x] Carry over map-header edits (commit 56b6609db). 58 of 593 headers: 57 weather and one battle background, all hand-made. The three type gyms get matching weather so the battle inherits it (Oreburgh sandstorm, Pastoria heavy rain, Snowpoint blizzard), Bertha's room sandstorm and Flint's ashfall, Mt Coronet B1F / Victory Road / every Turnback Cave room lose their fog, and Turnback Cave's Giratina room switches its battle background to the Distortion World. Verified byte-identical to the base ROM with `verify_narcs.py --map-headers`, which finds the table in both ROMs by content rather than by a build address
- [x] Carry over text. The 18 banks whose message count was unchanged were imported by index and verified message-for-message (`verify_narcs.py --text`): the options-menu relabel, Vs. Seeker renamed Pocket PC, Attack Order's description, 71 trainer names. The remaining 62 banks, the NPC dialogue for the field scripts, came over in the bulk pass below. Where a base-ROM bank filled a slot vanilla leaves unused (the Repel menu entries, a Teleport System entry, two Wayward Cave lines), the new message got a generated id; the code or script that calls it is a separate matter, see hard stop 3
- [x] Carry over scripts, events and their text. Six maps were rewritten by hand in the repo's idiom (`oreburgh_city_middle_house`, `sandgem_town_house`, `solaceon_town_northeast_house`, `floaroma_meadow_house`, `mt_coronet_1f_north_room_2`, `iron_island_b3f`) and 70 events-only maps patched; then **the rest was generated in bulk on 2026-09-20 at Ian's direction**. Against the base ROM: **events 158 of 158, scripts 89 of 91 matching, with `scripts_common` and `scripts_init_battleground` intentionally diverged, text banks 72 of 78**, no regressions. Method and tools: `docs/oxide/phase3-scripts-and-events-plan.md`
  - Tools: `tools/oxide/bulk_scripts.py`, `bulk_events.py`, `bulk_text.py` (each skips anything the current build already gets right, so the six hand-written maps keep their idiomatic source); `mapdiff.py` and `checkmap.py` for one map at a time; `scriptdis.py` underneath all of them
  - **The cost, and the thing to undo later.** The 86 generated scripts have machine labels (`JubilifeCity_0A4C`) and numeric operands where hand-written source would use named labels, `NPCMessage` and text-bank constants. New object events are named `LOCALID_OBJECT_<n>` and new messages `<Bank>_Text_<n>`. All correct, none readable, and it replaces hand-written upstream source, which is what the project's no-reformatting rule exists to protect. Every generated script says so at the top and the hand-written versions are in git history, so they can be re-humanised a map at a time (backlog)
  - [x] **Hard stop 1 closed (2026-09-20): `scripts_init_battleground`.** The base ROM's member starts with a zero, and that zero is the terminator: `FieldSystem_GetFixedInitScriptID` and `FieldSystem_GetFrameTableInitScriptID` both stop on it and return 0xffff. **The Battleground has no init script at all now**, where vanilla ran script 4 on transition and consulted a frame table on `VAR_BATTLEGROUND_STATE`. That fits the rest of the map being gutted. The 53 bytes after the terminator are the old table, left in place by DSPRE and never read; this builds the 4-byte equivalent instead, which the engine cannot distinguish. Listed in `bulk_scripts.py`'s `DIVERGED`. It was never a format problem: the earlier note said it needed its own emitter, when the whole content is one byte
  - [x] **Hard stop 2 closed (2026-09-20): `TEXT_BANK_NPC_TRAINER_MESSAGES` contains no edit.** The mapping never needed trainerproc's packing order: `poketool/trmsg/trtbl.narc` is that mapping, one (trainerID, messageType) pair per bank slot, already in the ROM and byte-identical between the ROMs. `tools/oxide/import_trainer_messages.py --check` validates all 2,497 slots against `res/trainers/data`. With the mapping in hand, **all 373 differences are unused slots whose filler byte DSPRE zeroed, every one on a DUMMY trainer**; no message text differs anywhere. Fourth instance of the same DSPRE re-save noise, after sprite heights, the vitamin item records and the encounter `unown_table`/`rate_form` fields, so vanilla's values are kept
  - [x] **Hard stop 3 closed (2026-09-20): the Repel prompt.** `SetRepelSteps <count>` added as a real script command, writing through `SpecialEncounter_GetRepelSteps`, registered at the end of the command table so no existing opcode moves; the three branches in `scripts_common` use it and drop their `ScrCmd_Unused_007` pokes. The base ROM's workaround (poke a fixed scratch address, then a hand-written routine in place of `Dummy088` copies it through the save pointer) is not reproduced. **`scripts_common` is now 15 bytes shorter than the base ROM's and is the first file that deliberately does not match it**, so `bulk_scripts.py` carries a `DIVERGED` list and refuses to regenerate it. Without that the restart check-list would silently undo the fix
  - Not defects, recorded so nobody re-opens them: `SPECIES_NAME` and `SPECIES_POKEDEX_ENTRY_EN` decode identically already; `NPC_TRAINER_NAMES` differs only in the `{TRNAME}` tag DSPRE re-applied across the bank; `SOLACEON_TOWN_NORTH_HOUSE` and `SURVIVAL_AREA_SOUTH_HOUSE` have identical text but DSPRE re-encrypted them with key 0xd43f where vanilla uses a per-bank key, and any key decodes the same; `ITEM_NAMES_WITH_ARTICLES` differs because renaming Vs. Seeker to Pocket PC propagates to the derived bank here where DSPRE left "a Vs. Seeker" behind
- [x] Re-apply the base ROM's small constant edits as C changes (commit 9f901de9c): shiny threshold 8 to 255 (~1/8192 to ~1/257), vitamin EV cap 100 to 252 (`MAX_EV_VITAMIN`; the total is still capped by `MAX_EVS_ALL_STATS`), new-game defaults of fast text and SET battle style, HMs forgettable from the summary screen's move-select (the `HandleInput_SelectMove+0x8E` edit the inventory had misfiled under the EV/IV viewer), and **reusable TMs**. That last one is a correction: the inventory read `TeachMove+0x5B` as "probably HMs can be forgotten", but the call it skips is `Bag_TryRemoveItem`, so the base ROM simply never takes a TM out of the bag. Each edit was traced to the exact instruction the base ROM changed and confirmed in the rebuilt arm9
- [ ] Still not ported from the base ROM's arm9, all needing real understanding rather than a constant swap: the Vs. Seeker repurposed as a portable PC (`UseVsSeekerFromMenu`, two sites, pairs with the item text already imported); the palette hue-shift patch (six hooks into code written over the Battle Arcade region, deferred as low priority, droppable); and a single two-byte change to a lookup table at arm9 `0x0EC478` that has not been identified. The Battle Arcade custom commands are closed: only `Dummy088` is called (hard stop 3), the rest is dropped
- [x] Working `git pull && make rom` loop with Ian, verified end to end with one visible change (2026-09-15)

## Phase 4: Port, one element at a time

Not started. The order was approved by Ian on 2026-09-15 (from `docs/oxide/phase2-approach-breakdown.md` section 6), chosen so each step is testable on its own and the structural changes come first while the tree is still close to vanilla. hg-engine's source is the reference implementation for each feature; Hardlove's tables are the content source. Each element gets its own checklist here when it starts, with the emulator test that proves it.

Prerequisites, none of which touch a Phase 3 file:

- [ ] Read Hardlove's `a/0/2/8` members 9_07..9_13 directly (base exp, hidden abilities, form data, form-to-species, reversion, icon palettes). The species port needs them and nothing else does. The NARC reader in `tools/oxide/import_base_rom.py` reads Platinum NARCs and should read these too
- [ ] Ian's "other engine changes" list. **Questions written 2026-09-20: `docs/oxide/phase4-engine-change-questions.md`**, grounded in what the donor ROM actually shows rather than hg-engine's menu. Two findings the Phase 1 survey could not get from the extraction: Hardlove's item table is **2,687 records of 36 bytes against Platinum's 446 of 34**, and its evolution records hold **9 slots and use 19 methods Platinum does not have**. The menu is also shorter than it looks, because much of it arrived with the base ROM in Phase 3
- [ ] Ian: evolution triggers for Gyarados M and Lopunny M (blocks only those two entries)
- [ ] **Settle the species-ID scheme before touching `MAX_SPECIES`.** The pick-list keeps National Dex numbers as internal IDs (Snivy is 495), which is convenient for imports but means every species-indexed table (personal, evo, learnsets, sprites, dex flags in the save, PC) grows to at least 1010 slots plus forms while only 159 new species are populated. Confirm that cost is acceptable, or renumber sparsely and give up the dex-plus-50 convenience. Decide once; it sizes everything after it

Elements, in order:

- [ ] **1. Fairy type.** Type enum, `sTypeMatchupMultipliers` in `src/battle/battle_lib.c`, type names and icons, Arceus plate handling. Reference: hg-engine `armips/asm/fairy.s` and `battle_pokemon.c`. Test: a Fairy move hits a Dragon for 2x and the summary screen shows the type
- [ ] **2. Ability field to u16, table to 512.** Boxed-mon and battle-mon structs, summary screen, script commands, save. hg-engine's `armips/asm/abilities.s` lists every site it had to touch. Test: a mon given ability 300 is named correctly and its effect fires
- [ ] **3. Species-slot expansion, sparse.** Raise `MAX_SPECIES`, expand personal/evo/learnset/sprite/icon/dex/text tables and the save's dex flags, PC. Bring in the 159 pick-list species from Hardlove's 44-byte `SpeciesData` records, with the `a/0/2/8` tables from the prerequisite. Test: a new species is catchable, appears in the dex, and battles
- [ ] **4. Move expansion.** Move table, battle scripts, effect handlers, animations, text. hg-engine's `data/battle_scripts` converts largely mechanically; animations and particles are the least examined part. Test: a new move animates and does the right damage
- [ ] **5. Ability effects.** Reimplement the new abilities' behaviour in the decomp's battle code from hg-engine's `ability.c` and `individual/*.c`
- [ ] **6. Battle AI.** Platinum's own AI (`src/battle/trainer_ai/`) is the baseline; Hardlove's AI is not ported.
  - [ ] **Understand it first (Ian, 2026-09-20).** Ian wants a rigorous understanding of how the trainer AI actually works, and is interested in fixing its bugs and making small deliberate changes. So: read `src/battle/trainer_ai/` and write down what each scoring function does, what the AI flags select, and how the thresholds interact, to the point where a change can be **predicted before it is made**. References Ian uses: lhearachel's Gen 4 AI gist (`ff61af1f58c84c96592b0b8184dba096`), `pokemow.com/Gen4/TrainerAI/` and its switching page. Note 350 carried-over trainers had their AI flags changed by the base ROM, so the flags in play are already non-vanilla and the write-up needs to say what each one does
  - [ ] Keep fixes and changes separate: a fix restores what the code plainly intended, a change makes the AI play differently and is Ian's call
  - [ ] Teach it the new moves and abilities
  - [ ] Apply the eleven ds-pokemon-hacking battle_edits fixes (Ian confirmed the full list, 2026-09-15): Fire Fang vs Wonder Guard, Rage Glitch, Water Immunity vs Dry Skin check, Sunny Day check, Foresight/Odor Sleuth Ghost check, Facade status check, Leaf Guard Sunny Day logic, Water Spout/Eruption HP check, charge-turn scoring fix, Thunder scoring fix, Discharge double-battle scoring fix. **These are the end of the AI work, not the start**: each should be explainable from the code before it is applied, not pattern-matched from the guide
- [ ] **7. Other engine changes**, from Ian's list, one at a time

## Phase 5: Verify

- [x] Boots in emulator (Ian's 2026-09-15 run reached Route 202 trainers on this build)
- [ ] New game to first battle without crash, re-checked after each Phase 4 element
- [ ] A ported element visibly works in-game (for example: a Fairy-type move hits for the right effectiveness)

## Backlog / follow-ups

- **Re-humanise the 86 generated scripts**, a map at a time, as each map gets attention: named labels, `NPCMessage`, text-bank constants, real names for `LOCALID_OBJECT_<n>` and `<Bank>_Text_<n>`. `checkmap.py` is the check. Never during a faithful carry-over; the point of the bulk pass was to be done
- **Unify the clown-gift events** (Ian, 2026-09-20). Roughly a dozen maps carry the same Clown who hands out a Pokemon, written out longhand each time and not even in a consistent block order: some roll the die first, some give first, and `sandgem_town_house` still uses a list menu where the rest were converted to a random roll. Collapse them into one common script parameterised by the map, and have it **name the possible Pokemon** rather than rolling blind, so the player can see what is on offer. The full set of species per map is catalogued in `docs/oxide/pokemon-gifts.md`; the choice names are mostly still in each map's text bank, orphaned, from the pick versions. Do this after the faithful carry-over, not during it, so the two are separable in the history. Orphaned pick-event names still to remove: canalave_library_2f 7, pastoria_city_north_house 6, floaroma_town_middle_house 4, eterna_city_condominiums_1f 3, floaroma_meadow_house 3, jubilife_city_south_house_1f 3, solaceon_town_northeast_house 3
- **Build caveat** (recorded 2026-09-20): changing a text bank regenerates `pl_msg.narc` but does not reassemble the scripts that include its generated header, so a script can be left pointing at old message ids. Touch the `.s` or build clean. The symptom is an empty message remap in `checkmap.py` where there should be one
- Two raw regions the disassembler emits as `.byte` rather than decoding (`scripts_spear_pillar` 0x04b5 and `scripts_common` 0x1268 are the two to start from). They round-trip exactly; they are just not understood
- The two trade json files still named `abra` and `haunter` for what are now a Ditto and a Gengar
- Seventeen dead trade-evolution entries (methods 5 and 6) left beside the non-trade routes the base ROM added; harmless in single player, stripping them would free evolution slots on Clamperl
- Move `Platinum Oxide VSMaker2 Data\` out of the DSPRE contents folder (Ian's call; it is a stale duplicate)
- Update the chat-surface doc `claude/engine-tooling-comparison.md`: plat-engine is a patch kit not a decomp build; pokeplatinum is matching and active
- Palette hue-shift patch (IV/nature-driven shiny or per-form palette, base ROM's eight palette-loading hooks): low priority, investigate only after Phase 4 is done; drop if it proves opaque (Ian, 2026-09-15)

## Decisions made

- 2026-09-15: Base ROM is `Platinum Unlocked - Challenge - Adjusted v1.1.nds`. Donor is Hardlove Gold 0.6.9.
- 2026-09-15: Everything in the working folder is a copy; the Hardlove ROM and its DSPRE contents may be modified. Originals elsewhere are never touched.
- 2026-09-15: The design doc is instructions for Claude, the tracker is a barebones top-level list. Both live in the repo under `docs/oxide/` and are mirrored to the working folder by `tools/oxide/sync-docs.sh`.
- 2026-09-15: Base ROM's Unlocked / Challenge-Adjusted tags are irrelevant; it is just a non-vanilla Platinum. Content scope: species subset, all moves with assets, all abilities.
- 2026-09-15: Hardlove Gold is confirmed built on hg-engine. Phase 1 works from hg-engine's source first, donor ROM second.
- 2026-09-15: Phase 1 finding: no scoped element is achievable at the data layer. Approach A is off the table.
- 2026-09-15: Approach C chosen. Fork of pret/pokeplatinum is the source of truth; Ian builds in WSL2.
- 2026-09-15: Battle AI is in scope; Platinum's AI is the baseline; Hardlove's AI is excluded.
- 2026-09-15: `~/roms/vanilla.nds` (byte-exact Rev 1, built once from `main`) is pinned for `import_base_rom.py --vanilla` and `verify_narcs.py --ref`; don't rebuild it. `~/roms/base.nds` is a pinned copy of the base ROM. The base ROM in the project folder must not be deleted, moved, or overwritten; it is the reference every verify tool compares against and the only source for the three hard stops.
- 2026-09-20: The encounter tool's design doc, survey and build plan are repo-tracked under `docs/oxide/`. Its findings (the R1 amendment Ian accepted, the descriptive/aspirational split of the linter's thresholds, the survey numbers that did and did not reproduce) live in the build plan's milestone sections, and the design doc carries them as v1.1 amendments.
- 2026-09-20: The species pick-list is an availability list, not a deletion list; National Dex IDs stay as internal species IDs (see Phase 0 and Phase 4's ID-scheme prerequisite).
- 2026-09-20: The remaining scripts, events and text were generated in bulk from the base ROM's bytecode at Ian's direction, trading readable source for being done. Re-humanising is backlog, a map at a time.
- 2026-09-20: Doc ownership for parallel sessions. Status has one home per track (this file for Phases 0 to 5, the build plan for the encounter tool); a track edits only its own home plus, for the encounter tool, its one paragraph at the top of this file. A second track's commits live on their own branch or worktree and merge into `oxide` when their tests are green. This replaces the "keep your footprint to one line" convention that still produced three tracker merge conflicts in a day.
