# Platinum Oxide: Tracker

Top-level task list. Tick items as they finish; add detail only where it changes what happens next. Companion to `docs/oxide/design-doc.md`.

Where things live, so each fact has one home: **status** is here; **durable facts** are in the design doc's findings log (section 8); the **encounter tool's** status, milestones and findings are in `docs/oxide/encounter-tool-build-plan.md`. Everywhere else points, it does not repeat.

**Where things stand (2026-09-22).** Phase 3, carrying the base ROM's edits into the source tree, is finished. **Phase 4, the engine port, is on element 4.** Elements 1 and 2 are done: the Fairy type, apart from two pieces of fixed Pokedex and Battle Hall art, and abilities widened to u16 with a hidden-ability slot. Element 2 broke the save format, so a test needs a new game. Element 3's structure is done, with all 159 new species in at ids 494 to 652, and what is left of it is fill-in. Element 4's move table is in, 923 moves with names, descriptions and animations, and the new species' learnsets are refilled; what is left is the new battle effects, of which 106 of 116 are done, need nothing, or are out of scope, and the rest are stubs. Each element's section below has the detail. **The machine's CPU is degraded until its warranty replacement arrives**: a build can crash and pass on a retry, and a local ROM is trusted when its hash matches GitHub's build of the same commit (CLAUDE.md, Build). **Playtest ROMs for Ian come from `tools/oxide/fetch-rom`** (2026-09-23), which builds a pushed commit in the private repo `iradspinner/oxide-rom-builder` and downloads it after checking its SHA-1; its first build reproduced the public workflow's hash for `458a34f59` exactly, so the private build is the ROM of record. **Bug fixing is its own track**, whose status home is the "Open bug" entries under Phase 5 and nothing else here; its method is the `debug-live` skill, with Ian driving melonDS. `git log -1` is the resume point; no commit hash is kept in this file because it goes stale within the hour.

**Second track: the encounter tool.** A separate tool for designing Oxide's wild encounter tables. It owns `tools/oxide/encounters/`, `res/field/encounters/`, `docs/oxide/encounters/` and the three `encounter-*.md` docs, and touches nothing else. **The tool is complete, all seven milestones done** (M1 round-trip I/O, M2 analysis, M3 linter, M4 browser editor, M5 dupe-out planner, M6 levels-only generator, M7 source-versus-ROM check; the generator's full species placement is deferred, possibly for good). **The authoring pass has started: writing every wild table from the species pick-list**, planned in `docs/oxide/encounter-authoring-plan.md` (all 171 land tables, early corridor first; the sidecar becomes the design source; its two-stage split is moot now that element 3 has put all 159 new species in the tree, so every pick-list line is placeable from Step 2 on and the reservation mechanism is not needed). Steps 0 to 2 are done: the tooling (35/35), the progression `order` and `tier` column with R12 evaluating (21/21), and the availability plan (17/17): `docs/oxide/encounters/availability-plan.json` gives every one of the 177 lines a wild home, a scripted source, a honey-tree tier or a place in the random legendary pool (Ian's call: the two lake caverns and the roamer draw from one pool of 23, so a playthrough meets three before the League as vanilla did; the new starters are wild, not gifted; Xerneas and Yveltal are the only new statics, post-League), and `availability.md` is its generated per-line view. Step 3 was regenerated twice on Ian's review (2026-09-21): the tool now designs per capture area (location name) and gym split with his level caps (Roark 16 to League 78) and rod gating, under his cap of about a quarter to a third per species, at Platinum Kaizo's width (eight to sixteen lines a table, which is what the base ROM's tables already were), with starters as the delay prizes and in the Old Rod tails; the Roark and Gardenia splits are written (twenty land tables, fourteen Old Rod tables, per-table lint 0 errors, suites 35/35, 21/21, 18/18, 28/28); its gate is the merge into `oxide` (Ian took the playthrough off the gate, 2026-09-21: too long). Ian approved the first two splits and Step 4 followed the same day: every remaining land table (152) and every water table (198 over 53 areas, surf and the three rods gated by split) is designed, the plan's gate passes, and game-wide lint's only errors are R12's 22 scripted lines (the legendary pool and the two post-League statics; 27 since Steps 7 and 8 re-pooled the gifts). Step 5, the no-leak pass, is done the same day (an Opus subagent on this track's brief): `cli audit --fail-on-leak` exits 0, every encounter source outside scripts is on-list. Suites 35/35, 21/21, 18/18, 28/28, 16/16 and the six milestone suites green. Ian's follow-ups the same evening are in (ice lines early on and beside Mt. Coronet with Alolan Ninetales at home on Route 211 west, Larvesta at the Fuego Ironworks, Mantyke back on the list; the tree has no Alolan Vulpix), and `docs/oxide/pokemon-sources.md` and `.csv` are this track's now, joined into the availability view. Step 6 is done: the branch merged into `oxide` on 2026-09-21 through `integrate.sh`, all 25 checks green and 183 of 183 tables matching their JSON, after one importer fix so that an authored water table counts as authored (the eleven water-only areas had no land cast to protect them). One finding outside the track: this machine's degraded CPU gives wrong answers and crashes under load (`tools/oxide/python_flake_repro.py` is the check), which is why `test_step0` fails on a random file now and then; see Waiting on Ian. Two items for the script track are in the backlog below: Verity Lakefront's header and grass, and the starter's own met location. Step 7 followed on Ian's notes the same day: the scripted gifts are encounters now, sorted into buckets by source (one clown is one source, its pool is even odds) and re-pooled onto the pick-list, which drops the catalogue's off-list species from 37 to 8, with the two remaining trades, the Day Care's Ditto, the museum's four spare fossils and Chimchar flagged rather than changed (`docs/oxide/encounters/scripted-sources.md`); all eighteen Solaceon Ruins rooms share one table because the rooms cannot be told apart in game; Sendoff Spring is the high-value table (the pseudo-legendaries and the best of the preferred tier) and its split moved to League, since only Turnback Cave inside it is post-champion; the Gastly and Misdreavus lines joined the pick-list for the Old Chateau, which incidentally puts the Snowpoint Gengar trade on the list; and anything post-champion is out of scope by Ian's word, which is Turnback Cave and nothing else. Twelve scripts and two text banks now diverge from the base ROM on purpose and `bulk_scripts.py`, `import_base_rom.py` and `bulk_text.py` have each been told. A second round of Ian's notes the same evening flag-guarded every gift (thirteen spare flags at 0x03BF named for them, one per source, set after the Pokemon is handed over), settled Chimchar by putting **Scorbunny** in Rowan's briefcase (`choose_starter_app.c` and `system_vars.c`; the rival's trainer files are named for the player's choice, so none needed touching), moved Scorbunny out of the wild as a gate-tier starter with Litten taking its home on Route 204 north, and recorded the decision to delete the four spare fossil items rather than repoint them. Step 8 the same day is the largest change the tables have had: **every wild slot holds the stage its level deserves** (`cli evolve`, a command rather than a one-off, since it must be re-run whenever levels move), with a line that evolves by level-up judged at its own level and a stone, friendship or trade judged at 30, 20 and 38; the Oreburgh and Eterna trades are rebuilt to take any Pokemon and hand over a shiny Vullaby at 10 and a shiny Popplio at 20, which needed a level table and the removal of vanilla's never-shiny assert in `src/overlay006/npc_trade.c`; and Sandgem and the Fan Club roll their gift like every other source. The Day Care's Ditto became a shiny Floette instead (perfect IVs, neutral nature, level 30), Ian having moved off Sawsbuck once neither Deerling nor Sawsbuck turned out to be in the tree, and ruled breeding out of scope; designing a gift needed the fork's second script command, `GiveDesignedPokemon`, since `GivePokemon` rolls the nature and the IVs. Its white flower is a form this tree does not have, so that part is backlog. The tree builds: `make rom` clean and `verify_narcs --encounters --source` at 184 of 184. Waiting on Ian, none blocking: the pool's scripting (two random statics, one random roamer) is script work outside this track. **M8, a dex and a damage calculator in the tool, is done** (2026-09-22; D1 to D5): a species viewer with sprites, base stats against their vanilla values, learnsets, moves, and cross-links both ways between a species and every table it appears in, plus the MIT `Dynamic-Calc-Decomps` vendored and fed from `res/` so damage numbers are Oxide's. Ian's rulings: build it in this tool rather than feeding `ddex`, go as far as a full local calculator, treat it as an authoring instrument rather than something players see. Uploading the built ROM to ddex was tried and garbles the dex, the encounter tables and the location names, which is exactly what a vanilla-Platinum parser does against archives that gained 159 members and records that grew from 44 bytes to 48 and 56; reading `res/` is the only source correct by construction. The calculator is vendored at `tools/oxide/encounters/calc/` (MIT, upstream `b347b337`, 12 MB of the 649 MB clone, `VENDORED.md` records the pruning and the patch list), and D1, the data layer, is done: `pokedex.py` reads all 652 species, 468 moves, the sprites and the type chart, says what changed from vanilla, and cross-links every species to the tables it appears in, with `test_m8` green. D2 put it on screen: the page has a Tables view and a Dex view now, the dex listing every species with its icon, types and base-stat change from vanilla, and a species page with stats as bars, abilities, the evolution line with how many tables each stage is in, the level-up learnset with each move's numbers, where it is met by location and split, and what every type does to it. The sprites are frame sheets with no alpha, so the server adds the transparency chunk the game implies as it serves them. `test_m8` is at 30 checks, one of which opens every pick-list line. D4 added a Moves view: every move with its changes from vanilla, a flag on each move whose effect is still a placeholder script (read from the scripts, so it clears as element 4 writes them: 136 when D4 landed, 49 at the integration after it), and who learns each move, from which split. On Ian's notes, a species in a table opens its dex page and a Back button retraces the path through browser history. **D5 put the damage calculator on Oxide's data**: a Calc view, fed by `/api/calc-data` from `res/` (Oxide's stats, types, moves and its own type chart), fully offline, in the tool's palette and toggle. Five hand-computed Generation 4 matchups agree roll for roll, Steel's Dark resistance included. Its species picker offers Oxide's species plus 32 hand-picked forms, the twelve with records carrying Oxide's numbers. Every trainer's party is in it too, with each Pokemon's nature rebuilt from the game's own personality roll (checked against upstream's vanilla data: 1,872 of 1,873 natures agree). **Trainer Pokemon can now name their nature** (`"nature": "NATURE_ADAMANT"`), freeing the IV scale: an engine change in `src/trainer_data.c`, the packer and the base ROM importer, taken on by this track on Ian's decision; trainer archives are byte-identical until one is named, and the build plan has the verification. Which trainers get one is Ian's Phase 5 call. `test_m8` is at 82. **Waiting on Ian, not blocking: one damage roll in melonDS against the calculator** (the build plan's D5 says how). Ian signed off the visual design on 2026-09-22. One finding for the calculator, on Ian's hunch and checked in the battle code rather than assumed: **this fork's type chart is Generation 4 with Fairy added**, Steel still resisting Dark and Ghost, so no stock setting matches and the chart has to travel with the exported data. **Ian's visual design, palette B, is built** (2026-09-22, `docs/oxide/encounter-tool-visual-design.md`), in its section 11 order: every colour is a token in `ui/theme.css` written once for both themes, a header toggle follows Windows or pins the other scheme, the pixel face carries the title, tabs and headline, type chips and split tags carry the eighteen type colours, "on" states moved from teal to place, and the Dex species page has its sprite stage and matchup grid. It departs from the design in four places the build plan explains, each because the letter would not have worked, one of them a ladder label under AA in the dark theme. It was rendered in a headless Chrome in both themes and its contrast re-checked against the tokens; Ian signed it off on 2026-09-22. Ian's first notes on it (2026-09-22) are in: play order groups by gym split with Post last, the unused unknown rooms are out of the browser tool, the centre's icons are twice the size with the odds list beside the slot table, every font is 2.5px larger, and the odds follow the time of day. The Overseer's QA pass before the merge (2026-09-22) found five things in this track, all fixed on the branch: Nidoran♂ taken for a mega, evolution lines in alphabetical order, the calculator's remote loads missing from D5's plan, dex caches outliving hand edits, and a bare `/api` answering 500. Its question about Steel keeping Generation 4's two resistances is settled: Ian ruled to keep them (2026-09-22). How to resume cold, what each milestone found, and what it is waiting on are all in `docs/oxide/encounter-tool-build-plan.md`. This paragraph is the tool's whole footprint in this file; per-milestone entries go in the build plan, not here.

**Done in Phase 3, all verified against the base ROM:** species/move/evolution/learnset import; the trainer carry-over (all 928, ability/gender fields, 0 field mismatches, emulator-confirmed); all four synthetic-overlay routines (no items in trainer battles, Rare Candy chaining, uncapped battle frame rate, EV/IV viewer); encounters (125 tables); both in-game trades; map headers (58); the small constant edits; and the scripts, events and text: events 158 of 158, scripts 89 of 91 matching, with `scripts_common` and `scripts_init_battleground` intentionally diverged, text banks 72 of 78, with every mismatch explained under Phase 3 and none of them an outstanding edit. Three sets of base-ROM changes were deliberately *not* carried over, as DSPRE noise rather than edits, each with its evidence recorded: sprite heights, the six vitamin item records, and the encounter `unown_table`/`rate_form` fields.

**Next steps, in order:**

1. **Every track is integrated** (2026-09-22, the second integration that day: element 6's AI fixes, the encounter tool's M8 D4 and D5 with trainer natures, and the test kit's sets 10 to 22, all gate checks green). Element 6 and the encounter branch had a `/qa-pass` first, in `docs/oxide/qa-review-2026-09-22-element6.md` and `docs/oxide/qa-review-2026-09-22-encounter-d4d5.md`; the test kit was left out of QA on Ian's word. Nothing in either blocked the merge, and each open finding is under its element here or in the encounter build plan. The next integration follows the next track to finish; `/integrate` runs it.
2. **Ian: the rest of the emulator pass** under "Waiting on Ian". The nurse hang is confirmed gone; the box deposit hang is confirmed still there and belongs to the bug track.
3. **Bug track:** the box deposit hang first, then the UNLOCK FPS ALWAYS crash entering Sandgem. Each bug entry under Phase 5 says what is known and which breakpoints to start from.
4. **Phase 4 element 4**, the move expansion, is down to its last piece: **the new battle effects**. 107 of the 116 are done, settled, unused or unreachable; the rest are stubs, so a new move does its damage but skips its extra. `convert_battle_scripts.py --audit` lists what is left. Element 3's remaining fill-in, the TM and tutor and egg-move lists, still waits on the Phase 5 TM pass.
5. **The encounter authoring pass** has designed every wild and scripted source and the tables build; it is now in a design and formatting pass on its own branch. What it has left is in its own build plan, and nothing there blocks Phase 4.
6. **Backlog items that are now cheap:** the six overworld sprites the carry-over missed (Phase 3 regression item), and the re-examination of the inventory's other "tool side effect" archives.
7. **Workflow improvements, the agent side** (Ian, 2026-09-22): `docs/oxide/agent-brief-workflow-improvements.md`. Everything but the tracker split is done; the split waits for Ian's word that no other session is working in the main checkout, and the encounter agent does its own half from `docs/oxide/agent-brief-encounter-docs-split.md`. The test kit is built on `worktree-testkit` and merges at the next integration.

**To confirm the state after a restart**, from the repo root, `bash tools/oxide/integrate.sh --verify-only` runs all of this plus the encounter suites, and checks the ROM's hash against GitHub's build of `HEAD`. One by one:

```
make rom
python3 tools/oxide/import_base_rom.py --base ~/roms/base.nds --vanilla ~/roms/vanilla.nds --dry-run   # every count 0
python3 tools/oxide/verify_narcs.py --built build/pokeplatinum.us.nds --ref ~/roms/base.nds
python3 tools/oxide/verify_narcs.py --built build/pokeplatinum.us.nds --encounters --source   # M7: built NARC vs res/ JSON
python3 tools/oxide/verify_narcs.py --built build/pokeplatinum.us.nds --ref ~/roms/base.nds --text
python3 tools/oxide/verify_narcs.py --built build/pokeplatinum.us.nds --ref ~/roms/base.nds --map-headers
python3 tools/oxide/bulk_scripts.py --dry-run   # would write 0; 15 skipped, the deliberate divergences
python3 tools/oxide/bulk_events.py --dry-run    # would write 0
python3 tools/oxide/bulk_text.py --dry-run      # would write 0; 8 skipped
python3 tools/oxide/scriptdis.py --rom ~/roms/vanilla.nds --verify
python3 tools/oxide/scriptdis.py --rom ~/roms/base.nds --verify --base-rom
```

The importer is idempotent, so a non-zero count means something moved. Both `scriptdis` runs should report 574 files walked, 0 failed, 550 init scripts read and 0 movement-target collisions.

**What "clean" looks like now that Phase 4 has changed these tables.** `verify_narcs.py` carries a `DIVERGED` list for Phase 4 changes, the same idea as the bulk tools' skip lists, and any Phase 4 element that changes a base-ROM table must add its members there or the gate fails. Since element 3 the three per-species archives no longer line up with the reference member for member, so the tool maps the reference's indices onto the built ones and reports each archive as a sentence rather than a diff. Expect exactly this:

- `pl_personal.narc`: 667 members against the reference's 508; **0 disagree**, 20 differ only at the intended bytes (the Fairy retypes), 159 are new species
- `wotbl.narc`: 667 against 508; 0 disagree, 159 new. Since element 4 widened the entry this is compared as decoded `(level, move)` lists rather than bytes, so there is no padding line any more
- `evo.narc`: 667 against 508; 0 disagree, 159 new; 7 members differ on purpose (the natives that gain an evolution) and 501 differ only in trailing zero padding, which is the record going from 44 bytes to 56
- `pl_waza_tbl.narc`: 923 members against the reference's 471; **0 disagree**, 452 are new moves, 98 differ only at the intended bytes (the three Fairy retypes and the 95 natives given the King's Rock flag). Element 4 appended to this archive, so the reference's members all stay where they were, including the three inaccessible retail records at 468 to 470
- the base ROM importer reports every count 0 and lists those same seven species' evolutions as not carried over

Anything else is a regression. The encounter tool's own checks are listed in its build plan. `tools/oxide/integrate.sh` runs all of this in one go. This whole list was last run clean on 2026-09-22 with `integrate.sh --verify-only`: 24 passed, 0 failed.

**Files outside the repo that the tools need**, all of them copies, none of them rebuildable from here: `~/roms/base.nds` (the base ROM), `~/roms/vanilla.nds` (a byte-exact Rev 1 build, made once from `main`) and `~/roms/hardlove.nds` (the donor). Also `~/hg-engine`, a sparse 30MB clone of the reference implementation (`data/battle_scripts`, `src`, `include`, `armips/data`), added 2026-09-22 for element 4's effect scripts; no tool depends on it, so it can be re-cloned or deleted freely. And `~/roms/balance-refs/` (2026-09-22), the balance track's reference data: the reference hacks' calculator data and Ian's Run & Bun sheet, with a `MANIFEST.txt` of SHA-256 sums; no tool depends on it yet, the balance plan's B1 will. If any goes missing, copy it again from the project folder on the G: drive; the paths are in the design doc and `docs/oxide/donor-tables.md`.

**Waiting on Ian:**

- **Element 6, the AI fixes** (2026-09-22, integrated into `oxide` the same day): the cheapest check is Camper Zackary (level 15, a Castform with Rain Dance), a Weather-flag trainer. With the `debug-live` method (Ian drives melonDS, the agent attaches and reads), break at the end of `TrainerAI_MainSingles` on his first turn and read `moveScore`: Rain Dance should read 105 and neither attack should carry the Weather flag's +5, where before the fix every move got it (the attacks' own scores also move with Evaluate Attack's ladder, so compare the +5, not the gap). Gardenia's Cherrim with Sunny Day is the same test later. Nothing in the save moved, so no new game is needed
- **The platform fault** (2026-09-22): the i9-14900K is degraded. memtest86 cleared the RAM, and the all-core ratio cap of 55 fixed single-threaded work but not all-core load (design doc findings log, same date, has the numbers). The warranty claim is filed. Meanwhile keep the cap, run `wsl --shutdown` if a process wedges on `__vma_start_write`, and trust a ROM by its hash against GitHub's build. **When the new chip is in:** delete the venv block in the `Makefile`, the build retry in `integrate.sh`, and the wedge guard (its rule in `.claude/hooks/oxide_guard.py`, `wedge_status.sh` and its test beside it, and the `statusLine` entry in `~/.claude/settings.json`), then rerun the parallel check (`C:\Users\Ian\oxide-flake-check\parallel.py`, and the same file from WSL), because WSL failed about ten times as often as Windows under the same load and the wedge may be a second, WSL-kernel problem that the CPU has been hiding.
- **A decision for element 4: port terrain, or not** (2026-09-22). Terrain is a whole field mechanic Platinum lacks: four terrains lasting five turns, each changing damage, status or priority for grounded battlers, set by Electric, Grassy, Misty and Psychic Terrain and ended by Steel Roller and Ice Spinner. Psyblade, Rising Voltage, Terrain Pulse, Expanding Force, Misty Explosion and Grassy Glide read it, and the Surge abilities (element 5) and the terrain seeds (element 7) would need it later. It is new battle state, new messages, a field animation and hooks in damage, status and priority, hg-engine's C for it spreading over 15 files. **Without it**, things stay as they are today: the four terrain moves say "But nothing happened!" and the rest are plain hits, Steel Roller included, though the faithful choice there would be to make it always fail, since the modern games fail it when no terrain is up. Nothing else in element 4 waits on this
- **Element 4, the next fifty-eight effects** (2026-09-22), in the ROM from `tools/oxide/fetch-rom` for the commit that adds the last of them or later: Draining Kiss healing its user by three quarters of the damage; Noble Roar lowering Attack and Special Attack; Heal Pulse healing its target; Pollen Puff healing an ally in a double battle and damaging a foe; Final Gambit knocking out its user only when it hits; Chloroblast costing half the user's HP; Burn Up failing for a non-Fire user, and its user no longer taking Fire's weaknesses afterwards (a pure Fire user takes neutral damage from Water until it switches out); Clear Smog clearing the target's boosts; Freeze Shock and Ice Burn showing their new charge line on the first turn and hitting on the second; Geomancy charging, then raising three stats; Poltergeist failing against a target with no item and naming the item when it works; Jaw Lock's "Neither Pokemon can run away!"; any of the new battle lines looking cut off in the text box, which would mean the width check was wrong; Fell Stinger raising its user's Attack sharply only when it knocks the target out; Stone Axe and Ceaseless Edge laying Stealth Rock and Spikes after they hit; Thousand Waves stopping the target from switching or fleeing; Mortal Spin clearing the user's hazards and poisoning the target; Cotton Guard raising Defense three stages; Quiver Dance, Shell Smash or Victory Dance raising the user's own stats; V-create or Clanging Scales lowering the user's stats after the hit, not the foe's; Fillet Away or Clangorous Soul costing HP and failing when there is not enough; Matcha Gotcha healing its user and sometimes burning; Take Heart curing the user's status; Spicy Extract raising the target's Attack and cutting its Defense; Hurricane never missing in rain and hitting a Pokemon mid-Fly; First Impression failing on any turn but its user's first; Triple Axel's three hits growing stronger each time; Storm Throw or Frost Breath landing a critical hit every time, and never against a Battle Armor or Shell Armor Pokemon; Bolt Beak doing about twice the damage when its user moves first as when it moves second; Flame Charge raising the user's own Speed; Venoshock and Hex hitting harder against a poisoned or statused target; Axe Kick hurting its user when it misses. A new battle script command is in the build, so a freeze or garbled battle right as Bolt Beak or Fishious Rend is used would point straight at it
- **Element 4 in Ian's test kit (2026-09-22, relayed by the Overseer).** Round 3 passed everything through `3da3cb67a`: the three-stage wording (Swords Dance still "sharply"), the charge glows, and every effect from `9664a529a` and `a32b5ab7d` it could set up. Still unseen, because the kit cannot set them up: Pollen Puff's ally heal and Coaching's effect (both need a double battle) and Incinerate burning a berry. Autotomize prints no "became nimble!", as in hg-engine; Ian's ruling is to keep it that way. Round 2 confirmed every effect through `c4d800e11` in game, the Glitzy Glow and Baddy Bad wording fix included, and Bolt Beak's doubling (about half the damage once Mew moved second). Round 1: sets 1 to 9 all worked in battle. Bolt Beak's doubling could not be told apart by eye; the Overseer is changing set 7 so Mew slows itself with Spin Out and compares a Bolt Beak that moves first with one that moves second. Three extras could not be seen because Chansey faints to physical hits: Sappy Seed's seeding, Axe Kick's confusion and Double Iron Bash's flinch; the Overseer is adding a wild Shuckle to the kit for them. Ian's one finding, fixed the same day: Glitzy Glow and Baddy Bad set their screen but said "Glitzy Glow raised your team's Special Defense!", which never mentions a screen. They now say "Light Screen raised..." and "Reflect raised...", and a screen already up no longer marks the move failed after its damage; Light Screen and Reflect themselves print what they always did. Worth a look in the next kit ROM
- **Element 4, the refilled learnsets and the first 15 new effects** (2026-09-23), in the ROM from `tools/oxide/fetch-rom` for `388331c51` or later: a new species' moves on the summary screen or at the Move Relearner; Esper Wing or Zippy Zap raising the user's own stat, not the foe's; Population Bomb's hit count varying from turn to turn. Two known departures from the modern games: Psychic Noise blocks healing for Platinum's five turns, and Population Bomb checks accuracy on every hit, as Triple Kick does
- **Element 4, the moves themselves** (2026-09-22): the cheapest check is the Move Relearner or a TM shop, where a long move list gets drawn; look at whether any name is clipped and whether the descriptions read sensibly in five lines. Then use a new move in battle and confirm it animates and does its damage. **A move whose effect is one of the 114 stubs will do its damage and skip its extra, or say "But nothing happened!" if it is a status move; that is expected, not a bug.** Eight names are known to clip by a few pixels on the summary screen, six of them Z-moves plus Thousand Arrows and Hyperspace Fury
- **Element 4, the widened learnset format** (2026-09-21): level a Pokemon through a move it should learn and confirm it learns the right one at the right level, then open the Move Relearner and confirm its list. Every learnset in the game was repacked, so if the widening were wrong it would be wrong for all 667 of them, not just the new species. No new save needed, nothing in the save moved. Also worth folding in while at the controls: the element 3 form fix, a Rotom in a form or a Giratina holding the Griseous Orb showing its form's stats rather than the base form's.
- Emulator checks not yet done: Rare Candy chaining; the frame-rate uncap (options menu reads UNLOCK FPS, with OFF / BATTLE / ALWAYS); Shinx's ability (always Rivalry, never Intimidate) and Bidoof/Starly hatch time (~255 steps, down from ~3825); and a walk through a few bulk-generated maps, the gift houses being the obvious ones. The "use another Repel?" prompt is now safe to answer yes; it is worth testing, since it is the one thing in this build that was broken and is now fixed rather than carried over.
- The ability/gender nibble direction (1=male, 2=female) is a guess; no Route 202 trainer exercises it. Swap if anything reads wrong in-game.
- **Start a new game first**: the old save reads every ability as NONE by design (element 2 moved the field) and is no longer a valid test bed.
- After the overworld-sprite carry-over lands (Phase 3, regression item): the NPC outside Sandgem's Pokemon Center draws correctly, and so do the Clown-type NPCs on the other 34 maps that share that sprite slot.
- Fairy in the emulator (Phase 5 has the exact checks): a Dragon move doing nothing to Clefairy or Ralts, the summary screen reading FAIRY, and the Pokedex info page showing the NORMAL plate rather than garbage.
- Two data questions from the carry-over, low priority because the default is to leave vanilla: whether the encounter `unown_table`/`rate_form` changes or the sprite-height changes were ever intended (evidence under Phase 3 says they were DSPRE re-saves).
- Tabled, not blocking anything: the 21 native species where `New Pokedex.xlsx` and the base ROM disagree on stats (see Phase 0).
- **The test kit, first run** (2026-09-22, Ian approved it the same day): `docs/oxide/test-kit.md`. Get the ROM with `tools/oxide/fetch-rom --testkit`, start a new game, and try each entry of the menu the scientist in the bedroom's corner offers; its table says what each should show, and between them they reach most of the checks above (the form fix, Sylveon, the widened learnset, Fairy, Rare Candy chaining, the Sandgem crash, the new moves). Each later batch of effect scripts adds its move set to the kit in the same commit.

## Phase 0: Setup

- [x] Working folder created, both ROMs and both DSPRE extractions in place
- [x] Design doc v0.1 and this tracker written
- [x] Ian confirms Hardlove is an hg-engine build (2026-09-15)
- [x] Content scope set: species subset (Ian picks), all new moves with scripts/sprites/animations, all new abilities (2026-09-15)
- [x] Ian finalizes the "other engine changes" list in design doc section 3 (2026-09-20, `docs/oxide/phase4-engine-change-answers.md`)
- [x] Ian produces the species pick-list (2026-09-15, landed in the repo 2026-09-20 as `docs/oxide/species-pick-list.md` and `.csv`). 360 rows: 199 already in Platinum, 159 new slots, 2 cut (Toxel/Toxtricity, which need nature-conditional evolution Gen 4 has no method for), so 358 obtainable entries and 652 species in the finished ROM. Key decision: it is an **availability list, not a deletion list**; all 493 natives stay in the tree, because the 928 carried-over trainers use species the curated dex omits. National Dex IDs stay as internal species IDs, so the donor's index-equals-dex-plus-50 rule keeps working. Verified locally: all 21 rows of its sheet-vs-base-ROM conflict table match what is actually in `res/pokemon` today
- [ ] **Tabled (Ian, 2026-09-20): the 21 native species where `New Pokedex.xlsx` and the base ROM disagree on base stats.** Not a per-row decision to make now. It folds into a larger balance pass over the whole existing dex, which Ian wants to approach with analysis and automation tooling built first. The base ROM's values are what `res/` holds today and they stay until that pass happens. The conflict table is in `docs/oxide/species-pick-list.md`
- [x] **Evolution triggers for the two alt-evolution megas**, Gyarados M and Lopunny M. Answered in `docs/oxide/phase4-engine-change-answers.md` Q4: level-up holding a stone-like item, entered twice as methods 18 (day) and 19 (night), with Dragon Scale and Fist Plate as the proposed items and the choice open to change during the species port. This was listed as outstanding in three places for longer than it was actually open; Ian pointed that out on 2026-09-20

## Phase 1: Find out where Hardlove's expansions live

Done 2026-09-15. Write-up: `docs/oxide/phase1-hg-engine-survey.md`.

- [x] Read hg-engine's repo structure, `CONFIG.md` and wiki; list every engine expansion and how each is injected
- [x] Map each scoped element to hg-engine source files and the ROM files it changes
- [x] Confirm the map against the Hardlove extraction (overlays 129..151 and numbering match; specific config toggles not determinable from the extraction)
- [x] Locate Platinum's type chart, ability field, species/move tables (named in design doc section 8; exact addresses come from a pokeplatinum build in Phase 3)
- [x] Check plat-engine's state (dormant since 2023-08, ~10% of hook surface, same architecture as hg-engine)
- [x] Inspect the VSMaker2 Data folder (stale duplicate extraction; ignore)
- [x] Write findings into design doc section 8

## Phase 2: Approach decision

Done 2026-09-15. Write-up: `docs/oxide/phase2-approach-breakdown.md`.

- [x] Write the considerations breakdown for Ian (recommends C; verified by building a matching ROM from the decomp)
- [x] Ian picks the approach: C (2026-09-15)
- [x] Ian answers the logistics questions: keep overworld/trainer/stat/move edits; has GitHub, will fork; wants a WSL2 runthrough; Phase 4 order approved; battle AI added as an engine change, Hardlove AI excluded
- [x] Finalize the scope table in design doc section 3 (2026-09-20, with the engine-change answers)

## Phase 3: Tooling and setup (approach C)

- [x] Ian forks pret/pokeplatinum: https://github.com/iradspinner/pokeplatinum (2026-09-15)
- [x] GitHub access for Claude. Closed 2026-09-20: `git push origin oxide` from the WSL2 checkout works with Ian's own credentials and has done so reliably. The 2026-09-15 note about the app's proxy stripping credentials was about a different environment; it does not apply here. Commits no longer need to be treated as session-local
- [x] Ian sets up WSL2 and builds the fork (`docs/oxide/setup-fork-and-wsl2.md`). Done by 2026-09-15: `~/pokeplatinum` in WSL2 is the working checkout and every build since has run there
- [x] Claude clones the fork and confirms a matching build from it (SHA-1 0862ec35..., 2026-09-15)
- [x] Diff the base ROM against vanilla (`docs/oxide/phase3-base-rom-inventory.md`, with its corrections listed at the top of that file)
- [x] Ian answers the inventory's five Qs (2026-09-15): port all four synthetic-overlay routines; shiny odds raise ports as a one-constant change, palette hue shift deferred/droppable; Battle Arcade script commands port only if the carried-over field scripts call them; apply all eleven battle_edits fixes. Full detail: `docs/oxide/phase3-answers-and-trainer-format.md`
- [x] Add optional `ability` (0|1|2) and `gender` (null|"male"|"female") per-mon fields to trainer JSON and to `tools/dataproc/src/trainerproc.c` (commit 5b8958368)
- [x] Make `TrainerData_BuildParty` (`src/trainer_data.c`) honor those fields, implemented correctly (pick a personality that satisfies the requested gender for that species' ratio) rather than copying the base ROM's buggy +/-2 nudge (commit 5b8958368); verified by inspecting the built `trdata`/`trpoke` NARCs directly, unset fields decode back to ability=0/gender=0 matching prior behavior
- [x] Extend `tools/oxide/import_base_rom.py` with a trainer importer (commit 3877a7641). Extended `jsonstyle.py` with array-index path support and `insert_key()` to reach into `party`'s list of objects. Nibble-direction question resolved: 1=male, 2=female (Ian's guess, 2026-09-15; swap later if anything reads wrong in-game)
- [x] Carried over all 928 trainers (header + party, including ability/gender). The 775 with unchanged party size were patched per-field; the other 153 (107 grow, 46 shrink; Ian resized their parties) had their whole `party` array rewritten in one shot instead of building separate insert/remove-element JSON tooling (commit 92527f42b)
- [x] Verified: `make rom` succeeds. Byte-identical NARC comparison no longer applies (the trainer-mon struct is 2 bytes wider now), so verification decodes both sides field-by-field instead: 0 mismatches across all 928 trainers, header and party
- [x] Emulator-confirmed by Ian (2026-09-15) against Route 202 (Tristan/Logan/Natalie, all bumped to Lv.7, Logan's Burmy using its new moveset): trainer data import is correct in-game, not just in the built NARCs
- [x] No items in trainer battles (commit 26e9b71d9): player side extends the existing `BATTLE_TYPE_FRONTIER_LINK` item-block in `BattleControllerPlayer_CommandSelectionInput` to also cover `BATTLE_TYPE_TRAINER`; AI side stops `BattleControllerPlayer_InitAI` from populating trainer items at all. hg-engine reference: `DISABLE_ITEMS_IN_TRAINER_BATTLE`. Emulator-confirmed by Ian (2026-09-15): Bag blocked with the "can't be used here" message in a Route 202 trainer battle
- [x] Rare Candy chaining (commit 5690f9203): `PartyMenuCB_LevelUp`'s post-level-up state now checks the bag for more of the used item and, if any remain, exits with a new `PARTY_MENU_EXIT_CODE_USE_ITEM_AGAIN` that `start_menu.c` handles by reopening the party menu straight into item-use (same item, mon preselected) instead of returning to the bag. hg-engine reference: `PartyMenu_ItemUseFunc_LevelUpLearnMovesLoop_Case6` in `src/party_menu.c`. **Not yet emulator-tested** (Ian had no Rare Candy on hand); still only verified by `make rom` succeeding
- [x] EV/IV viewer on the summary screen (commit 9af3fe16d). Reverse-engineered from the base ROM's own `EV+IV_Viewer` routine rather than from hg-engine's `IMPLEMENT_NEW_EV_IV_VIEWER`: the base splices `HandleInput_Main+0x34` so R cycles a mode byte 0/1/2, and `SetMonDataFromMon+0x1B2` so the seven stat reads become `MON_DATA_HP_IV..` in mode 1 and `MON_DATA_HP_EV..` in mode 2. In the decomp, `PokemonSummaryMonData` gains `ivs[STAT_MAX]`/`evs[STAT_MAX]` and `DrawSkillsPageWindows` picks which set to print; R is handled in `HandleInput_Main`. Scope held to numbers-only (2026-09-15): the HP/ATTACK/... labels are unchanged in all three views, so no text bank is touched. Three deliberate differences from the base ROM, all in the commit message: the real stats are not overwritten (the base ROM's overwrite makes `DrawHealthBar` divide by zero on a mon with 0 HP EVs), R is `JOY_NEW` not `JOY_REPEAT`, and the refresh is `PokemonSummaryScreen_DrawExtraWindows` rather than `ChangeSummaryMon(screen, 0)` (which replays the cry and can loop forever on an egg). **Not yet emulator-tested**; only verified by `make rom` and by reading the rebuilt arm9
- [x] Uncapped battle frame rate (commit 83770cd5b). Not ported from hg-engine's reference (raw unsymbolized ARM patch, no clean source) but reverse-engineered directly from the base ROM's own synthetic overlay (`weather_sys.narc` member 9, confirmed loaded at `0x023C8000` by diffing base vs `~/roms/vanilla.nds` arm9 and tracing the `NitroMain` boot hook byte-for-byte). `ShouldWaitForVBlank()` in `src/main.c` gates the main loop's `OS_WaitIrq` on `Options_ButtonMode`, repurposing `BUTTON_MODE_START_IS_X` (battle-only uncap) and `BUTTON_MODE_SWAP_XY` (always uncap) exactly as the base ROM does. The options-menu relabel (UNLOCK FPS, OFF / BATTLE / ALWAYS) came over with the text import on 2026-09-20, and exposed two defects in the selector that are fixed in commit 7f2b398e1. The message ids in `res/text/options_menu.json` still read `OptionsMenu_Text_ButtonMode*`; renaming them would mean touching `src/applications/options_menu.c` too, so they were left alone
- [x] Conversion scripts for species, moves, evolutions, learnsets: `tools/oxide/import_base_rom.py` + `verify_narcs.py`; 415 species and 108 move files updated; rebuilt NARCs verified byte-identical to the base ROM (commit c69ef83). The per-species diff of that import is the commit itself and the importer's regenerated `tools/oxide/import_report.md`; the frozen copy once kept under `docs/oxide/` was deleted 2026-09-20 as a duplicate
- [x] Conversion scripts for encounters and trades; heights and items deliberately not carried over (commits 7c2f36ead, 7f2b398e1)
  - [x] **Encounters**: 125 of 183 tables imported and verified field-by-field (`verify_narcs.py --encounters`). `unown_table` and `rate_form0..4` are excluded as DSPRE normalisation, not edits: 98 `unown_table` changes are 0 -> 1, which the game reads identically, and the 17 real ones set every Solaceon Ruins room to 8 (`UnownOnlyExcQue`), which would leave the whole ruins spawning only Unown ! and ? and make the F-R-I-E-N-D letters unobtainable. `rate_form0/1` picks the Shellos/Gastrodon form; 61 of the 63 changed tables contain neither species. Listed under "Waiting on Ian" in case either was intended
  - [x] **Trades**: both edited records imported and byte-verified. Kazza's Abra is now a Ditto with perfect IVs holding a Destiny Knot; Gaspar's Haunter is a Gengar. The json filenames still say abra and haunter
  - [x] **Heights: not carried over.** 298 of 1,976 `height.narc` members differ, but 164 of those write a byte where vanilla has an empty member (a gender the species does not have, which the decomp derives from the gender ratio and cannot express), and of the 116 species where vanilla had male and female offsets equal, every single one has only the male offset changed. A hand edit to a shared sprite would move both. Reads as the same DSPRE re-save that touched 2,788 of 2,964 `pl_pokegra` files. Listed under "Waiting on Ian"
  - [x] **Items: not carried over.** The only six differing records are the vitamins, and each grew from `ItemData`'s 34 bytes to 36 with the EV amount zeroed and everything after it shifted one byte right, so the friendship values no longer line up with the struct. That is DSPRE writing a malformed record, not an edit. The vitamin change Ian described is the EV cap (100 -> 252), which is a code edit and is done under the small constant edits
- [x] Carry over map-header edits (commit 56b6609db). 58 of 593 headers: 57 weather and one battle background, all hand-made. The three type gyms get matching weather so the battle inherits it (Oreburgh sandstorm, Pastoria heavy rain, Snowpoint blizzard), Bertha's room sandstorm and Flint's ashfall, Mt Coronet B1F / Victory Road / every Turnback Cave room lose their fog, and Turnback Cave's Giratina room switches its battle background to the Distortion World. Verified byte-identical to the base ROM with `verify_narcs.py --map-headers`, which finds the table in both ROMs by content rather than by a build address
- [x] Carry over text. The 18 banks whose message count was unchanged were imported by index and verified message-for-message (`verify_narcs.py --text`): the options-menu relabel, Vs. Seeker renamed Pocket PC, Attack Order's description, 71 trainer names. The remaining 62 banks, the NPC dialogue for the field scripts, came over in the bulk pass below. Where a base-ROM bank filled a slot vanilla leaves unused (the Repel menu entries, a Teleport System entry, two Wayward Cave lines), the new message got a generated id; the code or script that calls it is a separate matter, see hard stop 3
- [x] Carry over scripts, events and their text. Six maps were rewritten by hand in the repo's idiom (`oreburgh_city_middle_house`, `sandgem_town_house`, `solaceon_town_northeast_house`, `floaroma_meadow_house`, `mt_coronet_1f_north_room_2`, `iron_island_b3f`) and 70 events-only maps patched; then **the rest was generated in bulk on 2026-09-20 at Ian's direction**. Against the base ROM: **events 158 of 158, scripts 89 of 91 matching, with `scripts_common` and `scripts_init_battleground` intentionally diverged, text banks 72 of 78**, no regressions. Method and tools: `docs/oxide/phase3-scripts-and-events-plan.md`
  - Tools: `tools/oxide/bulk_scripts.py`, `bulk_events.py`, `bulk_text.py` (each skips anything the current build already gets right, so the six hand-written maps keep their idiomatic source); `mapdiff.py` and `checkmap.py` for one map at a time; `scriptdis.py` underneath all of them
  - **The cost, and the thing to undo later.** The 86 generated scripts have machine labels (`JubilifeCity_0A4C`) and numeric operands where hand-written source would use named labels, `NPCMessage` and text-bank constants. New object events are named `LOCALID_OBJECT_<n>` and new messages `<Bank>_Text_<n>`. All correct, none readable, and it replaces hand-written upstream source, which is what the project's no-reformatting rule exists to protect. Every generated script says so at the top and the hand-written versions are in git history, so they can be re-humanised a map at a time (backlog)
  - [x] **Hard stop 1 closed (2026-09-20): `scripts_init_battleground`.** The base ROM's member starts with a zero, and that zero is the terminator: `FieldSystem_GetFixedInitScriptID` and `FieldSystem_GetFrameTableInitScriptID` both stop on it and return 0xffff. **The Battleground has no init script at all now**, where vanilla ran script 4 on transition and consulted a frame table on `VAR_BATTLEGROUND_STATE`. That fits the rest of the map being gutted. The 53 bytes after the terminator are the old table, left in place by DSPRE and never read; this builds the 4-byte equivalent instead, which the engine cannot distinguish. Listed in `bulk_scripts.py`'s `DIVERGED`. It was never a format problem: the earlier note said it needed its own emitter, when the whole content is one byte
  - [x] **Hard stop 2 closed (2026-09-20): `TEXT_BANK_NPC_TRAINER_MESSAGES` contains no edit.** The mapping never needed trainerproc's packing order: `poketool/trmsg/trtbl.narc` is that mapping, one (trainerID, messageType) pair per bank slot, already in the ROM and byte-identical between the ROMs. `tools/oxide/import_trainer_messages.py --check` validates all 2,497 slots against `res/trainers/data`. With the mapping in hand, **all 373 differences are unused slots whose filler byte DSPRE zeroed, every one on a DUMMY trainer**; no message text differs anywhere. Fourth instance of the same DSPRE re-save noise, after sprite heights, the vitamin item records and the encounter `unown_table`/`rate_form` fields, so vanilla's values are kept
  - [x] **Hard stop 3 closed (2026-09-20): the Repel prompt.** `SetRepelSteps <count>` added as a real script command, writing through `SpecialEncounter_GetRepelSteps`, registered at the end of the command table so no existing opcode moves; the three branches in `scripts_common` use it and drop their `ScrCmd_Unused_007` pokes. The base ROM's workaround (poke a fixed scratch address, then a hand-written routine in place of `Dummy088` copies it through the save pointer) is not reproduced. **`scripts_common` is now 15 bytes shorter than the base ROM's and is the first file that deliberately does not match it**, so `bulk_scripts.py` carries a `DIVERGED` list and refuses to regenerate it. Without that the restart check-list would silently undo the fix
  - Not defects, recorded so nobody re-opens them: `SPECIES_NAME` and `SPECIES_POKEDEX_ENTRY_EN` decode identically already; `NPC_TRAINER_NAMES` differs only in the `{TRNAME}` tag DSPRE re-applied across the bank; `SOLACEON_TOWN_NORTH_HOUSE` and `SURVIVAL_AREA_SOUTH_HOUSE` have identical text but DSPRE re-encrypted them with key 0xd43f where vanilla uses a per-bank key, and any key decodes the same; `ITEM_NAMES_WITH_ARTICLES` differs because renaming Vs. Seeker to Pocket PC propagates to the derived bank here where DSPRE left "a Vs. Seeker" behind
- [ ] **Regression found in play (Ian, 2026-09-20): the base ROM's custom overworld sprites were not carried over.** Ian saw a red glyph where an NPC stands outside Sandgem's Pokemon Center. That NPC is object 15 of `events_sandgem_town.json`, a base-ROM addition with `OBJ_EVENT_GFX_DUMMY_075`, and 43 object events on 35 maps use dummy graphics slots the same way (39 use `DUMMY_075`; `DUMMY_057`, `058`, `061`, `066` once each, on Mt Coronet outside north and south, Resort Area and Stark Mountain room 1). The base ROM fills those slots: `data/mmodel/mmodel.narc` members 55, 56, 59, 64, 65 and 73 (`dummy_0.nsbtx` to `dummy_5.nsbtx` in `res/graphics/field_sprites/field_sprites.order`) are 1,640-byte placeholders in vanilla and real sprites in the base ROM (6,760 bytes each, one 13,352, one 4,544). The Phase 3 inventory listed that archive's six changed members as "building models" under tool side effects; they are overworld sprites and they are Ian's edits. Overlay 5's sprite tables are unchanged in the base ROM (its four ov5 differences are all in code), so vanilla's dummy rows already describe these slots as generic 32x32 walking sprites in the follower palette group, and the fix is data only: extract the six members from `~/roms/base.nds` and build them in place of `dummy.png` for those six entries in `res/graphics/field_sprites/meson.build` (prebuilt NSBTX is the faithful route; converting back to PNG is optional). Verify by rebuilding and comparing `mmodel.narc` member for member against the base ROM, then Ian looks at the Sandgem NPC. **Follow-up in the same spirit:** re-examine the inventory's other "tool side effect" archives (`trfgra` 8 trainer sprites, `pl_batt_bg` 78, `pl_batt_obj` 50, `pl_otherpoke` 26, `titledemo` 2, `item_icon` 1, `waza_particle` 1) with the same test, a member growing from a placeholder size or changing beyond its header is an edit, not a re-save
- [x] Re-apply the base ROM's small constant edits as C changes (commit 9f901de9c): shiny threshold 8 to 255 (~1/8192 to ~1/257), vitamin EV cap 100 to 252 (`MAX_EV_VITAMIN`; the total is still capped by `MAX_EVS_ALL_STATS`), new-game defaults of fast text and SET battle style, HMs forgettable from the summary screen's move-select (the `HandleInput_SelectMove+0x8E` edit the inventory had misfiled under the EV/IV viewer), and **reusable TMs**. That last one is a correction: the inventory read `TeachMove+0x5B` as "probably HMs can be forgotten", but the call it skips is `Bag_TryRemoveItem`, so the base ROM simply never takes a TM out of the bag. Each edit was traced to the exact instruction the base ROM changed and confirmed in the rebuilt arm9
- [ ] Still not ported from the base ROM's arm9, all needing real understanding rather than a constant swap: the Vs. Seeker repurposed as a portable PC (`UseVsSeekerFromMenu`, two sites, pairs with the item text already imported); the palette hue-shift patch (six hooks into code written over the Battle Arcade region, deferred as low priority, droppable); and a single two-byte change to a lookup table at arm9 `0x0EC478` that has not been identified. The Battle Arcade custom commands are closed: only `Dummy088` is called (hard stop 3), the rest is dropped
- [x] Working `git pull && make rom` loop with Ian, verified end to end with one visible change (2026-09-15)

## Phase 4: Port, one element at a time

Started 2026-09-20 with the Fairy type. The order was approved by Ian on 2026-09-15 (from `docs/oxide/phase2-approach-breakdown.md` section 6), chosen so each step is testable on its own and the structural changes come first while the tree is still close to vanilla. hg-engine's source is the reference implementation for each feature; Hardlove's tables are the content source. Each element gets its own checklist here when it starts, with the emulator test that proves it.

Prerequisites, none of which touch a Phase 3 file:

- [x] **Read Hardlove's `a/0/2/8` members 9_07..9_13 directly (2026-09-20).** Written up in `docs/oxide/donor-tables.md`, with the evidence for each member, since none of it came from hg-engine's source. 7 hidden abilities, 8 base experience, 9 icon palettes, 11 form data, 12 form-to-species, 13 reversion, and 10 is not needed by the port and is recorded only so nobody re-derives it. The reader is a new `tools/oxide/donor.py`, which also decodes the species records, the learnsets and the evolutions; `tools/oxide/import_donor.py` is the writing half. The donor ROM is pinned at `~/roms/hardlove.nds`
- [x] Ian's "other engine changes" list. **Answered 2026-09-20: `docs/oxide/phase4-engine-change-answers.md`**, grounded in what the donor ROM actually shows rather than hg-engine's menu. Two findings the Phase 1 survey could not get from the extraction: Hardlove's item table is **2,687 records of 36 bytes against Platinum's 446 of 34**, and its evolution records hold **9 slots and use 19 methods Platinum does not have**. The menu is also shorter than it looks, because much of it arrived with the base ROM in Phase 3
- [x] **Species-ID scheme settled (Ian, 2026-09-20): numerically increasing after the end of the Platinum dex.** The National-Dex-as-internal-id idea from the pick-list is dropped; it would have grown every species-indexed table to 1010 slots plus forms to hold 159 species. New species append after `SPECIES_ARCEUS`, taking **494 to 652** in `dex_pos` order, and `SPECIES_EGG` and `SPECIES_BAD_EGG` move up to 653 and 654. Everything sized by the species count derives from the enum, so that one insertion carries `MAX_SPECIES`, `NATIONAL_DEX_COUNT`, `MOVESET_FORM_*`, the sprite-offset count and the three per-species archives (508 to 667 members) with it. The ten regional forms and the two alt-evolution megas take ids of their own rather than form records, because each has its own `dex_pos` and Gen 4 forms cannot have separate dex entries. Written up in `docs/oxide/species-id-scheme.md`, assignment in `docs/oxide/species-id-map.csv`, **frozen once element 3 lands**

Elements, in order:

- [x] **1. Fairy type (2026-09-20).** Done in two commits, the mechanism and then the Generation 6 retypings, so either can be reverted alone.
  - `TYPE_FAIRY` goes on the end of `generated/pokemon_types.txt`: type 18, `NUM_POKEMON_TYPES` 19. On the end rather than in a gap, because Hidden Power's "skip the Mystery type" arithmetic then needs no change and Fairy is not a Hidden Power type in Generation 6 either
  - Three matchup tables: `sTypeMatchupMultipliers` (battle engine and AI both read it), `sMoveTesterTypeChart` (the Poketch app's square chart, so every row gained a column), and they agree entry for entry. The twelve Fairy entries sit **before** the `0xFE` marker in the battle table, because everything after that marker is what Foresight ignores
  - `res/text/pokemon_type_names.json` gains FAIRY as message 18. `res/graphics/battle/type_icons/fairy.png` is a new 32x16 plate drawn against sub-palette 1 of `shared.pal`, the one with the pinks; it reuses colours already there rather than adding a fourth sub-palette, because the palette count is baked into every screen that shows a type icon. `sMovePaletteFairy` gives the move-select box its pink
  - **The trap, for anyone adding a type later:** the species processor packs the dex index archive as one bucket per type and *then* one per body shape, so an eighteenth type bucket silently shifts every body-shape bucket down one. `PDSI_FAIRY` is inserted into `pokedex_sort.c`'s positional enum to match, with `FT_FAIRY` beside it
  - Retyped to Generation 6: twenty species (the Clefairy, Jigglypuff, Togepi, Marill, Snubbull and Ralts lines plus Mawile and the Mr. Mime line) and three moves (Charm, Sweet Kiss, Moonlight). Each species was checked against vanilla first so no base ROM edit was overwritten; none of the twenty had one. **Which species end up Fairy is a Phase 5 balance question**; this is only the canonical set
  - **The chart is Generation 4's with Fairy added**: Steel keeps its resistances to Ghost and Dark, which Generation 6 removed when it added Fairy. Ian's ruling, 2026-09-22, after the encounter tool's dex surfaced it; `test_m8` pins it
  - Arceus plates are **not** done and cannot be: Multitype reads the held item's `HOLD_EFFECT_ARCEUS_*`, and there is no Pixie Plate item. It belongs with Phase 4 element 7, the item pass
- [ ] **Fairy, the two pieces left, both fixed art.** Neither affects battle:
  - The **Pokedex info page** draws type plates from its own packed atlas (`res/graphics/pokedex/type_icons.png` plus its cell, anim and palette data), which still holds eighteen. A Fairy Pokemon shows the NORMAL plate there. `PokedexGraphics_GetAnimIDfromType` gained a `default` at the same time, because vanilla left `animID` uninitialised for any type it did not list, which a new type walked straight into
  - The **Pokedex search grid** and the **Battle Hall grid** are fixed cell layouts with no Fairy cell, so neither offers the type. `FT_FAIRY` and the Battle Hall's type-indexed trainer class row exist so the data is ready when the art is
- Known and intended: the built ROM's dex index archive and battle sprite NARC each hold one member more than vanilla, and `pokemon_type_names` one message more. `verify_narcs.py` against the base ROM will report all three; they are the Fairy type, not a regression
- [x] **2. Ability field to u16, plus the hidden-ability slot (2026-09-20).** The id space is now 16 bits everywhere an ability is stored or passed; nothing caps it at 512 or at anything else, so the cap is whatever `generated/abilities.txt` grows to.
  - **Species record.** `u8 abilities[2]` became `u16 abilities[3]`, which takes `pl_personal.narc` from 44 bytes a record to 48 and shifts everything after the abilities by four. Slot 2 is the hidden ability and `ABILITY_NONE` there means the species has none, which is all 508 of them today. `SPECIES_DATA_ABILITY_HIDDEN` reads it. In the species JSON the third entry is optional, so five hundred files did not need an `ABILITY_NONE` written into them
  - **Stored ability.** Block A of a boxed Pokemon was full, so the ability moved to block B's spare u16 at 0x1A and block A's byte is dead. `MON_DATA_UNUSED_114`, the only accessor for that spare, lost its cases; its enum member stays so nothing else shifts. Written up in `docs/oxide/save-layout.md`, which is the file Phase 5 asked for
  - **Everything else that held an ability in a byte**: `BattleMon`, `Battler_Ability`'s return, `DamageCalcParams`, `RefreshPartyStatusMessage`, the summary screen, the wild-encounter field params, the daycare and AI locals. `trainer_data.h`'s `u8 ability` is a slot selector, not an id, and stayed. `FrontierPokemon` is a packed data record and also stayed a byte, so the one call site that fed it to the setter now widens it first rather than reading a byte of friendship along with it
  - **Hidden abilities are a mechanism, not yet a policy.** `Pokemon_TryGiveHiddenAbility` switches a Pokemon to slot 2 and returns FALSE when the species has none, and a new `GiveHiddenAbility partySlot, success` script command exposes it. Nothing calls it: which encounters and gifts hand out hidden abilities is the Phase 5 encounter-design question, and no species has a hidden ability until element 3 brings Hardlove's `a/0/2/8` table across
  - Verified by rebuilding: all 508 personal records agree with the base ROM field for field once the four-byte shift is applied; a temporary hidden ability on Clefairy read back correctly from the built NARC and was then reverted; the script disassembler still round-trips 574 of 574 with the new opcode in its table; the three bulk dry-runs still report nothing to do
- [x] **Emulator check for element 2, done (Ian, 2026-09-20).** Every existing Pokemon's ability had moved to a different block, so if the move were wrong every ability in the game would have been silently gone. Confirmed on a new save: Piplup shows an ability and the starter battle runs, which is the in-play proof that the moved field reads and writes correctly. An old save still reads every ability as NONE, so it stays a new-save-only build

- [ ] **3. Species-slot expansion.** Started 2026-09-20. Raise `MAX_SPECIES`, expand personal/evo/learnset/sprite/icon/dex/text tables and the save's dex flags, PC to 30 boxes. Evolution records go to nine slots. The overworld sprite import should cover every species, not only the ones Platinum shows, so a follower system stays possible later. Bring in the 159 pick-list species from Hardlove's 44-byte `SpeciesData` records, with the `a/0/2/8` tables from the prerequisite. Test: a new species is catchable, appears in the dex, and battles. Step by step:
  - [x] **The ability list (2026-09-20).** The 159 need 59 ability ids Platinum does not have, so this comes first. The whole donor table came across rather than those 59: ids 0..123 already agree, so importing 124..318 contiguously makes donor ids and Oxide ids the same number for good and spares every later import a lookup table. Names, uppercase names and descriptions are in `res/text`; the **effects are element 5**, so all 195 are inert. Where Hardlove reuses a display name (As One twice, Embody Aspect four times, Placeholder twice) the repeats take a numeric suffix rather than a guess at which form each one belongs to
  - [x] **The move list does not block this (measured 2026-09-20).** Learnsets reference moves Platinum does not have, and `SpeciesLearnsetEntry` packs `move:9 / level:7`, so it caps at 511 against the donor's 924. Measured against the 159: **2,046 of 2,571 learnset entries survive a filter to Platinum's own 467 moves, 80 per cent, no species is left with an empty learnset and the median keeps 13 of 15.** So the species records can land now with filtered learnsets and be refilled when element 4 widens the format and brings the moves. Twenty-eight entries are at level 0, hg-engine's evolution-move marker, which Platinum has no concept of and which become level 1
  - [x] **The sprite pipeline is proven (2026-09-20).** The donor's `a/0/0/4` is laid out exactly like Platinum's `pl_pokegra`, six members per species in the same order, uncompressed NCGR and NCLR. `nitrogfx <member> <out>.png -encodefronttoback -palette <normal.NCLR>` decodes a sprite and writes the `.key` the build needs to re-encode it, and an NCLR converts straight to the JASC `.pal` the build wants. Checked visually on Annihilape: both animation frames, correct palette
  - [x] **The species records and the graphics (2026-09-20).** All 159 written by `tools/oxide/import_donor.py species`, which is idempotent and re-runnable. **Three sources, each authoritative for different fields**, the split and the evidence in `docs/oxide/donor-tables.md`: base stats, types, both abilities and the hidden ability from `New Pokedex.xlsx`, which matches all 159 by `dex_pos` and name; everything else from the donor, including sprites, palettes, icons and cries. The sheet and the donor disagree on stats for only the two alt-evolutions, on types for four species and on abilities for about 124, because Hardlove redesigns abilities wholesale
  - [x] **Cries (2026-09-20, fixed 2026-09-21 after Ian found them broken).** The donor keeps one wave archive per species, `WAVE_ARC_PV<species>`, each a single IMA-ADPCM sample; 150 of the 159 have one and the nine that do not are regional forms that take their base species'. The decoder is thirty lines in `species_import.py`, checked by decoding Lucario's and correlating its envelope against the one already in the repo, 0.87. **Generating the files was only half of it and the first pass stopped there**, which is why a wild Glimmora was silent and its Pokedex cry screen crashed. Two things were missing. Every cry needs a bank and a wave archive in `res/sound/pl_sound_data.json` at its own species id, because `Sound_PlayPokemonCry` indexes that table by species directly; indices 495 to 699 were empty placeholders so the 159 fit at 494 to 652, with Shaymin Sky moving off 494 to 653. And **a cry has a size limit**: `PLAYER_PV`'s heap is 24,200 bytes and has to hold the bank and the whole wave at one byte a sample, so the ceiling is vanilla's longest cry, Jynx's 23,524 samples. HeartGold trims less and 41 of the 159 were over it; they are cut to that with a short fade
  - [x] **A latent `nitrosfx` bug came out of the same chase and is worth sending upstream.** `WriteSdat` writes the FAT, then pads the first file's data to a 0x20 boundary, but computes the FAT offsets without that padding, so every file in the archive reads from a few bytes early. It has never shown because the padding is zero for the unmodified game; adding 159 cries moved the FILE block and made it 8. Fixed in `tools/nitrosfx/sdat.c`, and the fix is a no-op when the padding is zero, so a matching build is unaffected
  - [x] **Two engine changes the data forced.** Base experience was a byte and these species run to 340, so it moved into the two bytes of padding at the end of the record, leaving it 48 bytes and nothing between catch rate and the abilities shifted. `MAX_LEARNSET_ENTRIES` was 20 and three species have more, so it is 34, the donor's own slot count; `move_reminder_data.c`'s hand-written 22 now derives from it
  - [x] **Nine evolution slots and the seven natives that gain an evolution (2026-09-20).** Eevee forced the widening, exactly as the engine-change answers predicted. `LoadSpeciesEvolutions` was also reading a whole 44-byte member into a 42-byte buffer, true in vanilla too, and now uses the member's size
  - [ ] **What is deliberately left short**, none of it blocking a new species from being caught, seen in the dex or battling:
    - **TM, tutor and egg-move lists are empty.** Hardlove keeps TM compatibility somewhere this project has not found, and how many TMs and which moves is a Phase 5 pass, so filling them now would be work thrown away. A type-based default could be generated in an hour if the species need to be playable before that pass
    - **The dex size-comparison scales and positions are still defaults.** They affect only the Pokedex size screen, which draws the species against the player sprite; the donor's dex tables stop at HeartGold's own count, so there is nothing to import and each one is a judgement by eye. Left as a cosmetic pass for whoever is next in the Pokedex
    - **Level-up learnsets are filtered to Platinum's own 467 moves**, which keeps 80 per cent of the entries and leaves no species empty. Element 4 refills them
    - Five species' gender ratio bytes (the Minccino and Gothita lines) read 190 where the enum has 191, a donor off-by-one, and are snapped to the nearest named value
  - [x] **Pokedex entry text, body shapes and footprints (2026-09-21).** The three fill-in items Ian asked to clear. **Entry text**: 159 English entries written for this project, because nothing has them to import, and set in all six language blocks, which is what the placeholder did and what keeps the six banks the same length. Each is checked to wrap into exactly three lines of at most 36 columns, which is the shape all 493 natives have, and to use no character the native entries do not. **Body shapes**: all 159 assigned, every one of the fourteen buckets used, calibrated against natives rather than guessed, since the buckets are not self-explanatory (Primeape is `BIPEDAL_TAILED`, Wooper is `HEAD_LEGS`, Baltoy and Drifloon and Duskull and Spoink are all `HEAD_ARMS`, Lileep and Cradily are `HEAD_BASE`). 129 files changed; the other 30 already happened to match the `SHAPE_BIPEDAL_TAILED` default. **Footprints**: 12 of the 159 now have a real one and the remaining 147 are settled rather than outstanding. A footprint is a Generation 4 feature that Black and White dropped, and every new species here is Generation 5 or later, so no game ever drew one; Black 2 was checked for an archive and has none. What can be done properly is a regional form, a mega or a cross-gen evolution taking its Generation 4 relative's print, which is the same creature: Alolan Ninetales, Annihilape, Galarian Rapidash, Galarian Mr. Mime, Mr. Rime, Kleavor, Sylveon, the three Galarian birds, Clodsire and Mega Lopunny. Galarian Weezing and Mega Gyarados stay blank because Weezing and Gyarados are blank, having no feet. Verified in the built ROM, not just the source: `species_pokedex_entry_en` holds 653 real messages with nothing left of the placeholder, `zukan_data` member 2 reads back the assigned shape for every species, and `pokefoot.narc` is 656 members against vanilla's 497. `make rom` is clean
  - [x] **The caps in code, audited (2026-09-21).** Every site that references `MAX_SPECIES` or `NATIONAL_DEX_COUNT` was read. All but one derive correctly and needed no change; the save's dex flags had already grown on their own, which is the row `save-layout.md` gained on 2026-09-21. **One live defect, now fixed**: `Pokemon_GetFormNarcIndex` hardcoded 496, 499, 501, 502 and 503 as the `pl_personal` and `wotbl` members holding the twelve alternate-form records, and those records moved to 655 onward when 159 species were inserted ahead of them, so the Deoxys, Wormadam, Giratina, Shaymin and Rotom forms were all reading a new species' record for their stats, types, abilities and learnset. The five literals now derive as `MAX_SPECIES + n` in `include/constants/forms.h`; `MAX_SPECIES + 1` is 496 in vanilla, so the expression reproduces the old numbers exactly. Verified against both the pinned vanilla build and the current one, and `make rom` is clean. **Two facts recorded rather than acted on**: `BattleHallWinRecords` is now 3,948 of the 4,096 bytes of its one save sector, which caps `MAX_SPECIES` at 679, and the save budget that 30 boxes has to fit in is worked out in `save-layout.md`. The PC to 30 boxes itself is element 8 and stays there
  - [ ] **Emulator check for the form fix**, cheap and worth folding into the next session at the controls: a Rotom that has taken a form, or a Giratina holding the Griseous Orb, should show its form's stats and types rather than the base form's. No new save needed; the fix is code only and moves nothing in the save
  - [x] **Emulator check for element 3: done (Ian, 2026-09-21).** A wild Glimmora on Route 201, the highest new id at 652, from a debug encounter table. Confirmed working: it appears and battles, sprite and moves right (Toxic Spikes and Rock Throw), Pokedex entry at number 652 and scrolling to it, the menu icon in party and box, the summary screen's types and ability, catching it, storing it in a box, and its cry. Two things failed on the first pass and were fixed the same day, both recorded under the cry entry above: no cry at all, and a hard crash on the Pokedex cry screen. **Two hangs were found during the same session and are logged as open bugs under Phase 5**; they are not known to be element 3's doing
  - [ ] **Re-run that list after each further Phase 4 element.** Start a new game. A new species has to be seen at all before anything else can be judged, and the cheapest way is the Pokedex: evolve or trade one in, or use a debug encounter if one is to hand. What to look at: the sprite and its palette, the menu icon, the cry, the summary screen's types and ability, and that the Pokedex info page does not garble. Charm on an Eevee should now offer Sylveon on level-up
- [ ] **4. Move expansion.** Move table, battle scripts, effect handlers, animations, text. **Widen the level-up learnset format to (u16 level, u16 move) as part of this**: Platinum packs both into one u16 and caps the move id at 511, and there are 924 moves. hg-engine's `data/battle_scripts` converts largely mechanically; animations and particles are the least examined part. Test: a new move animates and does the right damage
  - [x] **The level-up learnset format, widened (2026-09-21).** The confirmed-in-advance half of element 4 (answer Q9), done first because nothing else in the element depends on it and it is what caps the move id today. `SpeciesLearnsetEntry` was one packed `u16`, `move:9 / level:7`, so a move id could not pass 511 and a level could not pass 127. It is now two whole halfwords, `u16 level` then `u16 move`, four bytes an entry. The level field keeps a whole `u16` rather than dropping to a `u8` so the entry stays four-byte aligned, which the packer and every whole-member read already assume.
    - **Where it reached**, and this is the whole list, because the decomp has no assembly: the struct in `include/struct_defs/species.h`, the packer in `tools/dataproc/src/speciesproc.c` (the `& maxbit(9)` and `& maxbit(7)` masks come off), four readers in `src/pokemon.c` that were doing their own `& 0xFE00` and `& 0x1FF` arithmetic on a raw `u16 *`, the signature of `Pokemon_LoadLevelUpMovesOf`, and `src/move_reminder_data.c`, which had its own private `GET_LEVEL` and `GET_MOVE` macros over the same packing. All of them now read `.level` and `.move` and no shift arithmetic is left anywhere
    - **A naming trap for the next reader**: `LEARNSET_SENTINEL_ENTRY` and `LEARNSET_ALL_SLOTS_FILLED` are both `0xFFFF` and are unrelated. The first ends a learnset; the second is a return code from `Pokemon_AddMove`. Vanilla's `Pokemon_LoadLevelUpMoveIdsOf` terminated its loop on the second while reading the first, which worked only because the values coincide; it now names the one it means. Both constants carry a comment saying so
    - **Verified by decoding, not by eye.** `verify_narcs.py` now knows both layouts and compares `wotbl.narc` as decoded `(level, move)` lists rather than bytes, the way element 2 taught it to compare the widened species record: all 508 of the reference ROM's learnsets decode identically under the new format, 0 disagree. Three species were also checked entry for entry against their own `data.json`. `make rom` is clean, `import_base_rom.py --dry-run` is every count 0, and the three bulk dry-runs still write nothing
    - Nothing here reaches the save file, so `save-layout.md` needs no row: `wotbl` is a ROM archive and a Pokemon's four known moves are stored elsewhere and were already `u16`
  - [x] **The donor's move tables, surveyed (2026-09-21):** `docs/oxide/donor-move-tables.md`, read with `tools/oxide/donor_moves.py`. The donor's effect numbering is Platinum's, appended (0 to 276 shared, hg-engine's own 277 to 406), so there is no translation layer.
  - [x] **The move table itself, imported (2026-09-22).** `MAX_MOVES` is 923, up from 468, and `pl_waza_tbl.narc` holds 923 records against the base ROM's 471 with **0 disagreeing and 452 new**. The importer is `tools/oxide/import_moves.py`, which is re-runnable: it owns every move from id 468 up and rebuilds them, and never touches Platinum's own 468
    - **Ids 468, 469 and 470 became real move directories**, `res/moves/unused_468` and its two neighbours. They are the three inaccessible records the retail game ships, and they used to be written by `pack_extra_moves()` in `moveproc.c`. The move enum is positional, so Hone Claws cannot be id 471 unless three entries sit in front of it; the directories reproduce the same bytes, which is why those three members still match the base ROM. `pack_extra_moves()` and `prep_extra_scripts()` are deleted and a comment in `moveproc.c` says why
    - **The enum name rule** is the repo's own, derived and then checked: split camelCase humps, drop apostrophes, every other run of non-alphanumerics becomes one underscore. It reproduces all 467 existing move enums from their display names exactly, ViceGrip to `MOVE_VICE_GRIP` and Double-Edge to `MOVE_DOUBLE_EDGE` included. **The eighteen names that collide are the generic Z-moves**, which the donor stores twice, once physical and once special, under one name; the display name stays as the donor has it and only the enum takes a `_PHYSICAL` or `_SPECIAL` suffix
    - **All 130 of the donor's effect ids 277 to 406 exist now, as stubs**, so the move data is final and element 4's next piece only has to fill in scripts. An effect whose users all deal damage gets effect 0's script and does its damage without the extra; an effect whose users are all status moves gets `BATTLE_EFFECT_DO_NOTHING`'s script, so the move says "But nothing happened!" rather than failing silently
    - **They carry hg-engine's own names**, from `docs/oxide/battle-effect-names.json`, so an Oxide constant and the hg-engine file whose script has to go into it are called the same thing. One deviation and only one: hg-engine's 299 is `HIT_THREE_TIMES`, which Platinum already uses for 104, and the two differ (299 is a flat three hits, Triple Dive; 104 gains ten power a hit, Triple Kick, which is what hg-engine's own name for 104 says). Platinum's constant cannot be renamed without touching every move pointing at it, so 299 is `HIT_THREE_TIMES_FIXED_POWER`
    - **This is also the strongest evidence yet that the effect numbering really is shared.** hg-engine and Platinum give 256 of the first 277 effects the same name at the same id, and the 21 that differ are two projects' words for one behaviour (`TRI_ATTACK` against `RANDOM_PRIMARY_STATUS_HIT`, `PSYWAVE` against `RANDOM_POWER_10_CASES`). 256 identical names at identical ids is not a coincidence
    - **Every new move reuses an existing move's animation**, mapped in `docs/oxide/move-animation-map.json` (an Opus subagent's brief; 250 distinct animations, checked so that no self-targeting move gets a non-self animation and no ordinary move gets a charge-turn one). Fairy has no Platinum animations at all, so its 29 moves borrow Psychic and Normal sparkle. 447 of the 468 existing moves have a `script.s` that is only `GoToEffectScript`, so every new move's battle script is that one line too
    - **Four fields did not carry verbatim**, each decided from measurement rather than taste. Range is a bitmask in the donor, `1 << (n-1)`, and the three moves storing 8|16 take `RANGE_ALL`, which is what all-adjacent plus the user means. King's Rock is set on one record in the donor's 924 against Platinum's 202, and Platinum's own 202 follow no rule that survives testing, so **every new damaging move gets the flag**, which is the later generations' behaviour; the natives were given it too on 2026-09-22 (below). Contest effect comes from the donor's own 0 or 5; contest type is derived from the move's type using the commonest pairing in Platinum's own 467, because hg-engine authored none and the field is noise. Donor type 9 is Fairy and Platinum's type 9 is `TYPE_MYSTERY`, which Curse uses, so that substitution only ever runs on a donor record
    - **The text did not fit and had to be remade.** The donor's descriptions are wrapped for HeartGold's wider box. Platinum's is 15 x 10 tiles, **120 pixels by exactly five lines**, which is the `WindowTemplate` behind both `SUMMARY_WINDOW_BATTLE_MOVE_DESCRIPTION` and `MOVE_REMINDER_WIN_MOVE_BATTLE_DESCRIPTION`. `tools/oxide/textfit.py` measures a string the way `FontManager_CalcStringWidth` does and is calibrated against the game: all 468 of Platinum's own descriptions fit and the widest is exactly 120 px. Every description is rewrapped on import, and the **176 that still needed six lines were shortened** (an Opus subagent's brief, `docs/oxide/move-descriptions.json`, which keeps the donor's text in a `was` field so each edit is reviewable). All 452 fit now
    - **The trap for whoever adds the next batch of anything with a name**: Easy Chat word ids are a running total across text banks, so adding names to an early group renumbers every later group, and those ids are stored in mail and in trainer greetings. Elements 2, 3 and 4 have each done it and only this one noticed; there is now a row in `docs/oxide/save-layout.md`
    - **Deliberately left short.** Eight move names are wider than the 87 px move-name window, six of them Z-moves and the other two Thousand Arrows and Hyperspace Fury; they clip by a few pixels on the summary screen and nowhere else, because the battle move box sizes itself to the string. Contest data for the 452 is a derived default, not authored. The 56 Z-moves are imported as inert data to keep the ids contiguous, since Z-crystals are not in the item list and Megas were cut for the same reason
  - [ ] **The 116 new battle effect scripts.** 97 are done, settled, unused or unreachable (15 done on 2026-09-23, the rest on 2026-09-22); the other 10 are stubs, 7 of them in the focused pass below. hg-engine is cloned at `~/hg-engine`, and `tools/oxide/convert_battle_scripts.py` converts its scripts into Platinum's dialect
    - **The two projects' battle scripts are the same language**: same opcodes and structure, with a header and 53 constant names between them. The rename map is derived on every run from the 277 effects both projects ship, and 16 names no shared script uses are matched by value in the tool's `SUPPLEMENT`. `--selftest` converts the shared effects back: **196 of the 198 that align come out as Platinum's own script exactly**, and the two that do not are String Shot and Rapid Spin, which Hardlove really changed
    - **A defect in last session's version, found and fixed 2026-09-23.** When Hardlove changed a native move, the derivation recorded the two games' different choices as renames: `MOVE_SIDE_EFFECT_TO_ATTACKER` to `ON_HIT`, `SPEED_UP_1_STAGE` to `RAPID_SPIN`, and `SPEED_DOWN_2_STAGES` to `SPEED_DOWN_1_STAGE`. Applied to every script, that would have aimed every self-targeting side effect at battler slot 0 (Platinum's engine sets that target when neither `TO_` flag is given) and taken Esper Wing's Speed boost away. The rule now is that a name Platinum already defines is shared, never renamed. The self-test's old figure of 161 exact was mostly this defect's doing, not the games differing, and last session's "no identifier left unaccounted for" was wrong
    - **`--audit` triages all 116** and `--write` converts only what it calls ready, so it refuses anything already written, including scripts finished by hand. The verdicts as of 2026-09-22:

      | Verdict | Effects | Meaning |
      |---|---|---|
      | done | 79 | a script is written in the tree |
      | settled | 5 | correct with the stub script: Storm Throw's set always crits from C, Psyblade is a plain hit until Electric Terrain exists, Mighty Cleave's move record already lacks the Protect flag as Feint's does, and Techno Blast and Multi-Attack read Drives and Memories, which Oxide does not have. The list is `SETTLED` in the tool |
      | unused | 2 | the donor's Howl and Tail Glow effects, which no Oxide move uses, because the native moves keep Platinum's own effects |
      | unreachable | 20 | no Oxide species learns a move that uses it, so only Metronome reaches it; not ported (Ian, 2026-09-22). Computed from the learnsets, so an effect comes back if a later TM or tutor pass makes one of its moves learnable |
      | C | 5 | hg-engine's C names the effect or one of its moves in code, not just in a list; each needs reading, because some are duplicates Platinum does not need (hg-engine's damage code ignores the script's power multiplier and doubles Venoshock in C, while Platinum's reads the multiplier) |
      | names | 5 | uses a side-effect pointer, subscript or opcode Platinum lacks |
      | text | 0 | prints an hg-engine battle string Oxide has not added; the six it needed are in (below) |

    - **The 15 written** are Population Bomb, Triple Dive, Sappy Seed, Glitzy Glow, Baddy Bad, Freezy Frost, Sparkly Swirl, Zippy Zap, Psychic Noise, Infernal Parade, Barb Barrage, Dire Claw, Spin Out, Alolan Guardian and Esper Wing. Each converted script was read before writing and checked against how Platinum's own scripts aim the same subscript. The built ROM differs from the previous verified one in exactly those 15 members of `be_seq.narc` and nothing else. Two small departures from the modern games: Psychic Noise uses Platinum's five-turn Heal Block rather than two turns, and Population Bomb checks accuracy on every hit, as Triple Kick does
    - **The first twelve of the C group (2026-09-22) needed little C.** Venoshock, Hex, Acrobatics, the Flame Charge set, Axe Kick and Double Iron Bash are duplicates: hg-engine does their extra in C because its damage code ignores the script, and Platinum's own scripts already do the same thing. Relic Song and Wicked Torque are sleep hits whose C is Meloetta's form change, and Meloetta is not in Oxide. Psyblade's C is its Electric Terrain boost, so it stays a plain hit until terrain exists. Triple Axel gets the one line hg-engine does in C, adding 20 power a hit the way Triple Kick's script adds 10. Two needed C. Flower Trick, Frost Breath, Storm Throw, Wicked Blow and Surging Strikes always crit, from a check in `BattleSystem_CalcCriticalMulti` keyed on their two effects that still lets Battle Armor, Shell Armor and Lucky Chant stop them. Bolt Beak and Fishious Rend use `CalcBoltBeakPower`, Payback's rule turned around and the first battle script command Oxide adds, appended after `End` so that no opcode moves. Against the previous verified ROM, the build differs in those ten `be_seq.narc` members and in overlay 16, which grew 128 bytes; the only other differences are 137 branches and one address in arm9 and overlays 13 and 14, every one checked to point into overlay 16 and to have moved with it. Two departures: Axe Kick's crash damage follows Platinum's Jump Kick, not the modern half of the user's HP, and Double Iron Bash keeps the King's Rock flag that every damaging move now has
    - **Seven more of the C group (2026-09-22).** First Impression is Fake Out's first-turn check without the flinch. Diamond Storm is its script; hg-engine's C only stops a spread move raising Defense once per target, so in a double battle Oxide's may roll twice. Hurricane and Bleakwind, Wildbolt and Sandsear Storm never miss in rain, and Hurricane drops to 50 in sun, from the two checks in `battle_controller_player.c` that already did this for Thunder; hitting a flying target comes from the script, as Thunder's does. Mighty Cleave was already right, because Platinum decides Protect from a move's `MOVE_FLAG_CAN_PROTECT` rather than from code, and the donor's record for it lacks the flag, as Feint's does. Checked the same way as the first batch, with `romdiff.py`, which now explains a relink itself (below)
    - **Two tools learned something.** The audit only looked for a bare message number in `PrintMessage`, and Platinum's own scripts never use one in any message command, so it now checks `BufferMessage` and the rest too; that moved Geomancy from C to text. And `romdiff.py` now checks a C change's side effects itself: when an overlay changes size, every difference in the other code files must be a branch or an address into the grown overlay, and anything else is printed as unexplained with a non-zero exit
    - **The converter brings subscripts across now (2026-09-22)**, and fifteen of the names group came with it: Hone Claws, Quiver Dance, Coil, Shift Gear, Shell Smash, V-create, Work Up, Spicy Extract, Fillet Away, Clanging Scales, Hyperspace Fury, Victory Dance, Clangorous Soul, Matcha Gotcha and Take Heart, through 16 subscripts and 16 side-effect pointers appended with `--write-sub` and `--add-ptr` (Entrainment's subscript is in too, but its failure rules are C, so the effect waits). Three facts made it possible, each checked rather than assumed. HeartGold's battle strings are Platinum's with more appended, so hg-engine's message numbers below 1,269 are Platinum's messages at the same index: 459 of the 511 numbered messages in hg-engine's scripts carry a comment with Platinum's text at that index and the other 52 are the same message reworded. The two projects number subscripts 0 to 296 alike, 292 with the same name, and all 145 of Platinum's side-effect pointers have hg-engine's name and number. And nothing stores a pointer or subscript by number, so new ones are appended, only when implemented. With subscripts in it the derivation reads 387 aligned pairs instead of 198 and the map has 176 renames instead of 69; the self-test reproduces 213 of 216 effects and 167 of 171 subscripts, and every difference is a mechanic hg-engine updated (Growth, String Shot, Rapid Spin, Recover's rounding, Uproar, Minimize, the no-target message). No already-written script converts differently under the new map
    - **What the converter now refuses, and why.** A rename must agree on value wherever both sides have one, and a command on its opcode; that rejected hg-engine's new round-up divide being mapped onto Platinum's truncating one, and a false rename of Growth's pointer that the map had carried from the start. A bare number where a command takes a variable is flagged; it caught **a defect in hg-engine's Matcha Gotcha subscript**, which compares the target's HP with battle script variable 0, the battle type, where it means the value 0, and the correction is recorded in the tool's `FIXES`. And a side effect aimed at no battler is flagged when its subscript acts on the side-effect battler, which is trap 2 as a check: it flags none of Platinum's 277 effects and none of the 31 written before it, and it caught Hyperspace Fury, whose Defense drop would have landed on battler slot 0, also corrected in `FIXES`. C that turned out to repeat the script (the stat moves' early "won't go higher" check, Clangorous Soul's HP check) is recorded in `C_REVIEWED` with the reason, and Matcha Gotcha's thaw of a frozen user is ported beside Flame Wheel's. Two departures: Shell Smash still lowers the defenses when its three raised stats are maxed, and Hyperspace Fury has no Hoopa check, which only Metronome can reach since no Oxide species learns it
    - **Six more, and three-stage stat changes (2026-09-22).** `ChangeStatStage` now decodes three-stage rises and drops, matched first and by range because the vanilla chain treats anything past the two-stage drops as one and would index past the stat array; the 14 three-stage pointers are appended in Platinum's stat order, and `--add-ptr` accepts a stat-stage pointer only once `ChangeStatStage` names its range. A three-stage rise reads "sharply rose", Platinum having no "rose drastically". With that, Cotton Guard is Iron Defense at three stages, and Fell Stinger raises its user's Attack three stages when the hit knocks the target out, which works because a hit's side effect runs before fainting is processed (Superpower's self-drop on a knockout relies on the same). Mortal Spin is hg-engine's own subscript, poison then Rapid Spin, behind the pointer hg-engine already defines; Platinum's poison subscript checks for a substitute itself, so the spin always happens and the poison does not go through one. Thousand Waves, Spirit Shackle and Anchor Shot, Stone Axe and Ceaseless Edge use three subscripts written for Oxide, each commented: Mean Look's own could not be reused on a hit, because it plays the move again and fails it when the target is already trapped, and hg-engine's hazard subscripts queue hazards in an ordering Platinum does not have. They end quietly when there is nothing to do
    - **The text group (2026-09-22).** Sixteen battle strings are appended to Platinum's bank, messages 1,269 to 1,284: the charge lines for Geomancy, Freeze Shock and Ice Burn, Poltergeist's and Fickle Beam's lines, and Jaw Lock's "Neither Pokemon can run away!". The text is the donor ROM's own, from its bank 197, which is the bank hg-engine prints from, in Platinum's ally, wild and foe layout and with Platinum's "The foe’s" for HeartGold's "The opposing". Every line was measured against Platinum's own worst case, 196 px with a ten-letter name, and Fickle Beam's was rewrapped, its foe line being 2 px over. The converter's `HG_STRINGS` maps hg-engine's numbers onto them, and its message mapping now stops at Platinum's own 1,269 so an Oxide string can never be mistaken for hg-engine's at the same number. With them: Poltergeist and Fickle Beam as converted (hg-engine's Poltergeist C repeats the script's item check); Geomancy as Skull Bash's shape, its second turn's stray buffered message dropped in `FIXES`; Freeze Shock and Ice Burn as Sky Attack's, the charge line set in each move's own `script.s`; and Jaw Lock through a subscript written for Oxide, trapping both, or whichever is not trapped yet. The three two-turn effects are on `Move_IsMultiTurn`, so Sleep Talk skips them, Protect counts only their second turn and a charge turn survives the target fainting. `import_moves.py` now leaves an existing `script.s` alone on a rerun, as it already did `anim.s` and the effect scripts, because the charge lines live there. **A gap in the gate, found and not fixed**: `verify_narcs --text` compares a grown bank on its shared prefix only when its first message is not a placeholder, and the battle strings' message 0 is one, so the gate does not check them; checked by hand instead, all 1,269 shared messages match the base ROM. Widening the rule exposes `SPECIES_NAME_WITH_ARTICLES`, whose Egg and Bad Egg entries moved on purpose in element 3, so the fix needs the species-index mapping the per-species archives already use
    - **Five more of the C group (2026-09-22), each through a subscript written for Oxide.** Final Gambit deals its user's HP as fixed damage, the way Seismic Toss deals its level, and its user then faints the way Explosion's does, through the self-destructed bit the controller acts on after the move; as a hit's side effect it does not run on a miss, which is the later generations' rule. Chloroblast costs half the user's maximum HP, rounded up by adding one before halving since Platinum's divide truncates; Magic Guard prevents it, Rock Head does not, and Reckless boosts the move as hg-engine has it. Burn Up and Double Shock fail unless the user has the type, then take it away after the hit: a dual type keeps its other type, which Platinum writes as that type twice, and a single type leaves the ??? type until the battler switches out. Clear Smog resets the target's seven stat stages unless it fainted or the hit met a substitute, with Haze's line, as hg-engine prints. Six donor strings for Burn Up and Double Shock joined the bank, messages 1,285 to 1,290, the wild and foe Burn Up lines broken after the name to fit. The converter's own check read `0x10000000` as a name `x10000000` and so had listed a bogus identifier under Bouncy Bubble and Draining Kiss; it now reads a hex number whole
    - **The focused pass (Ian, 2026-09-22).** Ian's ruling: port what an Oxide Pokemon can learn, leave what only Metronome reaches, and hold terrain and Entrainment (which wants element 5's list of abilities that cannot be replaced). No trainer, gift or field script uses a new move, so a learnset is the only way to meet one. The converter learned hg-engine's ten ability names that differ from Oxide's at the same id (MEGA_LAUNCHER against MEGALAUNCHER and the like; 309 of 320 agree). **Seven done so far**, each through a subscript written for Oxide from a Platinum one: Draining Kiss and Oblivion Wing drain three quarters, from Platinum's half drain, so they keep its Heal Block rule rather than hg-engine's (which forbids the move outright); Noble Roar, Tearful Look and Venom Drench are Tickle's subscript with other stats; Coaching fails when its target is the user, since Platinum aims an ally move at the user when there is no ally; Heal Pulse and Life Dew heal half and a quarter, rounded up, and fail under Heal Block, Life Dew reaching the ally in a double battle through the rule that partners' ids differ by 2; and Pollen Puff heals an ally, told apart with Platinum's `IfSameSide`, the way Present heals
    - **Seven more (2026-09-22).** Heavy Slam and Heat Crash take their power from the weight ratio through a new `CalcHeavySlamPower` (Platinum's battlers already carry the weight Low Kick reads), and Autotomize lightens its user through `ReduceWeight`, both named as hg-engine names them so its scripts convert as they are. Strength Sap heals by the target's Attack with its stage applied, through `CalcStrengthSap` and a small `Battler_AttackAfterStage` in `battle_lib.c`, since the stage table is private there. Guard Split and Power Split are hg-engine's subscripts with Platinum's attack message and animation added, run through the direct path as Pain Split is (a `FIXES` entry). Soak and Incinerate are subscripts written for Oxide: Soak fails against a substitute, Multitype or a target already pure Water; Incinerate burns a berry, Platinum's berries being one run of item ids. A substitute now blocks Guard Split, Power Split, Soak and Heal Pulse, as hg-engine's C has it. Twelve more donor strings, 1,291 to 1,302. The converter maps hg-engine's `BMON_DATA_` names to Platinum's battler fields by number; all 101 agree in meaning at the same number
    - **Dragon Tail, Circle Throw and Parting Shot (2026-09-22).** Dragon Tail and Circle Throw drag the target out after the hit through a subscript built from Roar's switch and a new `TryDragonTail`, which applies the later games' rule: in a trainer battle the target is always dragged out when its trainer has another Pokemon, and a wild battle ends unless the target is the higher level (a wild double battle is left alone). Its party pick is shared with Roar's `TryWhirlwind` through a helper, `BattleScript_PickDraggedOutMon`, which with the level check on is the vanilla code with the same random-number calls in the same order. Parting Shot lowers Attack and Special Attack as Tickle does, then switches its user out as Baton Pass does without passing anything; the user stays in behind a substitute, when both stats are already at their lowest, or with nothing to switch to. Test-kit set 23 carries all three, with Roar. **Found, present in vanilla, not fixed**: Roar's and Whirlwind's subscript tests Ingrain with an equality on the whole move-effects mask (`main`, `subscript_force_target_to_switch_or_flee.s` line 7), so an Ingrained target is dragged out anyway whenever it has any other move effect up; Oxide's Dragon Tail subscript tests the flag. Whether to fix Roar is Ian's call
    - **Laser Focus (2026-09-22).** The user's move on the next turn is a critical hit, under the same Battle Armor, Shell Armor and Lucky Chant rules as the always-crit moves. The state is a two-bit countdown in bits 19 and 20 of the battler's move-effects mask, which Platinum leaves unused: the subscript sets it to 2 and the end-of-turn check runs it down beside Lock-On's identical countdown, so it covers exactly the next turn and clears when the battler switches out. A first version kept a turn number in `MoveEffectsData`, but growing that struct moves every later field of the battle context and so changed the trainer AI overlay in ways `romdiff.py` cannot explain; the mask bits leave the struct alone, and the build differs from 38766c70a only in `be_seq` member 399, one new subscript, the battle strings bank and an overlay 16 relink, every other difference explained. Three donor strings for "concentrated intensely", 1,309 to 1,311. Test-kit set 24 pairs it with Tackle and, since the two share the countdown, Lock-On and Zap Cannon
    - **Next, the rest of the focused pass (6).** After You, Sticky Web and Aurora Veil need a command or new battle state each. The C group's Wide Guard set, Belch (a per-battler record of a berry eaten) and Smack Down (a grounded state). Held back: Entrainment for element 5, and terrain, including Steel Roller and Ice Spinner, for Ian's decision
    - **Two follow-ups from the test kit, done (Ian, 2026-09-22).** A three-stage change now prints the donor's "rose drastically" or "severely fell" (ally, wild and foe, appended as 1,303 to 1,308), chosen in `ChangeStatStage` only when the change is three stages, so one- and two-stage lines print exactly as before. And Freeze Shock, Ice Burn and Geomancy have a charge part in their `anim.s`: a glow on the user on the first turn, branching on the move's effect chance as Sky Attack's and Solar Beam's do (even on the charge turn, 1 on the second, set by the charge cleanup), with the borrowed animation kept for the second turn. Geomancy plays its own second-turn animation before that cleanup, so its script now sets the chance to 1 first (a `FIXES` entry too). The three are noted in `docs/oxide/move-animation-map.json`, since `import_moves.py --force` would overwrite them. Meteor Beam and Electro Shot need the same when their effects land
    - Two natives want two of the same set, Tail Glow and Howl, which Hardlove gave hg-engine effects. Until a script is written the move does its damage and skips its extra, or says "But nothing happened!" if it is a status move
  - [x] **King's Rock on every damaging move (Ian, 2026-09-22).** The new moves all carried the flag and 95 native damaging moves did not, so the natives got it too; `verify_narcs.py` lists them as an intended divergence from the base ROM
  - [ ] **Left open by the 2026-09-22 QA** (`docs/oxide/qa-review-2026-09-22.md` has the detail). `RANGE_ALL` has no branch in `BattleSystem_Defender`, so Rototiller, Flower Shield and Teatime fall through until their effects are written. Five computed-power moves are 0-power until their effects exist, and Metronome can still pick them. The `.shared` scripts under `res/moves/` are dead but still assembled; remove them with the moves build file once two builds agree. The TV segment's random move can still name a placeholder record, which is cosmetic

  - [x] **The 159 new species' learnsets, refilled (2026-09-23).** Element 3 kept only moves up to id 467; now that the table runs to 922, the donor's full lists are back: 525 entries restored across 150 species (9 had lost nothing), 2,571 in all, none over the 34-entry cap. Every current learnset first matched the filtered donor list exactly, so nothing hand-edited was overwritten, and no evolution had been dropped for a missing move. Written in place, one field per file, after proving the splice reproduced all 159 files byte for byte. **Verified by decoding the built `wotbl.narc`**: all 2,571 entries match their JSON as (level, move id) pairs, and 361 carry a move id above 511, the old packed format's cap, so this is the first real test of the widened entry and it holds. Level-0 evolution moves still become level 1, as in element 3, so an evolved Pokemon gets them only from the Move Relearner. 17 more repeated moves, harmless, since the relearner skips a move it has already listed and 425 repeats already existed
  - [ ] **Emulator test for the widened learnset** (Ian): level a Pokemon through a move it should learn and confirm it learns the right move at the right level, then open the Move Relearner and confirm the list is right. A new save is not needed; nothing in the save moved
- [ ] **5. Ability effects.** Reimplement the new abilities' behaviour in the decomp's battle code from hg-engine's `ability.c` and `individual/*.c`
- [ ] **6. Battle AI.** Platinum's own AI (`src/battle/trainer_ai/`) is the baseline; Hardlove's AI is not ported.
  - **Taken on by the encounter-track session on Ian's instruction (2026-09-22)**, on branch `worktree-element6` (integrated into `oxide` the same day), to move it forward while element 4 finishes. If the Phase 4 session completes elements 4 and 5 before element 6 is done, element 6 goes back to it: the branch and `docs/oxide/battle-ai/` are the hand-over. Two standing rules from Ian for this element: every bug found is labelled present in vanilla Platinum or introduced by Oxide, and **any fix to a vanilla bug is called out explicitly** (the eleven battle_edits fixes are vanilla fixes, each to be named when applied)
  - [x] **Understood, 2026-09-22: `docs/oxide/battle-ai/`** (README first, then one file per part). About 70 bugs, all but one present in vanilla; the Oxide one is the AI's remembered-ability byte, still `u8` after element 2. The README's table lists the ones that change what Oxide's trainers do, and which battle_edits fix sits where. Two facts that reshape the rest of the element: wild Pokemon never run the AI (they pick a random move, `battle_display.c` lines 3586 to 3612), so wild doubles do not exercise it; and the AI's per-move damage roll is never used, every damage check taking the top of the range
  - [x] **Understand it first (Ian, 2026-09-20).** Ian wants a rigorous understanding of how the trainer AI actually works, and is interested in fixing its bugs and making small deliberate changes. So: read `src/battle/trainer_ai/` and write down what each scoring function does, what the AI flags select, and how the thresholds interact, to the point where a change can be **predicted before it is made**. References Ian uses: lhearachel's Gen 4 AI gist (`ff61af1f58c84c96592b0b8184dba096`), `pokemow.com/Gen4/TrainerAI/` and its switching page. Note 350 carried-over trainers had their AI flags changed by the base ROM, so the flags in play are already non-vanilla and the write-up needs to say what each one does
  - [ ] Keep fixes and changes separate: a fix restores what the code plainly intended, a change makes the AI play differently and is Ian's call
  - **Fixes so far (2026-09-22), each its own commit:** the Oxide ability byte (`AIContext.battlerAbilities` to u16), and six vanilla fixes Ian approved: the Weather flag, immunity checks for damaging moves outside the damage comparison, Punishment's ladder, Trick, Switcheroo and Gastro Acid on the partner, Weather Ball's unset type in clear weather, and Weather Ball in the AI's damage estimate. Ian kept three others as vanilla has them. `docs/oxide/battle-ai/README.md` lists them
  - **Open from the QA pass before the merge (2026-09-22, `docs/oxide/qa-review-2026-09-22-element6.md`).** Every fix does what its message says. The `docs/oxide/battle-ai/` files predate the fixes: they still call `script.s` byte-identical to `main`, their line numbers are off, and four statements are now wrong (Weather Ball in `switching-and-items.md`, and three in the README). Two commit messages understate their reach: the immunity fix routes 52 moves through the checks, not the 12 it names, and the Weather Ball type fix also changes AI switching through `Move_CalcVariableType`. Weather Ball is still costed as a 50-power Normal move in the replacement pick after a knockout (`battle_lib.c`, near 8124) and in the four readers of a move's listed type, so a rain Weather Ball into Water Absorb is not refused (vanilla gaps, not regressions). **Ian ruled to fix both (2026-09-22)**; they are this element's next fixes, each its own VANILLA FIX commit. The Camper Zackary check under Waiting on Ian was worded loosely and now reads the tighter way.
  - [ ] Teach it the new moves and abilities, and the held items that change a decision (Eviolite, Assault Vest, Air Balloon, Rocky Helmet, Weakness Policy, the seeds)
  - [ ] Wild double battles are in, so the doubles-specific scoring gets exercised far more than in vanilla and needs the same scrutiny as singles
  - [ ] Apply the eleven ds-pokemon-hacking battle_edits fixes (Ian confirmed the full list, 2026-09-15): Fire Fang vs Wonder Guard, Rage Glitch, Water Immunity vs Dry Skin check, Sunny Day check, Foresight/Odor Sleuth Ghost check, Facade status check, Leaf Guard Sunny Day logic, Water Spout/Eruption HP check, charge-turn scoring fix, Thunder scoring fix, Discharge double-battle scoring fix. **These are the end of the AI work, not the start**: each should be explainable from the code before it is applied, not pattern-matched from the guide
- [ ] **7. Items, a curated subset.** Sits after the move expansion, because several held items reference new moves and abilities. New items go into Platinum's free slots rather than taking Hardlove's 2,687-record table; widen only if the list outgrows the free slots. In and out lists are in the answers file. Each held item needs its battle effect written into Platinum's engine with hg-engine's C as reference. **The TM count must not be hard-capped at 92** - how many TMs and which moves is a Phase 5 balance item, but the mechanism has to allow more
- [ ] **8. The remaining engine changes**, one at a time: hidden abilities (third slot, the script flag, Ability Patch); **nine-slot evolution records are done, landed with element 3 because Eevee needed the eighth**, but the 17 dead trade entries are still to strip; 30 PC boxes; always-set battle mode; 60 fps outside battle (drop it without asking if it misbehaves); wild double battles; single-use items restored after battle; always national dex; script-driven level caps (Phase 4 delivers the mechanism only); a lowered friendship evolution threshold
- [x] **A faster HP bar (2026-09-20).** Found in the base ROM as Ian expected: `UpdateGauge`'s `*temp -= fillOffset` patched to `*temp -= 2`, and since the HP call passes 1 that is exactly 2x. Implemented at the HP call site rather than in `UpdateGauge`, because the patched instruction is shared with the EXP gauge, whose step is computed and would crawl if forced to 2. HP behaviour is identical
- [x] **The two gameplay rules found in the base ROM (2026-09-20). Ian: keep both.** Chasing the HP bar closed all four ov16 edits the Phase 3 inventory could not explain, and two of them were rules rather than cosmetics. Both are now ported, with one deliberate difference from the base ROM on the EV side:
  - **Traded Pokemon always obey.** `BattleControllerPlayer_CheckObedience`'s badge-scaled thresholds (10/30/50/70) are all set to 255 in the base ROM, so the level test always passes and both disobedience rolls are unreachable. Ported as `OBEY_ALWAYS_MAX_LEVEL`, assigned once after the vanilla ladder so the rule it replaces stays readable in the source
  - **Battling grants no EVs, and vitamins grant 26 each.** The base ROM zeroes the literal 510 in ov16's copy of `MAX_EVS_ALL_STATS`, which makes the payout loop break before it writes anything. That cannot be copied literally here: the decomp has one `MAX_EVS_ALL_STATS`, and `src/item_use_pokemon.c` needs it at 510 to cap the vitamin total. So the battle side gets its own constant, `MAX_EVS_FROM_BATTLE 0` in `src/battle/battle_script.c`, and the loop guard reads that instead. Vitamins then went from 10 EVs to **26** (`itemUseParams` in the six `res/items/data/*.json`), so with `MAX_EV_VITAMIN` at 252 a stat maxes in 10 vitamins and the 510 total in 20. Ian asked for "26 or 52"; 26 was taken for the finer spreads, and 52 is a one-byte change in each of the six files if the vanilla 9,800 price makes 10-per-stat too expensive. Verified against the build: exactly six item records differ from vanilla, one byte each, 10 to 26
  - The fourth edit, `+0x10D58`, is the no-items-in-trainer-battles change and was already ported in Phase 3

## Phase 5: design passes raised while answering Phase 4

All four came out of Ian's 2026-09-20 answers. None is Phase 4 work; all of them
shape the finished game.

- [ ] **Balance analysis** (Ian, 2026-09-22): its own track, status in `docs/oxide/balance-plan.md`; it scores every fight against reference hacks. Ian answered its questions the same day and widened it to the whole game's balance: trainers, level caps, species stats, abilities, learnsets, TMs, item access, and route and gym weather. **The three passes below are that track's work**; their status lives in its plan, not here.

- [ ] **Level-cap split design** (balance track). Per split: the areas, trainers, items, and learnsets/evolutions available. Ian expects this to be a lot of work, and it **must precede the trainer balance pass** because it constrains it. Phase 4 only delivers the mechanism
- [ ] **TM pass** (balance track). How many TMs (likely more than 92) and which moves, done with the overall balance pass
- [ ] **Ability balance pass** (balance track) across all species, including the base ROM's 228 duplicated second slots, which stay as they are until then
- [ ] **Encounter design decisions** that Phase 4 leaves open: which encounters set the hidden-ability flag (gifts, statics, a late area), and which areas have wild double battles. **Decided 2026-09-21 (Ian): swarms, the Poke Radar, the dual-slot GBA lists, the Trophy Garden dailies and the base ROM's Twinleaf legendary menu are never used**, so they are not acquisition sources and `docs/oxide/pokemon-sources.md` leaves them out; what happens to their data (emptied or left as-is) is the authoring pass's call
- [ ] **Verity Lakefront as a capture area** (from the encounter track, 2026-09-21): `res/field/encounters/encounters_verity_lakefront.json` exists and is in the NARC, but `MAP_HEADER_VERITY_LAKEFRONT` still points at `ENCOUNTERS_NONE` and the map has no tall grass. Point the header at the table and add grass to the map
- [ ] **The starter's own met location** (Ian's preference, 2026-09-21): give the starter a unique met-location name so Route 201 counts as a nuzlocke capture area; the encounter plan already assumes it. A script and text-bank change
- [ ] **Route 211 east as a delay** (encounter track, Byron's split): the plan homes Ralts there with Rowlet and Litten at real shares as the prizes; author it with that split. The clown gifts were levers of the same kind and have now been re-pooled, see the next item
- [ ] **Delete the Old Amber, Helix Fossil, Dome Fossil and Claw Fossil** (Ian, 2026-09-21): they revive off-list species, and his call is to remove the items rather than repoint the museum. Take them out of the Underground's dig pools and any route or NPC that hands them out; the museum's four revive branches then become unreachable, which is fine. Item and script work, outside the encounter track
- [ ] **Floette's flower forms** (from the encounter track, 2026-09-21): the Day Care now gives a shiny Floette, and Ian asked for the white-flowered one. The tree has a single Floette with one sprite and one palette, so a white flower is new art and a form record, not a number in a script. Flabébé, Floette and Florges all have the same gap. The shiny Floette stands until then
- [ ] Optional, not scheduled: a ROM-specific PKHeX/PKHaX build. Depends on the save layout being documented as it changes, so keep a `docs/oxide/save-layout.md` listing every block that moved and why

## Phase 5: Verify

- [x] Boots in emulator (Ian's 2026-09-15 run reached Route 202 trainers on this build; re-confirmed 2026-09-20 in melonDS with Fairy and the ability widening in, no crash on boot)
- [x] The save-format break behaves as documented (2026-09-20): Ian's pre-element-2 save loads and every Pokemon in it shows no ability, exactly what `docs/oxide/save-layout.md` predicts. That save is retired as a test bed; everything below is from a new game
- [x] New game to first battle without crash (Ian, 2026-09-20, melonDS, with elements 1 and 2 in: the starter battle ran and Piplup shows an ability on the new save, which is the in-play confirmation that the moved ability field reads and writes correctly). Re-check after each further Phase 4 element
- [ ] **Open bug (Ian, 2026-09-20): UNLOCK FPS set to ALWAYS hard-crashes on entering Sandgem Town**, taking melonDS down with the game. Not seen with the default OFF. The uncap is `ShouldWaitForVBlank` in `src/main.c`, which passes FALSE to `OS_WaitIrq` for the VBlank wait in the main loop when the option is ALWAYS (or BATTLE while the battle overlay is loaded); the selector and the disabled button remapping in `src/system.c` are the whole change. An emulator crashing rather than the game alone points at the ARM9 doing something the emulator cannot model when it never waits for VBlank, most likely a VRAM or DMA transfer racing the display at a map load (Sandgem is the first map with a town-sized transition after the option can be set). Method: the `debug-live` skill. Reproduce with `~/roms/route202-hang.sav`, set ALWAYS, walk into Sandgem. Until fixed, the uncap should be treated as OFF-only; if it proves opaque, Ian's 2026-09-20 answer allows dropping the 60 fps feature without asking. **New observation (Ian, 2026-09-22, test kit ROM):** with ALWAYS set, warping to Sandgem and walking in did not crash; the game **hung** (melonDS stayed up) when Ian stepped on the event trigger where Lucas or Dawn leads the player into the lab. So the failure point may be that scripted scene rather than the map load, and a hang is traceable with `live_watch.py` where a crash was not. Not yet repeated with OFF as a control
- [x] **Pokemon Center nurse hang on "yes": fixed by the whiteout fix below, confirmed gone by Ian in melonDS (2026-09-21).** Its "yes" branch applies `Common_Movement_3012`, one of the seven `scripts_common` movement blocks the Repel edit pushed to an odd offset, and all seven are realigned in commit bb0c45993. Test: new game, first Pokemon Center, answer yes.
- [ ] **Open bug (Ian, 2026-09-21): backing out of the box deposit screen hangs. Still hangs on the realigned build (Ian, 2026-09-21), so it is not the `scripts_common` alignment bug and is the bug track's first open item.** On a new save. Not traced yet. The PC is a map object rather than a common script, and the base ROM added a PC entry, `Common_Entry58`, behind the shortened region, so the first read is: which script runs on leaving the deposit screen, and whether it applies a movement from a generated script other than `scripts_common` (those files keep the base ROM's padding and can be odd-aligned for the same reason; the `carry-over-map` skill's gotcha 6 says how to check). If no movement is involved, it is a third thing: trace with `live_watch.py` from the deposit screen, heartbeat first, then `ScriptContext_WaitForMovement`, `MapObject_HasAnimationEnded` and the PC overlay's exit
- [x] **Whiteout hang, found and fixed 2026-09-21, confirmed by Ian in melonDS the same night** (player and Mom turn to face each other, her lines play). The Repel fix shortened `scripts_common` by 15 bytes and pushed its seven movement blocks to odd offsets; the fix is `.balign 4, 0` before every `Common_Movement_*` label in `scripts_common.s`. The root cause, the stub method that found it and the rule for every generated script are in the design doc's findings log (2026-09-21) and the `carry-over-map` skill, gotcha 6. The three UNLOCK FPS and Sandgem items are unaffected
- [ ] A ported element visibly works in-game. Fairy is now testable: Clefairy and Ralts are both early and both Fairy, so a Dragon move against one should do nothing at all, a Poison or Steel move should do double, and the summary screen should read FAIRY. Charm, Sweet Kiss and Moonlight are Fairy now too, though all three are status moves, so the damage chart can only be checked from the defending side until a damaging Fairy move exists
- [ ] Check the Poketch move tester agrees with the battle engine on Fairy, and that the Pokedex info page on a Fairy Pokemon shows the NORMAL plate rather than garbage (the known gap, not a crash)

## Backlog / follow-ups

- **Live inspection of the running game (Ian, 2026-09-20).** A `tools/oxide/live.py` that speaks the GDB remote protocol to melonDS's stub directly (no GDB needed for reads): halt, continue, registers, memory by symbol from `build/main.nef.xMAP`, and decoders on top for the party and boxes (Gen 4 block shuffle and encryption, with the ability field where `save-layout.md` says), script flags and vars, current map and player position, the running script's state, and writes for a debug console (set a flag, warp, give an item or a Pokemon, set an ability). Every Phase 4 emulator test then becomes something an agent can run itself. Second piece: an offline reader for the melonDS `.sav` (raw flash image, layout from the decomp's save code) so the encounter tool can take its caught list from the real dex flags and Phase 4 save changes can be checked against a real save. The stub connection is done (`live.py`, `live_watch.py`, recipe in `docs/oxide/setup-fork-and-wsl2.md` part 5b); the decoders and the `.sav` reader are what is left
- **Re-humanise the 86 generated scripts**, a map at a time, as each map gets attention: named labels, `NPCMessage`, text-bank constants, real names for `LOCALID_OBJECT_<n>` and `<Bank>_Text_<n>`. `checkmap.py` is the check. Never during a faithful carry-over; the point of the bulk pass was to be done
- **Unify the clown-gift events** (Ian, 2026-09-20). Roughly a dozen maps carry the same Clown who hands out a Pokemon, written out longhand each time and not even in a consistent block order: some roll the die first, some give first, and `sandgem_town_house` still uses a list menu where the rest were converted to a random roll. Collapse them into one common script parameterised by the map, and have it **name the possible Pokemon** rather than rolling blind, so the player can see what is on offer. The full set of species per map is catalogued in `docs/oxide/pokemon-gifts.md`; the choice names are mostly still in each map's text bank, orphaned, from the pick versions. Do this after the faithful carry-over, not during it, so the two are separable in the history. Orphaned pick-event names still to remove: canalave_library_2f 7, pastoria_city_north_house 6, floaroma_town_middle_house 4, eterna_city_condominiums_1f 3, floaroma_meadow_house 3, jubilife_city_south_house_1f 3, solaceon_town_northeast_house 3
- Two raw regions the disassembler emits as `.byte` rather than decoding (`scripts_spear_pillar` 0x04b5 and `scripts_common` 0x1268 are the two to start from). They round-trip exactly; they are just not understood
- The two trade json files still named `abra` and `haunter` for what are now a Ditto and a Gengar
- Seventeen dead trade-evolution entries (methods 5 and 6) left beside the non-trade routes the base ROM added; harmless in single player, stripping them would free evolution slots on Clamperl
- Move `Platinum Oxide VSMaker2 Data\` out of the DSPRE contents folder (Ian's call; it is a stale duplicate)
- Update the chat-surface doc `claude/engine-tooling-comparison.md`: plat-engine is a patch kit not a decomp build; pokeplatinum is matching and active
- Palette hue-shift patch (IV/nature-driven shiny or per-form palette, base ROM's eight palette-loading hooks): low priority, investigate only after Phase 4 is done; drop if it proves opaque (Ian, 2026-09-15)

## Decisions made

Each decision lives with the fact it changed: the design doc's sections 3, 4 and 5 and the file each of them points at. The list that used to sit here duplicated those and was removed in the 2026-09-21 docs pass.
